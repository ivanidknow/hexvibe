# Java / Spring

## Stack overview

Server-side **Java** with **Spring**-style patterns: injection, deserialization, JWT, multipart, and path handling. Metrics are prefixed **`JAVA`**.

## Top threats

- Code/exec and SpEL/Jackson risks (`JAVA-001`–`JAVA-011`).
- XXE and Spring Security misconfig (`JAVA-012`–`JAVA-014`).
- Open redirect, JWT checks, crypto (`JAVA-015`–`JAVA-020`).

## Pattern catalog

Complete Anti-Pattern / Safe-Pattern definitions live in [`patterns.md`](patterns.md). The table below is a **table of contents** by metric ID.

| ID | Metric | Stack |
|---|---|---|
| `JAVA-001` | Java Eval Injection: выполнение выражения из пользовательского ввода | `String expr = request.getParameter("expr");` `if (!expr.matches("^[0-9+\\-*/(). ]{1,64}$")) throw new IllegalArgumentException();` `...` `Object result = safeMathEval(expr);` |
| `JAVA-002` | Runtime Exec Injection: `Runtime.getRuntime().exec` со строкой команды | `String host = req.getParameter("host");` `if (!host.matches("^[a-zA-Z0-9.-]{1,255}$")) throw new IllegalArgumentException();` `...` `new ProcessBuilder("ping","-c","1",host).start();` |
| `JAVA-003` | ProcessBuilder Command Injection: shell-строка через `/bin/sh -c` | `String action = req.getParameter("action");` `Map<String,List<String>> allowed = Map.of("uptime", List.of("uptime"));` `...` `new ProcessBuilder(allowed.get(action)).start();` |
| `JAVA-004` | Unsafe Reflection: загрузка класса из пользовательского ввода | `String key = req.getParameter("handler");` `Map<String,Class<?>> allow = Map.of("health", HealthHandler.class);` `...` `Class<?> c = allow.get(key);` |
| `JAVA-005` | Method Invocation Injection: вызов произвольного метода через reflection | `String method = req.getParameter("method");` `Set<String> allow = Set.of("health","status");` `if (!allow.contains(method)) throw new SecurityException();` `...` `target.getClass().getMethod(method).invoke(target);` |
| `JAVA-006` | JDBC Command Composition: SQL/command фрагмент из input без allowlist | `String order = req.getParameter("order");` `if (!Set.of("name","created_at").contains(order)) order = "name";` `...` `String q = "SELECT * FROM users ORDER BY " + order;` |
| `JAVA-007` | SpEL Injection: expression parser на пользовательских данных | `String key = req.getParameter("key");` `Map<String,String> allow = Map.of("env","prod");` `...` `return allow.getOrDefault(key, "n/a");` |
| `JAVA-008` | Nashorn/Graal JS Injection: выполнение произвольного JS кода | `String cmd = req.getParameter("cmd");` `if (!Set.of("normalize").contains(cmd)) throw new SecurityException();` `...` `runFixedJsFunction(cmd);` |
| `JAVA-009` | SpEL Injection (Spring): expression из запроса исполняется в контексте | `String key = request.getParameter("key");` `Map<String,Object> allowed = Map.of("health", true);` `...` `return allowed.getOrDefault(key, false);` |
| `JAVA-010` | Jackson Unsafe Deserialization: default typing на недоверенных данных | `ObjectMapper mapper = new ObjectMapper();` `...` `mapper.disableDefaultTyping();` `UserDTO obj = mapper.readValue(body, UserDTO.class);` |
| `JAVA-011` | Log4j/JNDI Deserialization Risk: логирование сырых user-строк | `String msg = request.getParameter("msg");` `String safe = msg.replace("${", "\\${");` `...` `logger.error("msg={}", safe);` |
| `JAVA-012` | XXE in XML Parsers: внешние сущности не запрещены | `DocumentBuilderFactory f = DocumentBuilderFactory.newInstance();` `f.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);` `f.setFeature("http://xml.org/sax/features/external-general-entities", false);` `f.setFeature("http://xml.org/sax/features/external-parameter-entities", false);` |
| `JAVA-013` | Insecure Spring Security: `permitAll()` на критичных эндпоинтах | `http.authorizeHttpRequests(auth -> auth` `    .requestMatchers("/admin/**").hasRole("ADMIN")` `);` |
| `JAVA-014` | CSRF Disabled Globally в stateful приложении | `http.csrf(csrf -> csrf` `    .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())` `);` |
| `JAVA-015` | Open Redirect in Spring MVC | `String next = request.getParameter("next");` `if (!next.startsWith("/")) next = "/";` `...` `return "redirect:" + next;` |
| `JAVA-016` | JWT Signature Bypass: no alg check in parser | `JwsHeader<?> h = Jwts.parserBuilder().build().parseClaimsJws(token).getHeader();` `if (!"HS256".equals(h.getAlgorithm())) throw new SecurityException();` `Claims c = Jwts.parserBuilder().setSigningKey(key).build().parseClaimsJws(token).getBody();` |
| `JAVA-017` | Insecure Random for tokens (`java.util.Random`) | `SecureRandom r = new SecureRandom();` `...` `byte[] token = new byte[32];` `r.nextBytes(token);` |
| `JAVA-018` | Hardcoded Secrets in config/code | `String jwtSecret = System.getenv("JWT_SECRET");` `if (jwtSecret == null) throw new IllegalStateException();` |
| `JAVA-019` | Unbounded Multipart Upload (DoS risk) | `if (file.getSize() > 5 * 1024 * 1024) throw new IllegalArgumentException();` `...` `byte[] data = file.getBytes();` |
| `JAVA-020` | Path Traversal in file download | `String p = request.getParameter("path");` `Path target = Paths.get(root, p).normalize();` `if (!target.startsWith(Paths.get(root))) throw new SecurityException();` |
| `JAVA-021` | OWASP Benchmark Java: Cross-Site Scripting (XSS) (CWE-79) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-022` | OWASP Benchmark Java: SQL Injection (CWE-89) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-023` | OWASP Benchmark Java: LDAP Injection (CWE-90) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-024` | OWASP Benchmark Java: Weak Cryptography (CWE-327) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-025` | OWASP Benchmark Java: Weak Hash (CWE-328) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-026` | OWASP Benchmark Java: Weak Randomness (CWE-330) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-027` | OWASP Benchmark Java: Trust Boundary Violation (CWE-501) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-028` | OWASP Benchmark Java: Insecure Cookie (CWE-614) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-029` | OWASP Benchmark Java: XPath Injection (CWE-643) | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-030` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-031` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-032` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-033` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-034` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-035` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-036` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-037` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-038` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-039` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-040` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-041` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-042` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-043` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-044` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-045` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-046` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-047` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-048` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-049` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-050` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-051` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-052` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-053` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-054` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-055` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-056` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-057` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-058` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-059` | Semgrep: The value of `$X` is being ignored and will be used in the conditional test | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-060` | Semgrep: This if statement will always have the same behavior and is therefore unnecess... | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-061` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-062` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-063` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-064` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-065` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-066` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-067` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-068` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-069` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-070` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-071` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-072` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-073` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-074` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-075` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-076` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-077` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-078` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-079` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-080` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-081` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-082` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-083` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-084` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-085` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-086` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-087` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-088` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-089` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-090` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-091` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-092` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-093` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-094` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-095` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-096` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-097` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-098` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-099` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-100` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-101` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-102` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-103` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-104` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-105` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-106` | Semgrep: DOCTYPE declarations are enabled for this DocumentBuilderFactory. This | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-107` | Semgrep: DOCTYPE declarations are enabled for this SAXParserFactory. This | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-108` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-109` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-110` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-111` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-112` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-113` | Semgrep: Default session middleware settings: `setSecure` not set to true. This | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-114` | Semgrep: Default session middleware settings: `setSecure` not set to true. This | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-115` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-116` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-117` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-118` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-119` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-120` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-121` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-122` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-123` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-124` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-125` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-126` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-127` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-128` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-129` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-130` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-131` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-132` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-133` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-134` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-135` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-136` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-137` | Semgrep: User controlled data in a HTML string may result in XSS | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-138` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-139` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-140` | Semgrep: Directory listing/indexing is enabled, which may lead to disclosure of sensiti... | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-141` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-142` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-143` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-144` | Semgrep: Detected use of parseXml() function with the `noent` field set to `true`. This... | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-145` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-146` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-147` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-148` | Semgrep: The following function call $SER.$FUNC accepts user controlled data which can ... | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-149` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-150` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-151` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-152` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-153` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-154` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-155` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-156` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-157` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-158` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-159` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-160` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-161` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-162` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-163` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-164` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-165` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-166` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-167` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-168` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-169` | Semgrep: Found an initialization of the Intercom Messenger that identifies a | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-170` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-171` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-172` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-173` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-174` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-175` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-176` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-177` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-178` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-179` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-180` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-181` | Semgrep: `$X` is assigned twice; the first assignment is useless | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-182` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-183` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-184` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-185` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-186` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-187` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-188` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-189` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-190` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-191` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-192` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-193` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-194` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-195` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-196` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-197` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-198` | Semgrep: Insecure WebSocket Detected. WebSocket Secure (wss) should be used for all Web... | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-199` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-200` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-201` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-202` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-203` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-204` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-205` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-206` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-207` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-208` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-209` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-210` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-211` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-212` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-213` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-214` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-215` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-216` | Semgrep: >- | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-217` | Semgrep: `$X` is assigned twice; the first assignment is useless | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-218` | Semgrep: spring-ftp-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-219` | Semgrep: spring-http-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-220` | Semgrep: disallow-old-tls-versions1 | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-221` | Semgrep: disallow-old-tls-versions2 | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-222` | Semgrep: http-components-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-223` | Semgrep: httpclient-http-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-224` | Semgrep: httpget-http-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-225` | Semgrep: httpurlconnection-http-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-226` | Semgrep: socket-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-227` | Semgrep: tls-renegotiation | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-228` | Semgrep: unirest-http-request | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |
| `JAVA-229` | Semgrep: rest-http-client-support | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. |

## Verification

**Verification:** Check the gold testbed file(s) below for `Vulnerable: <ID>` markers (static Semgrep + `detection-matrix.md` ground truth).

- [`gold-standard-testbed/multi_lang_vulnerable/java_vulnerable.java`](../gold-standard-testbed/multi_lang_vulnerable/java_vulnerable.java)

After changing [`patterns.md`](patterns.md), run from the repo root:

```bash
python scripts/sync_semgrep.py
```

## Workflow: Recon → Scan → Verify

### 1) Recon
- Map entrypoints, data flows, and trust boundaries for this stack.
- Identify which metrics in [`patterns.md`](patterns.md) apply to the code under review.

### 2) Scan
- Run Semgrep with `semgrep-rules/<skill>.yaml` (generated) and correlate with Anti-Patterns.
- Eliminate findings that cannot bind to a metric row.

### 3) Verify
- Confirm markers or scanner hits for touched IDs in the gold testbed when adding metrics.
- Emit findings as `Vulnerable: <PREFIX>-<NNN>` in written reviews.

