| ID | Название метрики | Anti-Pattern (Vulnerable Code/YAML) | Safe-Pattern (Remediation) | Stack | Источник  fix_template | Exploit scenario |
|---|---|---|---|---|---|---|
| JAVA-001 | Java Eval Injection: выполнение выражения из пользовательского ввода | `String expr = request.getParameter("expr");`<br>`...`<br>`Object result = engine.eval(expr);` | `String expr = request.getParameter("expr");`<br>`if (!expr.matches("^[0-9+\\-*/(). ]{1,64}$")) throw new IllegalArgumentException();`<br>`...`<br>`Object result = safeMathEval(expr);` | Java/Spring | `CWE-94` | `String expr = request.getParameter("expr");` `if (!expr.matches("^[0-9+\\-*/(). ]{1,64}$")) throw new IllegalArgumentException();` `...` `Object result = safeMathEval(expr);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-001 java eval injection выполнение выражения из пользовательского ввода string expr request getparameter object result engine if matches 0 9 1 -->
| JAVA-002 | Runtime Exec Injection: `Runtime.getRuntime().exec` со строкой команды | `String host = req.getParameter("host");`<br>`...`<br>`Runtime.getRuntime().exec("ping -c 1 " + host);` | `String host = req.getParameter("host");`<br>`if (!host.matches("^[a-zA-Z0-9.-]{1,255}$")) throw new IllegalArgumentException();`<br>`...`<br>`new ProcessBuilder("ping","-c","1",host).start();` | Java/Spring | `CWE-78` | `String host = req.getParameter("host");` `if (!host.matches("^[a-zA-Z0-9.-]{1,255}$")) throw new IllegalArgumentException();` `...` `new ProcessBuilder("ping","-c","1",host).start();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-002 runtime exec injection getruntime со строкой команды string host req getparameter ping c 1 if matches a za z0 9 -->
| JAVA-003 | ProcessBuilder Command Injection: shell-строка через `/bin/sh -c` | `String cmd = req.getParameter("cmd");`<br>`...`<br>`new ProcessBuilder("sh","-c", cmd).start();` | `String action = req.getParameter("action");`<br>`Map<String,List<String>> allowed = Map.of("uptime", List.of("uptime"));`<br>`...`<br>`new ProcessBuilder(allowed.get(action)).start();` | Java/Spring | `CWE-77` | `String action = req.getParameter("action");` `Map<String,List<String>> allowed = Map.of("uptime", List.of("uptime"));` `...` `new ProcessBuilder(allowed.get(action)).start();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-003 processbuilder command injection shell строка через bin sh c string cmd req getparameter new start action map list allowed of -->
| JAVA-004 | Unsafe Reflection: загрузка класса из пользовательского ввода | `String className = req.getParameter("class");`<br>`...`<br>`Class<?> c = Class.forName(className);` | `String key = req.getParameter("handler");`<br>`Map<String,Class<?>> allow = Map.of("health", HealthHandler.class);`<br>`...`<br>`Class<?> c = allow.get(key);` | Java/Spring | `CWE-470` | `String key = req.getParameter("handler");` `Map<String,Class<?>> allow = Map.of("health", HealthHandler.class);` `...` `Class<?> c = allow.get(key);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-004 unsafe reflection загрузка класса из пользовательского ввода string classname req getparameter class c forname key handler map allow of health -->
| JAVA-005 | Method Invocation Injection: вызов произвольного метода через reflection | `String method = req.getParameter("method");`<br>`...`<br>`target.getClass().getMethod(method).invoke(target);` | `String method = req.getParameter("method");`<br>`Set<String> allow = Set.of("health","status");`<br>`if (!allow.contains(method)) throw new SecurityException();`<br>`...`<br>`target.getClass().getMethod(method).invoke(target);` | Java/Spring | `CWE-74` | `String method = req.getParameter("method");` `Set<String> allow = Set.of("health","status");` `if (!allow.contains(method)) throw new SecurityException();` `...` `target.getClass().getMethod(method).invoke(target);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-005 method invocation injection вызов произвольного метода через reflection string req getparameter target getclass getmethod invoke set allow of health status -->
| JAVA-006 | JDBC Command Composition: SQL/command фрагмент из input без allowlist | `String order = req.getParameter("order");`<br>`...`<br>`String q = "SELECT * FROM users ORDER BY " + order;` | `String order = req.getParameter("order");`<br>`if (!Set.of("name","created_at").contains(order)) order = "name";`<br>`...`<br>`String q = "SELECT * FROM users ORDER BY " + order;` | Java/Spring | `CWE-74` | `String order = req.getParameter("order");` `if (!Set.of("name","created_at").contains(order)) order = "name";` `...` `String q = "SELECT * FROM users ORDER BY " + order;` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-006 jdbc command composition sql фрагмент из input без allowlist string order req getparameter q select from users by if set -->
| JAVA-007 | SpEL Injection: expression parser на пользовательских данных | `String exp = req.getParameter("exp");`<br>`...`<br>`parser.parseExpression(exp).getValue(ctx);` | `String key = req.getParameter("key");`<br>`Map<String,String> allow = Map.of("env","prod");`<br>`...`<br>`return allow.getOrDefault(key, "n/a");` | Java/Spring | `CWE-94` | `String key = req.getParameter("key");` `Map<String,String> allow = Map.of("env","prod");` `...` `return allow.getOrDefault(key, "n/a");` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-007 spel injection expression parser на пользовательских данных string exp req getparameter parseexpression getvalue ctx key map allow of env prod -->
| JAVA-008 | Nashorn/Graal JS Injection: выполнение произвольного JS кода | `String js = req.getParameter("script");`<br>`...`<br>`engine.eval(js);` | `String cmd = req.getParameter("cmd");`<br>`if (!Set.of("normalize").contains(cmd)) throw new SecurityException();`<br>`...`<br>`runFixedJsFunction(cmd);` | Java/Spring | `CWE-95` | `String cmd = req.getParameter("cmd");` `if (!Set.of("normalize").contains(cmd)) throw new SecurityException();` `...` `runFixedJsFunction(cmd);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-008 nashorn graal js injection выполнение произвольного кода string req getparameter script engine eval cmd if set of normalize contains throw -->
| JAVA-009 | SpEL Injection (Spring): expression из запроса исполняется в контексте | `String expr = request.getParameter("expr");`<br>`...`<br>`spelParser.parseExpression(expr).getValue(context);` | `String key = request.getParameter("key");`<br>`Map<String,Object> allowed = Map.of("health", true);`<br>`...`<br>`return allowed.getOrDefault(key, false);` | Java/Spring | `CWE-917` | `String key = request.getParameter("key");` `Map<String,Object> allowed = Map.of("health", true);` `...` `return allowed.getOrDefault(key, false);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-009 spel injection spring expression из запроса исполняется в контексте string expr request getparameter spelparser parseexpression getvalue context key map object -->
| JAVA-010 | Jackson Unsafe Deserialization: default typing на недоверенных данных | `ObjectMapper mapper = new ObjectMapper();`<br>`...`<br>`mapper.enableDefaultTyping();`<br>`Object obj = mapper.readValue(body, Object.class);` | `ObjectMapper mapper = new ObjectMapper();`<br>`...`<br>`mapper.disableDefaultTyping();`<br>`UserDTO obj = mapper.readValue(body, UserDTO.class);` | Java/Spring | `CWE-502` | `ObjectMapper mapper = new ObjectMapper();` `...` `mapper.disableDefaultTyping();` `UserDTO obj = mapper.readValue(body, UserDTO.class);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-010 jackson unsafe deserialization default typing на недоверенных данных objectmapper mapper new enabledefaulttyping object obj readvalue body class disabledefaulttyping userdto -->
| JAVA-011 | Log4j/JNDI Deserialization Risk: логирование сырых user-строк | `String msg = request.getParameter("msg");`<br>`...`<br>`logger.error(msg);` | `String msg = request.getParameter("msg");`<br>`String safe = msg.replace("${", "\\${");`<br>`...`<br>`logger.error("msg={}", safe);` | Java/Spring | `CWE-502` | `String msg = request.getParameter("msg");` `String safe = msg.replace("${", "\\${");` `...` `logger.error("msg={}", safe);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-011 log4j jndi deserialization risk логирование сырых user строк string msg request getparameter logger error replace -->
| JAVA-012 | XXE in XML Parsers: внешние сущности не запрещены | `DocumentBuilderFactory f = DocumentBuilderFactory.newInstance();`<br>`...`<br>`DocumentBuilder b = f.newDocumentBuilder();`<br>`Document d = b.parse(input);` | `DocumentBuilderFactory f = DocumentBuilderFactory.newInstance();`<br>`f.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);`<br>`f.setFeature("http://xml.org/sax/features/external-general-entities", false);`<br>`f.setFeature("http://xml.org/sax/features/external-parameter-entities", false);` | Java/Spring | `CWE-611` | `DocumentBuilderFactory f = DocumentBuilderFactory.newInstance();` `f.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);` `f.setFeature("http://xml.org/sax/features/external-general-entities", false);` `f.setFeature("http://xml.org/sax/features/external-parameter-entities", false);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-012 xxe xml parsers внешние сущности не запрещены documentbuilderfactory f newinstance documentbuilder b newdocumentbuilder document d parse input setfeature http apache -->
| JAVA-013 | Insecure Spring Security: `permitAll()` на критичных эндпоинтах | `http.authorizeHttpRequests(auth -> auth`<br>`    .requestMatchers("/admin/**").permitAll()`<br>`);` | `http.authorizeHttpRequests(auth -> auth`<br>`    .requestMatchers("/admin/**").hasRole("ADMIN")`<br>`);` | Java/Spring | `CWE-306` | `http.authorizeHttpRequests(auth -> auth` `    .requestMatchers("/admin/**").hasRole("ADMIN")` `);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-013 insecure spring security permitall на критичных эндпоинтах http authorizehttprequests auth requestmatchers admin hasrole -->
| JAVA-014 | CSRF Disabled Globally в stateful приложении | `http.csrf(csrf -> csrf.disable());` | `http.csrf(csrf -> csrf`<br>`    .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())`<br>`);` | Java/Spring | `CWE-352` | `http.csrf(csrf -> csrf` `    .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())` `);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-014 csrf disabled globally в stateful приложении http disable csrftokenrepository cookiecsrftokenrepository withhttponlyfalse -->
| JAVA-015 | Open Redirect in Spring MVC | `String next = request.getParameter("next");`<br>`...`<br>`return "redirect:" + next;` | `String next = request.getParameter("next");`<br>`if (!next.startsWith("/")) next = "/";`<br>`...`<br>`return "redirect:" + next;` | Java/Spring | `CWE-601` | `String next = request.getParameter("next");` `if (!next.startsWith("/")) next = "/";` `...` `return "redirect:" + next;` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-015 open redirect spring mvc string next request getparameter return if startswith -->
| JAVA-016 | JWT Signature Bypass: no alg check in parser | `Claims c = Jwts.parserBuilder().setSigningKey(key).build().parseClaimsJws(token).getBody();` | `JwsHeader<?> h = Jwts.parserBuilder().build().parseClaimsJws(token).getHeader();`<br>`if (!"HS256".equals(h.getAlgorithm())) throw new SecurityException();`<br>`Claims c = Jwts.parserBuilder().setSigningKey(key).build().parseClaimsJws(token).getBody();` | Java/Spring | `CWE-347` | `JwsHeader<?> h = Jwts.parserBuilder().build().parseClaimsJws(token).getHeader();` `if (!"HS256".equals(h.getAlgorithm())) throw new SecurityException();` `Claims c = Jwts.parserBuilder().setSigningKey(key).build().parseClaimsJws(token).getBody();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-016 jwt signature bypass no alg check parser claims c jwts parserbuilder setsigningkey key build parseclaimsjws token getbody jwsheader h getheader -->
| JAVA-017 | Insecure Random for tokens (`java.util.Random`) | `Random r = new Random();`<br>`...`<br>`String token = Long.toHexString(r.nextLong());` | `SecureRandom r = new SecureRandom();`<br>`...`<br>`byte[] token = new byte[32];`<br>`r.nextBytes(token);` | Java/Spring | `CWE-338` | `SecureRandom r = new SecureRandom();` `...` `byte[] token = new byte[32];` `r.nextBytes(token);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-017 insecure random for tokens java util r new string token long tohexstring nextlong securerandom byte 32 nextbytes -->
| JAVA-018 | Hardcoded Secrets in config/code | `String jwtSecret = "prod-secret-123";` | `String jwtSecret = System.getenv("JWT_SECRET");`<br>`if (jwtSecret == null) throw new IllegalStateException();` | Java/Spring | `CWE-798` | `String jwtSecret = System.getenv("JWT_SECRET");` `if (jwtSecret == null) throw new IllegalStateException();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-018 hardcoded secrets config string jwtsecret prod secret 123 system getenv jwt if null throw new illegalstateexception -->
| JAVA-019 | Unbounded Multipart Upload (DoS risk) | `MultipartFile file = req.getFile("file");`<br>`...`<br>`byte[] data = file.getBytes();` | `if (file.getSize() > 5 * 1024 * 1024) throw new IllegalArgumentException();`<br>`...`<br>`byte[] data = file.getBytes();` | Java/Spring | `CWE-400` | `if (file.getSize() > 5 * 1024 * 1024) throw new IllegalArgumentException();` `...` `byte[] data = file.getBytes();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-019 unbounded multipart upload dos risk multipartfile file req getfile byte data getbytes if getsize 5 1024 throw new illegalargumentexception -->
| JAVA-020 | Path Traversal in file download | `String p = request.getParameter("path");`<br>`...`<br>`Path target = Paths.get(root, p);` | `String p = request.getParameter("path");`<br>`Path target = Paths.get(root, p).normalize();`<br>`if (!target.startsWith(Paths.get(root))) throw new SecurityException();` | Java/Spring | `CWE-22` | `String p = request.getParameter("path");` `Path target = Paths.get(root, p).normalize();` `if (!target.startsWith(Paths.get(root))) throw new SecurityException();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java-020 path traversal file download string p request getparameter target paths get root normalize if startswith throw new securityexception -->
| JAVA-021 | OWASP Benchmark Java: Cross-Site Scripting (XSS) (CWE-79) | `String param = "";`<br>`java.util.Enumeration<String> headers = request.getHeaders("Referer");`<br>`if (headers != null && headers.hasMoreElements()) {`<br>`param = headers.nextElement(); // just grab first element`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`response.setHeader("X-XSS-Protection", "0");`<br>`Object[] obj = {"a", "b"};`<br>`response.getWriter().format(java.util.Locale.US, param, obj);` | `String param = "";`<br>`if (request.getHeader("Referer") != null) {`<br>`param = request.getHeader("Referer");`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`String bar = "alsosafe";`<br>`if (param != null) {`<br>`java.util.List<String> valuesList = new java.util.ArrayList<String>();`<br>`valuesList.add("safe");`<br>`...`<br>`Object[] obj = {"a", bar};` | Java/Spring | `OWASP Benchmark BenchmarkTest00013` | `String param = ""; if (request.getHeader("Referer") != null) { param = request.getHeader("Referer"); param = java.net.URLDecoder.decode(param, "UTF-8"); String bar = "alsosafe"; if (param != null) { java.util.List<String> valuesList = new java.util.ArrayList<String>(); valuesList.add("safe"); ... Object[] obj = {"a", bar};` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 021 owasp benchmark cross site scripting xss cwe 79 string param util enumeration headers request getheaders referer if null hasmoreelements nextelement just grab --> |
| JAVA-022 | OWASP Benchmark Java: SQL Injection (CWE-89) | `String param = "";`<br>`if (request.getHeader("BenchmarkTest00008") != null) {`<br>`param = request.getHeader("BenchmarkTest00008");`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`String sql = "{call " + param + "}";`<br>`try {`<br>`java.sql.Connection connection =`<br>`org.owasp.benchmark.helpers.DatabaseHelper.getSqlConnection();`<br>`...`<br>`response.getWriter().println("Error processing request.");` | `org.owasp.benchmark.helpers.SeparateClassRequest scr =`<br>`new org.owasp.benchmark.helpers.SeparateClassRequest(request);`<br>`String param = scr.getTheValue("BenchmarkTest00052");`<br>`String sql = "{call " + param + "}";`<br>`try {`<br>`java.sql.Connection connection =`<br>`org.owasp.benchmark.helpers.DatabaseHelper.getSqlConnection();`<br>`java.sql.CallableStatement statement =`<br>`...`<br>`response.getWriter().println("Error processing request.");` | Java/Spring | `OWASP Benchmark BenchmarkTest00008` | `org.owasp.benchmark.helpers.SeparateClassRequest scr = new org.owasp.benchmark.helpers.SeparateClassRequest(request); String param = scr.getTheValue("BenchmarkTest00052"); String sql = "{call " + param + "}"; try { java.sql.Connection connection = org.owasp.benchmark.helpers.DatabaseHelper.getSqlConnection(); java.sql.CallableStatement statement = ... response.getWriter().println("Error process...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 022 owasp benchmark sql injection cwe 89 string param if request getheader benchmarktest00008 null net urldecoder decode utf call try --> |
| JAVA-023 | OWASP Benchmark Java: LDAP Injection (CWE-90) | `String param = "";`<br>`java.util.Enumeration<String> headers = request.getHeaders("BenchmarkTest00012");`<br>`if (headers != null && headers.hasMoreElements()) {`<br>`param = headers.nextElement(); // just grab first element`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`org.owasp.benchmark.helpers.LDAPManager ads = new org.owasp.benchmark.helpers.LDAPManager();`<br>`try {`<br>`String base = "ou=users,ou=system";`<br>`...`<br>`} catch (Exception e) {` | `String param = "";`<br>`if (request.getHeader("BenchmarkTest00138") != null) {`<br>`param = request.getHeader("BenchmarkTest00138");`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`String bar;`<br>`int num = 106;`<br>`bar = (7 * 18) + num > 200 ? "This_should_always_happen" : param;`<br>`org.owasp.benchmark.helpers.LDAPManager ads = new org.owasp.benchmark.helpers.LDAPManager();`<br>`...`<br>`} catch (Exception e) {` | Java/Spring | `OWASP Benchmark BenchmarkTest00012` | `String param = ""; if (request.getHeader("BenchmarkTest00138") != null) { param = request.getHeader("BenchmarkTest00138"); param = java.net.URLDecoder.decode(param, "UTF-8"); String bar; int num = 106; bar = (7 * 18) + num > 200 ? "This_should_always_happen" : param; org.owasp.benchmark.helpers.LDAPManager ads = new org.owasp.benchmark.helpers.LDAPManager(); ... } catch (Exception e) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 023 owasp benchmark ldap injection cwe 90 string param util enumeration headers request getheaders benchmarktest00012 if null hasmoreelements nextelement just grab first element --> |
| JAVA-024 | OWASP Benchmark Java: Weak Cryptography (CWE-327) | `String param = "";`<br>`if (request.getHeader("BenchmarkTest00005") != null) {`<br>`param = request.getHeader("BenchmarkTest00005");`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`java.security.SecureRandom random = new java.security.SecureRandom();`<br>`byte[] iv = random.generateSeed(8); // DES requires 8 byte keys`<br>`try {`<br>`javax.crypto.Cipher c = javax.crypto.Cipher.getInstance("DES/CBC/PKCS5Padding");`<br>`...`<br>`e.printStackTrace(response.getWriter());` | `javax.servlet.http.Cookie[] theCookies = request.getCookies();`<br>`String param = "noCookieValueSupplied";`<br>`if (theCookies != null) {`<br>`for (javax.servlet.http.Cookie theCookie : theCookies) {`<br>`if (theCookie.getName().equals("BenchmarkTest00054")) {`<br>`param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8");`<br>`break;`<br>`String bar = param;`<br>`...`<br>`response.getWriter()` | Java/Spring | `OWASP Benchmark BenchmarkTest00005` | `javax.servlet.http.Cookie[] theCookies = request.getCookies(); String param = "noCookieValueSupplied"; if (theCookies != null) { for (javax.servlet.http.Cookie theCookie : theCookies) { if (theCookie.getName().equals("BenchmarkTest00054")) { param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8"); break; String bar = param; ... response.getWriter()` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 024 owasp benchmark weak cryptography cwe 327 string param if request getheader benchmarktest00005 null net urldecoder decode utf security securerandom random new byte --> |
| JAVA-025 | OWASP Benchmark Java: Weak Hash (CWE-328) | `javax.servlet.http.Cookie[] theCookies = request.getCookies();`<br>`String param = "noCookieValueSupplied";`<br>`if (theCookies != null) {`<br>`for (javax.servlet.http.Cookie theCookie : theCookies) {`<br>`if (theCookie.getName().equals("BenchmarkTest00003")) {`<br>`param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8");`<br>`break;`<br>`try {`<br>`...`<br>`.println(` | `String param = "";`<br>`java.util.Enumeration<String> names = request.getHeaderNames();`<br>`while (names.hasMoreElements()) {`<br>`String name = (String) names.nextElement();`<br>`if (org.owasp.benchmark.helpers.Utils.commonHeaders.contains(name)) {`<br>`continue; // If standard header, move on to next one`<br>`java.util.Enumeration<String> values = request.getHeaders(name);`<br>`if (values != null && values.hasMoreElements()) {`<br>`...`<br>`.println(` | Java/Spring | `OWASP Benchmark BenchmarkTest00003` | `String param = ""; java.util.Enumeration<String> names = request.getHeaderNames(); while (names.hasMoreElements()) { String name = (String) names.nextElement(); if (org.owasp.benchmark.helpers.Utils.commonHeaders.contains(name)) { continue; // If standard header, move on to next one java.util.Enumeration<String> values = request.getHeaders(name); if (values != null && values.hasMoreElements()) ...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 025 owasp benchmark weak hash cwe 328 javax servlet http cookie thecookies request getcookies string param nocookievaluesupplied if null for thecookie getname equals --> |
| JAVA-026 | OWASP Benchmark Java: Weak Randomness (CWE-330) | `String param = request.getParameter("BenchmarkTest00023");`<br>`if (param == null) param = "";`<br>`float rand = new java.util.Random().nextFloat();`<br>`String rememberMeKey = Float.toString(rand).substring(2); // Trim off the 0. at the front.`<br>`String user = "Floyd";`<br>`String fullClassName = this.getClass().getName();`<br>`String testCaseNumber =`<br>`fullClassName.substring(`<br>`...`<br>`+ "<br/>");` | `String param = "";`<br>`java.util.Enumeration<String> names = request.getHeaderNames();`<br>`while (names.hasMoreElements()) {`<br>`String name = (String) names.nextElement();`<br>`if (org.owasp.benchmark.helpers.Utils.commonHeaders.contains(name)) {`<br>`continue; // If standard header, move on to next one`<br>`java.util.Enumeration<String> values = request.getHeaders(name);`<br>`if (values != null && values.hasMoreElements()) {`<br>`...`<br>`response.getWriter()` | Java/Spring | `OWASP Benchmark BenchmarkTest00023` | `String param = ""; java.util.Enumeration<String> names = request.getHeaderNames(); while (names.hasMoreElements()) { String name = (String) names.nextElement(); if (org.owasp.benchmark.helpers.Utils.commonHeaders.contains(name)) { continue; // If standard header, move on to next one java.util.Enumeration<String> values = request.getHeaders(name); if (values != null && values.hasMoreElements()) ...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 026 owasp benchmark weak randomness cwe 330 string param request getparameter benchmarktest00023 if null float rand new util random nextfloat remembermekey tostring substring --> |
| JAVA-027 | OWASP Benchmark Java: Trust Boundary Violation (CWE-501) | `javax.servlet.http.Cookie[] theCookies = request.getCookies();`<br>`String param = "noCookieValueSupplied";`<br>`if (theCookies != null) {`<br>`for (javax.servlet.http.Cookie theCookie : theCookies) {`<br>`if (theCookie.getName().equals("BenchmarkTest00004")) {`<br>`param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8");`<br>`break;`<br>`request.getSession().setAttribute(param, "10340");`<br>`...`<br>`+ org.owasp.benchmark.helpers.Utils.encodeForHTML(param)` | `javax.servlet.http.Cookie[] theCookies = request.getCookies();`<br>`String param = "noCookieValueSupplied";`<br>`if (theCookies != null) {`<br>`for (javax.servlet.http.Cookie theCookie : theCookies) {`<br>`if (theCookie.getName().equals("BenchmarkTest00097")) {`<br>`param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8");`<br>`break;`<br>`String bar;`<br>`...`<br>`+ org.owasp.benchmark.helpers.Utils.encodeForHTML(bar)` | Java/Spring | `OWASP Benchmark BenchmarkTest00004` | `javax.servlet.http.Cookie[] theCookies = request.getCookies(); String param = "noCookieValueSupplied"; if (theCookies != null) { for (javax.servlet.http.Cookie theCookie : theCookies) { if (theCookie.getName().equals("BenchmarkTest00097")) { param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8"); break; String bar; ... + org.owasp.benchmark.helpers.Utils.encodeForHTML(bar)` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 027 owasp benchmark trust boundary violation cwe 501 javax servlet http cookie thecookies request getcookies string param nocookievaluesupplied if null for thecookie getname --> |
| JAVA-028 | OWASP Benchmark Java: Insecure Cookie (CWE-614) | `javax.servlet.http.Cookie[] theCookies = request.getCookies();`<br>`String param = "noCookieValueSupplied";`<br>`if (theCookies != null) {`<br>`for (javax.servlet.http.Cookie theCookie : theCookies) {`<br>`if (theCookie.getName().equals("BenchmarkTest00087")) {`<br>`param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8");`<br>`break;`<br>`String bar = "";`<br>`...`<br>`+ org.owasp.esapi.ESAPI.encoder().encodeForHTML(str)` | `String param = "";`<br>`java.util.Enumeration<String> headers = request.getHeaders("BenchmarkTest00016");`<br>`if (headers != null && headers.hasMoreElements()) {`<br>`param = headers.nextElement(); // just grab first element`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`byte[] input = new byte[1000];`<br>`String str = "?";`<br>`Object inputParam = param;`<br>`...`<br>`+ org.owasp.esapi.ESAPI.encoder().encodeForHTML(str)` | Java/Spring | `OWASP Benchmark BenchmarkTest00087` | `String param = ""; java.util.Enumeration<String> headers = request.getHeaders("BenchmarkTest00016"); if (headers != null && headers.hasMoreElements()) { param = headers.nextElement(); // just grab first element param = java.net.URLDecoder.decode(param, "UTF-8"); byte[] input = new byte[1000]; String str = "?"; Object inputParam = param; ... + org.owasp.esapi.ESAPI.encoder().encodeForHTML(str)` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 028 owasp benchmark insecure cookie cwe 614 javax servlet http thecookies request getcookies string param nocookievaluesupplied if null for thecookie getname equals benchmarktest00087 --> |
| JAVA-029 | OWASP Benchmark Java: XPath Injection (CWE-643) | `String param = "";`<br>`if (request.getHeader("BenchmarkTest00207") != null) {`<br>`param = request.getHeader("BenchmarkTest00207");`<br>`param = java.net.URLDecoder.decode(param, "UTF-8");`<br>`String bar = "";`<br>`if (param != null) {`<br>`bar =`<br>`new String(`<br>`...`<br>`+ "'");` | `javax.servlet.http.Cookie[] theCookies = request.getCookies();`<br>`String param = "noCookieValueSupplied";`<br>`if (theCookies != null) {`<br>`for (javax.servlet.http.Cookie theCookie : theCookies) {`<br>`if (theCookie.getName().equals("BenchmarkTest00116")) {`<br>`param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8");`<br>`break;`<br>`String bar = "safe!";`<br>`...`<br>`+ "'");` | Java/Spring | `OWASP Benchmark BenchmarkTest00207` | `javax.servlet.http.Cookie[] theCookies = request.getCookies(); String param = "noCookieValueSupplied"; if (theCookies != null) { for (javax.servlet.http.Cookie theCookie : theCookies) { if (theCookie.getName().equals("BenchmarkTest00116")) { param = java.net.URLDecoder.decode(theCookie.getValue(), "UTF-8"); break; String bar = "safe!"; ... + "'");` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 029 owasp benchmark xpath injection cwe 643 string param if request getheader benchmarktest00207 null net urldecoder decode utf bar --> |
| JAVA-030 | Semgrep: >- | `        AnthropicClient client = AnthropicClient.builder().apiKey("sk-ant-api03-abcdef1234567890").build();` | `        AnthropicClient client2 = AnthropicClient.builder().apiKey(System.getenv("ANTHROPIC_API_KEY")).build();` | Java/Spring | `Semgrep anthropic-hardcoded-api-key-java` | `        AnthropicClient client2 = AnthropicClient.builder().apiKey(System.getenv("ANTHROPIC_API_KEY")).build();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 030 semgrep anthropicclient client builder apikey sk ant api03 abcdef1234567890 build --> |
| JAVA-031 | Semgrep: >- | `const client = new Anthropic({apiKey: "sk-ant-api03-abcdef1234567890"});` | `const client2 = new Anthropic({apiKey: process.env.ANTHROPIC_API_KEY});` | Java/Spring | `Semgrep anthropic-hardcoded-api-key-javascript` | `const client2 = new Anthropic({apiKey: process.env.ANTHROPIC_API_KEY});` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 031 semgrep const client new anthropic apikey sk ant api03 abcdef1234567890 --> |
| JAVA-032 | Semgrep: >- | `    const response = await client.messages.create({`<br>`        model: "claude-sonnet-4-5-20250929",`<br>`        messages: [{ role: "user", content: "Hello" }]`<br>`    });` | `    const response2 = await client.messages.create({`<br>`        model: "claude-sonnet-4-5-20250929",`<br>`        max_tokens: 1024,`<br>`        messages: [{ role: "user", content: "Hello" }]`<br>`    });`<br>`}` | Java/Spring | `Semgrep anthropic-missing-max-tokens-javascript` | `    const response2 = await client.messages.create({         model: "claude-sonnet-4-5-20250929",         max_tokens: 1024,         messages: [{ role: "user", content: "Hello" }]     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 032 semgrep const response await client messages create model claude sonnet 20250929 role user content hello --> |
| JAVA-033 | Semgrep: >- | `    client.messages.create({`<br>`        model: "claude-sonnet-4-5-20250929",`<br>`        max_tokens: 1024,`<br>`        messages: [{role: "user", content: "Hello"}]`<br>`    });`<br>`}`<br>`async function withTry() {`<br>`    try {` | `        const response = await client.messages.create({`<br>`            model: "claude-sonnet-4-5-20250929",`<br>`            max_tokens: 1024,`<br>`            messages: [{role: "user", content: "Hello"}]`<br>`        });`<br>`    } catch (error) {`<br>`        handleError(error);`<br>`    }`<br>`}`<br>`async function withSpecificCatch() {`<br>`    try {` | Java/Spring | `Semgrep anthropic-no-error-handling-javascript` | `        const response = await client.messages.create({             model: "claude-sonnet-4-5-20250929",             max_tokens: 1024,             messages: [{role: "user", content: "Hello"}]         });     } catch (error) {         handleError(error);     } } async function withSpecificCatch() {     try {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 033 semgrep client messages create model claude sonnet 20250929 max tokens 1024 role user content hello --> |
| JAVA-034 | Semgrep: >- | `const client = new CohereClient({token: "abcdef1234567890"});` | `const client2 = new CohereClient({token: process.env.COHERE_API_KEY});` | Java/Spring | `Semgrep cohere-hardcoded-api-key-javascript` | `const client2 = new CohereClient({token: process.env.COHERE_API_KEY});` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 034 semgrep const client new cohereclient token abcdef1234567890 --> |
| JAVA-035 | Semgrep: >- | `    const response = await co.chat({`<br>`        model: "command-a-03-2025",`<br>`        messages: [{ role: "user", content: "Hello" }]`<br>`    });` | `    const response2 = await co.chat({`<br>`        model: "command-a-03-2025",`<br>`        messages: [{ role: "user", content: "Hello" }],`<br>`        safetyMode: "STRICT"`<br>`    });`<br>`}` | Java/Spring | `Semgrep cohere-missing-safety-mode-javascript` | `    const response2 = await co.chat({         model: "command-a-03-2025",         messages: [{ role: "user", content: "Hello" }],         safetyMode: "STRICT"     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 035 semgrep const response await co chat model command 03 2025 messages role user content hello --> |
| JAVA-036 | Semgrep: >- | `        GenerativeModel model = GenerativeModel.builder().apiKey("AIzaSyA1234567890abcdefghijklmnopqrs").build();` | `        GenerativeModel model2 = GenerativeModel.builder().apiKey(System.getenv("GOOGLE_API_KEY")).build();` | Java/Spring | `Semgrep gemini-hardcoded-api-key-java` | `        GenerativeModel model2 = GenerativeModel.builder().apiKey(System.getenv("GOOGLE_API_KEY")).build();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 036 semgrep generativemodel model builder apikey aizasya1234567890abcdefghijklmnopqrs build --> |
| JAVA-037 | Semgrep: >- | `const genAI = new GoogleGenerativeAI("AIzaSyA1234567890abcdefghijklmnopqrs");` | `const genAI2 = new GoogleGenerativeAI(process.env.GOOGLE_API_KEY);` | Java/Spring | `Semgrep gemini-hardcoded-api-key-javascript` | `const genAI2 = new GoogleGenerativeAI(process.env.GOOGLE_API_KEY);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 037 semgrep const genai new googlegenerativeai aizasya1234567890abcdefghijklmnopqrs --> |
| JAVA-038 | Semgrep: >- | `    const response = await model.generateContent({`<br>`        contents: [{ role: "user", parts: [{ text: "Hello" }] }]`<br>`    });` | `    const response2 = await model.generateContent({`<br>`        contents: [{ role: "user", parts: [{ text: "Hello" }] }],`<br>`        safetySettings: safetyConfig`<br>`    });`<br>`}` | Java/Spring | `Semgrep gemini-missing-safety-settings-javascript` | `    const response2 = await model.generateContent({         contents: [{ role: "user", parts: [{ text: "Hello" }] }],         safetySettings: safetyConfig     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 038 semgrep const response await model generatecontent contents role user parts text hello --> |
| JAVA-039 | Semgrep: >- | `    const model = genAI.getGenerativeModel({`<br>`        model: "gemini-pro"`<br>`    });` | `    const model2 = genAI.getGenerativeModel({`<br>`        model: "gemini-pro",`<br>`        systemInstruction: "You are a helpful assistant."`<br>`    });`<br>`}` | Java/Spring | `Semgrep gemini-missing-system-instruction-javascript` | `    const model2 = genAI.getGenerativeModel({         model: "gemini-pro",         systemInstruction: "You are a helpful assistant."     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 039 semgrep const model genai getgenerativemodel gemini pro --> |
| JAVA-040 | Semgrep: >- | `const client2 = new InferenceClient("hf_abcdefghijklmnopqrstuvwxyz1234", { endpointUrl: "https://example.com" });` | `const client3 = new InferenceClient(process.env.HF_TOKEN);` | Java/Spring | `Semgrep huggingface-hardcoded-api-key-javascript` | `const client3 = new InferenceClient(process.env.HF_TOKEN);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 040 semgrep const client2 new inferenceclient hf abcdefghijklmnopqrstuvwxyz1234 endpointurl https example com --> |
| JAVA-041 | Semgrep: >- | `        String hfToken = "hf_abcdefghijklmnopqrstuvwxyz1234";` | `        String apiKey2 = System.getenv("OPENAI_API_KEY");` | Java/Spring | `Semgrep llm-api-key-in-source-java` | `        String apiKey2 = System.getenv("OPENAI_API_KEY");` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 041 semgrep string hftoken hf abcdefghijklmnopqrstuvwxyz1234 --> |
| JAVA-042 | Semgrep: >- | `const hfToken = "hf_abcdefghijklmnopqrstuvwxyz1234";` | `const apiKey2 = process.env.OPENAI_API_KEY;` | Java/Spring | `Semgrep llm-api-key-in-source-javascript` | `const apiKey2 = process.env.OPENAI_API_KEY;` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 042 semgrep const hftoken hf abcdefghijklmnopqrstuvwxyz1234 --> |
| JAVA-043 | Semgrep: >- | `    eval(code);`<br>`}`<br>`async function safeUsage() {`<br>`    const response = await client.chat.completions.create({model: "gpt-4", messages: [{role: "user", content: "Hello"}]});` | `    const content = response.choices[0].message.content;`<br>`    console.log(content);`<br>`}` | Java/Spring | `Semgrep llm-output-to-exec-javascript` | `    const content = response.choices[0].message.content;     console.log(content); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 043 semgrep eval code async function safeusage const response await client chat completions create model gpt messages role user content hello --> |
| JAVA-044 | Semgrep: >- | `const client = new Mistral({apiKey: "mySecretKey123456"});` | `const client2 = new Mistral({apiKey: process.env.MISTRAL_API_KEY});` | Java/Spring | `Semgrep mistral-hardcoded-api-key-javascript` | `const client2 = new Mistral({apiKey: process.env.MISTRAL_API_KEY});` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 044 semgrep const client new mistral apikey mysecretkey123456 --> |
| JAVA-045 | Semgrep: >- | `    const response = await client.chat.complete({`<br>`        model: "mistral-large-latest",`<br>`        messages: [{ role: "user", content: "Hello" }]`<br>`    });` | `    const response2 = await client.chat.complete({`<br>`        model: "mistral-large-latest",`<br>`        messages: [{ role: "user", content: "Hello" }],`<br>`        safePrompt: true`<br>`    });`<br>`}` | Java/Spring | `Semgrep mistral-missing-safe-prompt-javascript` | `    const response2 = await client.chat.complete({         model: "mistral-large-latest",         messages: [{ role: "user", content: "Hello" }],         safePrompt: true     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 045 semgrep const response await client chat complete model mistral large latest messages role user content hello --> |
| JAVA-046 | Semgrep: >- | `        OpenAiService service = new OpenAiService("sk-1234567890abcdef");` | `        OpenAiService service2 = new OpenAiService(System.getenv("OPENAI_API_KEY"));` | Java/Spring | `Semgrep openai-hardcoded-api-key-java` | `        OpenAiService service2 = new OpenAiService(System.getenv("OPENAI_API_KEY"));` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 046 semgrep openaiservice service new sk 1234567890abcdef --> |
| JAVA-047 | Semgrep: >- | `const client2 = new OpenAI({apiKey: "sk-proj-abc123"});` | `const client3 = new OpenAI({apiKey: process.env.OPENAI_API_KEY});` | Java/Spring | `Semgrep openai-hardcoded-api-key-javascript` | `const client3 = new OpenAI({apiKey: process.env.OPENAI_API_KEY});` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 047 semgrep const client2 new openai apikey sk proj abc123 --> |
| JAVA-048 | Semgrep: >- | `    const content = response.choices[0].message.content;` | `    if (response.choices[0].message.refusal) {`<br>`        handleRefusal();`<br>`    } else {`<br>`        const safeContent = response.choices[0].message.content;`<br>`    }`<br>`}` | Java/Spring | `Semgrep openai-missing-refusal-check-javascript` | `    if (response.choices[0].message.refusal) {         handleRefusal();     } else {         const safeContent = response.choices[0].message.content;     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 048 semgrep const content response choices message --> |
| JAVA-049 | Semgrep: >- | `    const response = await client.responses.create({`<br>`        model: "gpt-4.1",`<br>`        input: "Hello, how are you?"`<br>`    });` | `    const response2 = await client.responses.create({`<br>`        model: "gpt-4.1",`<br>`        input: "Hello, how are you?",`<br>`        safety_identifier: "user_abc123"`<br>`    });`<br>`}` | Java/Spring | `Semgrep openai-missing-safety-identifier-javascript` | `    const response2 = await client.responses.create({         model: "gpt-4.1",         input: "Hello, how are you?",         safety_identifier: "user_abc123"     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 049 semgrep const response await client responses create model gpt input hello how are you --> |
| JAVA-050 | Semgrep: >- | `const response = client.chat.completions.create({`<br>`    model: "gpt-4",`<br>`    messages: [`<br>`        {role: "user", content: "Hello"}`<br>`    ]`<br>`});` | `const response2 = client.chat.completions.create({`<br>`    model: "gpt-4",`<br>`    messages: [`<br>`        {role: "system", content: "You are helpful"},`<br>`        {role: "user", content: "Hello"}`<br>`    ]`<br>`});` | Java/Spring | `Semgrep openai-missing-system-message-js` | `const response2 = client.chat.completions.create({     model: "gpt-4",     messages: [         {role: "system", content: "You are helpful"},         {role: "user", content: "Hello"}     ] });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 050 semgrep const response client chat completions create model gpt messages role user content hello --> |
| JAVA-051 | Semgrep: >- | `    client.chat.completions.create({`<br>`        model: "gpt-4",`<br>`        messages: [{role: "user", content: "Hello"}]`<br>`    });`<br>`}`<br>`async function withTry() {`<br>`    try {` | `        const response = await client.chat.completions.create({`<br>`            model: "gpt-4",`<br>`            messages: [{role: "user", content: "Hello"}]`<br>`        });`<br>`    } catch (error) {`<br>`        handleError(error);`<br>`    }`<br>`}`<br>`async function withSpecificCatch() {`<br>`    try {` | Java/Spring | `Semgrep openai-no-error-handling-javascript` | `        const response = await client.chat.completions.create({             model: "gpt-4",             messages: [{role: "user", content: "Hello"}]         });     } catch (error) {         handleError(error);     } } async function withSpecificCatch() {     try {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 051 semgrep client chat completions create model gpt messages role user content hello async function withtry --> |
| JAVA-052 | Semgrep: >- | `        ResultSet resultSet = statement.executeQuery(query);` | `        ResultSet resultSet2 = statement.executeQuery("SELECT * FROM employee");`<br>`      }`<br>`    } catch (SQLException e) {`<br>`      e.printStackTrace();`<br>`      context.getLogger().log("error : " + e);`<br>`    }`<br>`    if (s == "") {`<br>`      s = "Sucess " + String.format("Added %s %s %s %s %s.", request.emp_id, request.month, request.year, request.overtime);`<br>`    }`<br>`    return s;`<br>`  }`<br>`}` | Java/Spring | `Semgrep tainted-sqli` | `        ResultSet resultSet2 = statement.executeQuery("SELECT * FROM employee");       }     } catch (SQLException e) {       e.printStackTrace();       context.getLogger().log("error : " + e);     }     if (s == "") {       s = "Sucess " + String.format("Added %s %s %s %s %s.", request.emp_id, request.month, request.year, request.overtime);     }     return s;   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 052 semgrep resultset statement executequery query --> |
| JAVA-053 | Semgrep: >- | `            String token = JWT.create()`<br>`                .withIssuer("auth0")`<br>`                .sign(Algorithm.none());`<br>`        } catch (JWTCreationException exception){`<br>`            //Invalid Signing configuration / Couldn't convert Claims.`<br>`        }`<br>`    }`<br>`    private static void ok1(String secretKey) {`<br>`        try {` | `            Algorithm algorithm = Algorithm.HMAC256(secretKey);`<br>`            String token = JWT.create()`<br>`                .withIssuer("auth0")`<br>`                .sign(algorithm);`<br>`        } catch (JWTCreationException exception){`<br>`            //Invalid Signing configuration / Couldn't convert Claims.`<br>`        }`<br>`    }`<br>`    public static void main( String[] args )`<br>`    {`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep java-jwt-none-alg` | `            Algorithm algorithm = Algorithm.HMAC256(secretKey);             String token = JWT.create()                 .withIssuer("auth0")                 .sign(algorithm);         } catch (JWTCreationException exception){             //Invalid Signing configuration / Couldn't convert Claims.         }     }     public static void main( String[] args )     { ...     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 053 semgrep string token jwt create withissuer auth0 sign algorithm none catch jwtcreationexception exception invalid signing configuration couldn convert claims --> |
| JAVA-054 | Semgrep: >- | `  @POST`<br>`  @Path("/vulnerable")`<br>`  @Produces(MediaType.APPLICATION_JSON)`<br>`  public Map<String, String> doConcat(Pair pair) {`<br>`    HashMap<String, String> result = new HashMap<String, String>();`<br>`    result.put("Result", pair.getP1() + pair.getP2());`<br>`    return result;`<br>`  }`<br>`  @POST`<br>`  @Path("/count")`<br>`  @Produces(MediaType.APPLICATION_JSON)` | `  @Consumes(MediaType.APPLICATION_JSON)`<br>`  public Map<String, Integer> doCount(ArrayList<Object> elements) {`<br>`    HashMap<String, Integer> result = new HashMap<String, Integer>();`<br>`    result.put("Result", elements.size());`<br>`    return result;`<br>`  }` | Java/Spring | `Semgrep default-resteasy-provider-abuse` | `  @Consumes(MediaType.APPLICATION_JSON)   public Map<String, Integer> doCount(ArrayList<Object> elements) {     HashMap<String, Integer> result = new HashMap<String, Integer>();     result.put("Result", elements.size());     return result;   }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 054 semgrep post path vulnerable produces mediatype application json public map string doconcat pair hashmap result new put getp1 getp2 --> |
| JAVA-055 | Semgrep: >- | `    @GET`<br>`    @Path("/images/{image}")`<br>`    @Produces("images/*")`<br>`    public Response getImage(@javax.ws.rs.PathParam("image") String image) {`<br>`        File file = new File("resources/images/", image); //Weak point`<br>`        if (!file.exists()) {`<br>`            return Response.status(Status.NOT_FOUND).build();`<br>`        }`<br>`        return Response.ok().entity(new FileInputStream(file)).build();`<br>`    }` | `    @GET`<br>`    @Path("/images/{image}")`<br>`    @Produces("images/*")`<br>`    public Response ok(@javax.ws.rs.PathParam("image") String image) {`<br>`        File file = new File("resources/images/", FilenameUtils.getName(image)); //Fix`<br>`        if (!file.exists()) {`<br>`            return Response.status(Status.NOT_FOUND).build();`<br>`        }`<br>`        return Response.ok().entity(new FileInputStream(file)).build();`<br>`    }`<br>`}` | Java/Spring | `Semgrep jax-rs-path-traversal` | `    @GET     @Path("/images/{image}")     @Produces("images/*")     public Response ok(@javax.ws.rs.PathParam("image") String image) {         File file = new File("resources/images/", FilenameUtils.getName(image)); //Fix         if (!file.exists()) {             return Response.status(Status.NOT_FOUND).build();         }         return Response.ok().entity(new FileInputStream(file)).build();     ...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 055 semgrep get path images image produces public response getimage javax ws rs pathparam string file new resources weak point if exists --> |
| JAVA-056 | Semgrep: >- | `        log.info("Current logged in user : " + user.getUsername());`<br>`    }`<br>`    public void logUser(User user) {` | `        log.info("Current logged in user : #0", user.getUsername());`<br>`    }`<br>`}` | Java/Spring | `Semgrep seam-log-injection` | `        log.info("Current logged in user : #0", user.getUsername());     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 056 semgrep log info current logged in user getusername public void loguser --> |
| JAVA-057 | Semgrep: >- | `    protected void danger2(String biz) {`<br>`        String query = "select foo from bar where" + biz + " limit 1";`<br>`        Session session = this.sessionFactory.openSession();`<br>`        try {`<br>`            PreparedStatement ps = session.connection().prepareStatement(query);`<br>`            ResultSet rs = ps.executeQuery();`<br>`            while (rs.next()) {`<br>`                Integer item = rs.getInt("foo");`<br>`            }`<br>`        } catch (SQLException e) {`<br>`...`<br>`        }`<br>`    }` | `    protected void ok(String foo) throws ServletException, IOException {`<br>`        String query = "select foo from bar where ? limit 1";`<br>`        Session session = this.sessionFactory.openSession();`<br>`        try {`<br>`            PreparedStatement ps = session.connection().prepareStatement(query);`<br>`            ps.setString(1,foo);`<br>`            ResultSet rs = ps.executeQuery();`<br>`            while (rs.next()) {`<br>`                return rs.getInt("foo");`<br>`            }`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep find-sql-string-concatenation` | `    protected void ok(String foo) throws ServletException, IOException {         String query = "select foo from bar where ? limit 1";         Session session = this.sessionFactory.openSession();         try {             PreparedStatement ps = session.connection().prepareStatement(query);             ps.setString(1,foo);             ResultSet rs = ps.executeQuery();             while (rs.next()) ...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 057 semgrep protected void danger2 string biz query select foo from bar where limit session this sessionfactory opensession try preparedstatement ps connection preparestatement --> |
| JAVA-058 | Semgrep: >- | `        String jws = Jwts.builder()`<br>`                .setSubject("Bob")`<br>`                .compact();`<br>`    }`<br>`    private static void ok1() {`<br>`        Key key = Keys.secretKeyFor(SignatureAlgorithm.HS256);` | `        String jws = Jwts.builder()`<br>`                .setSubject("Bob")`<br>`                .signWith(key)`<br>`                .compact();`<br>`    }`<br>`    public static void main( String[] args )`<br>`    {`<br>`        bad1();`<br>`        ok1();`<br>`    }`<br>`}` | Java/Spring | `Semgrep jjwt-none-alg` | `        String jws = Jwts.builder()                 .setSubject("Bob")                 .signWith(key)                 .compact();     }     public static void main( String[] args )     {         bad1();         ok1();     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 058 semgrep string jws jwts builder setsubject bob compact private static void ok1 key keys secretkeyfor signaturealgorithm hs256 --> |
| JAVA-059 | Semgrep: The value of `$X` is being ignored and will be used in the conditional test | `        if (myBoolean = true) {`<br>`            continue;`<br>`        }` | `        if (myBoolean) {`<br>`        }`<br>`    }`<br>`}` | Java/Spring | `Semgrep assignment-comparison` | `        if (myBoolean) {         }     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 059 semgrep the value of is being ignored and will be used in conditional test if myboolean true continue --> |
| JAVA-060 | Semgrep: This if statement will always have the same behavior and is therefore unnecessary. | `        if (myBoolean) {`<br>`        }`<br>`        // to prevent constant propagation to assumes`<br>`        // myBoolean is true below`<br>`        myBoolean = arg;` | `        if (myBoolean == myBoolean) {`<br>`            continue;`<br>`        }` | Java/Spring | `Semgrep hardcoded-conditional` | `        if (myBoolean == myBoolean) {             continue;         }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 060 semgrep this if statement will always have the same behavior and is therefore unnecessary myboolean to prevent constant propagation assumes true below --> |
| JAVA-061 | Semgrep: >- | `        if ("hello" == a) return 2;` | `        if (null == "hello") return 12;` | Java/Spring | `Semgrep no-string-eqeq` | `        if (null == "hello") return 12;` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 061 semgrep if hello return --> |
| JAVA-062 | Semgrep: >- | `        KeyGenerator keyGen = KeyGenerator.getInstance("Blowfish");`<br>`        keyGen.init(64);`<br>`    }`<br>`    public void safeKeySize() {` | `        KeyGenerator keyGen = KeyGenerator.getInstance("Blowfish");`<br>`        keyGen.init(128);`<br>`    }`<br>`    public void superSafeKeySize() {` | Java/Spring | `Semgrep blowfish-insufficient-key-size` | `        KeyGenerator keyGen = KeyGenerator.getInstance("Blowfish");         keyGen.init(128);     }     public void superSafeKeySize() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 062 semgrep keygenerator keygen getinstance blowfish init 64 public void safekeysize --> |
| JAVA-063 | Semgrep: >- | `        Cipher c = Cipher.getInstance("AES/CBC/PKCS5Padding");`<br>`        c.init(Cipher.ENCRYPT_MODE, k, iv);`<br>`        byte[] cipherText = c.doFinal(plainText);`<br>`    }`<br>`    protected void ok(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {` | `        Cipher c = Cipher.getInstance("AES/GCM/NoPadding");`<br>`        c.init(Cipher.ENCRYPT_MODE, k, iv);`<br>`        byte[] cipherText = c.doFinal(plainText);`<br>`    }`<br>`}` | Java/Spring | `Semgrep cbc-padding-oracle` | `        Cipher c = Cipher.getInstance("AES/GCM/NoPadding");         c.init(Cipher.ENCRYPT_MODE, k, iv);         byte[] cipherText = c.doFinal(plainText);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 063 semgrep cipher getinstance aes cbc pkcs5padding init encrypt mode iv byte ciphertext dofinal plaintext protected void ok httpservletrequest req httpservletresponse resp throws --> |
| JAVA-064 | Semgrep: >- | `      builder.command("cmd", "/c", userInput);`<br>`      return "foo";`<br>`    }`<br>`    public String okTest() {`<br>`      ProcessBuilder builder = new ProcessBuilder();` | `      builder.command("bash", "-c", "ls");`<br>`      return "foo";`<br>`    }`<br>`}` | Java/Spring | `Semgrep command-injection-process-builder` | `      builder.command("bash", "-c", "ls");       return "foo";     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 064 semgrep builder command cmd userinput return foo public string oktest processbuilder new --> |
| JAVA-065 | Semgrep: >- | `      response.addCookie(c);`<br>`    }`<br>`    return this;`<br>`  }`<br>`  public Response clearCookie(String cookie) {`<br>`    Cookie existingCookie = HttpRequest.getCookie(request.getCookies(), cookie);`<br>`    if (existingCookie != null) {`<br>`      existingCookie.setPath(Constant.cookiePath);`<br>`      existingCookie.setValue("");`<br>`      existingCookie.setMaxAge(0);` | `      response.addCookie(existingCookie);`<br>`    }`<br>`    return this;`<br>`  }`<br>`}` | Java/Spring | `Semgrep cookie-missing-httponly` | `      response.addCookie(existingCookie);     }     return this;   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 065 semgrep response addcookie return this public clearcookie string cookie existingcookie httprequest getcookie request getcookies --> |
| JAVA-066 | Semgrep: >- | `        cookie.setSecure(false);`<br>`        cookie.setHttpOnly(false);`<br>`        response.addCookie(cookie);`<br>`    }`<br>`    @RequestMapping(value = "/cookie5", method = "GET")`<br>`    public void explicitDisable(@RequestParam String value, HttpServletResponse response) {`<br>`	// ignore cookies created by Spring's ResponseCookie builder, since the interface is different`<br>`	Cookie cookie = ResponseCookie.from("name", "value").build();` | `        response.addCookie(cookie);`<br>`    }`<br>`    // test case cf. https://github.com/Dreampie/Resty//blob/9ef059c065d1894c79e7d69c150e588a61eb1cd5/resty-common/src/main/java/cn/dreampie/common/http/HttpResponse.java#L69`<br>`    public Response addCookie(String name, String value, int expiration, boolean httpOnly) {`<br>`    Cookie existingCookie = HttpRequest.getCookie(request.getCookies(), name);`<br>`    if (existingCookie != null) {`<br>`      if (Constant.cookiePath.equals(existingCookie.getPath())`<br>`          \|\| existingCookie.getPath() == null // in some cases cookies set on path '/' are returned with a null path`<br>`          ) {`<br>`        // update existing cookie`<br>`...`<br>`          existingCookie.setDomain(Constant.cookieDomain);`<br>`        }` | Java/Spring | `Semgrep cookie-missing-secure-flag` | `        response.addCookie(cookie);     }     // test case cf. https://github.com/Dreampie/Resty//blob/9ef059c065d1894c79e7d69c150e588a61eb1cd5/resty-common/src/main/java/cn/dreampie/common/http/HttpResponse.java#L69     public Response addCookie(String name, String value, int expiration, boolean httpOnly) {     Cookie existingCookie = HttpRequest.getCookie(request.getCookies(), name);     if (exi...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 066 semgrep cookie setsecure false sethttponly response addcookie requestmapping value cookie5 method get public void explicitdisable requestparam string httpservletresponse --> |
| JAVA-067 | Semgrep: >- | `      String param = httpServletReq.getParameter("foo");`<br>`      log.log(log.getLevel(), param+"bar");`<br>`  }`<br>`}`<br>`public class OkTestLog1 {`<br>`  private final static NotLogger log = new NorLogger();`<br>`  @Override`<br>`  public void doFilter(ServletRequest request, ServletResponse response,`<br>`    FilterChain chain) throws IOException, ServletException {`<br>`      HttpServletRequest httpServletReq = (HttpServletRequest) request;` | `      String param = httpServletReq.getParameter("param");`<br>`      log.info(param);`<br>`  }`<br>`}`<br>`public class OkTestLog2 {`<br>`  @Override`<br>`  public void doFilter(ServletRequest request, ServletResponse response,`<br>`    FilterChain chain) throws IOException, ServletException {`<br>`      Logger log = Logger.getLogger(Logger.GLOBAL_LOGGER_NAME);`<br>`      HttpServletRequest httpServletReq = (HttpServletRequest) request;` | Java/Spring | `Semgrep crlf-injection-logs` | `      String param = httpServletReq.getParameter("param");       log.info(param);   } } public class OkTestLog2 {   @Override   public void doFilter(ServletRequest request, ServletResponse response,     FilterChain chain) throws IOException, ServletException {       Logger log = Logger.getLogger(Logger.GLOBAL_LOGGER_NAME);       HttpServletRequest httpServletReq = (HttpServletRequest) request;` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 067 semgrep string param httpservletreq getparameter foo log getlevel bar public class oktestlog1 private final static notlogger new norlogger --> |
| JAVA-068 | Semgrep: >- | `    Cipher c = Cipher.getInstance("AES/ECB/NoPadding");`<br>`    c.init(Cipher.ENCRYPT_MODE, k, iv);`<br>`    byte[] cipherText = c.doFinal(plainText);`<br>`  }`<br>`  public void noEcbCipher() {` | `    Cipher c = Cipher.getInstance("AES/GCM/NoPadding");`<br>`    c.init(Cipher.ENCRYPT_MODE, k, iv);`<br>`    byte[] cipherText = c.doFinal(plainText);`<br>`  }`<br>`}` | Java/Spring | `Semgrep ecb-cipher` | `    Cipher c = Cipher.getInstance("AES/GCM/NoPadding");     c.init(Cipher.ENCRYPT_MODE, k, iv);     byte[] cipherText = c.doFinal(plainText);   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 068 semgrep cipher getinstance aes ecb nopadding init encrypt mode iv byte ciphertext dofinal plaintext public void noecbcipher --> |
| JAVA-069 | Semgrep: >- | `    byte[] iv = {`<br>`        (byte) 0, (byte) 0, (byte) 0, (byte) 0,`<br>`        (byte) 0, (byte) 0, (byte) 0, (byte) 0,`<br>`        (byte) 0, (byte) 0, (byte) 0, (byte) 0,`<br>`        (byte) 0, (byte) 0, (byte) 0, (byte) 0`<br>`    };`<br>`    public StaticIV2() {`<br>`        IvParameterSpec staticIvSpec = new IvParameterSpec(iv);`<br>`        c.init(Cipher.ENCRYPT_MODE, skeySpec, staticIvSpec, new SecureRandom());`<br>`    }`<br>`...`<br>`public class RandomIV {`<br>`    public RandomIV() {` | `        byte[] iv = new byte[16];`<br>`        new SecureRandom().nextBytes(iv);`<br>`        IvParameterSpec staticIvSpec = new IvParameterSpec(iv); // IvParameterSpec initialized using its own randomizer.`<br>`        c.init(Cipher.ENCRYPT_MODE, skeySpec, staticIvSpec, new SecureRandom());`<br>`    }`<br>`}` | Java/Spring | `Semgrep no-static-initialization-vector` | `        byte[] iv = new byte[16];         new SecureRandom().nextBytes(iv);         IvParameterSpec staticIvSpec = new IvParameterSpec(iv); // IvParameterSpec initialized using its own randomizer.         c.init(Cipher.ENCRYPT_MODE, skeySpec, staticIvSpec, new SecureRandom());     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 069 semgrep byte iv --> |
| JAVA-070 | Semgrep: >- | `    useCipher(Cipher.getInstance("RSA/None/NoPadding"));`<br>`  }`<br>`  public void rsaPadding() {` | `    Cipher.getInstance("RSA/ECB/OAEPWithMD5AndMGF1Padding");`<br>`  }`<br>`}` | Java/Spring | `Semgrep rsa-no-padding` | `    Cipher.getInstance("RSA/ECB/OAEPWithMD5AndMGF1Padding");   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 070 semgrep usecipher cipher getinstance rsa none nopadding public void rsapadding --> |
| JAVA-071 | Semgrep: >- | `public class MyProprietaryMessageDigest extends MessageDigest {`<br>`    @Override`<br>`    protected byte[] engineDigest() {`<br>`        return "";`<br>`    }`<br>`}` | `public class NotMessageDigest {`<br>`    public NotMessageDigest() {`<br>`        System.out.println("");`<br>`    }`<br>`}` | Java/Spring | `Semgrep avoid-implementing-custom-digests` | `public class NotMessageDigest {     public NotMessageDigest() {         System.out.println("");     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 071 semgrep public class myproprietarymessagedigest extends messagedigest override protected byte enginedigest return --> |
| JAVA-072 | Semgrep: >- | `        HttpClient client = new DefaultHttpClient();`<br>`        HttpGet request = new HttpGet("http://google.com");`<br>`        HttpResponse response = client.execute(request);`<br>`    }`<br>`}`<br>`public class SecureWebCrawler {`<br>`    public void crawl(String[] args) throws Exception {` | `        HttpClient client = new SystemDefaultHttpClient();`<br>`        HttpGet request = new HttpGet("http://google.com");`<br>`        HttpResponse response = client.execute(request);`<br>`    }`<br>`}` | Java/Spring | `Semgrep defaulthttpclient-is-deprecated` | `        HttpClient client = new SystemDefaultHttpClient();         HttpGet request = new HttpGet("http://google.com");         HttpResponse response = client.execute(request);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 072 semgrep httpclient client new defaulthttpclient httpget request http google com httpresponse response execute public class securewebcrawler --> |
| JAVA-073 | Semgrep: >- | `    useCipher(Cipher.getInstance("AES/ECB/PKCS5Padding"));`<br>`  }`<br>`  public void ok() {` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");`<br>`  }`<br>`}` | Java/Spring | `Semgrep use-of-aes-ecb` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 073 semgrep usecipher cipher getinstance aes ecb pkcs5padding public void ok --> |
| JAVA-074 | Semgrep: >- | `    useCipher(Cipher.getInstance("Blowfish"));`<br>`  }`<br>`  public void ok() {` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");`<br>`  }`<br>`}` | Java/Spring | `Semgrep use-of-blowfish` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 074 semgrep usecipher cipher getinstance blowfish public void ok --> |
| JAVA-075 | Semgrep: >- | `    useCipher(javax.crypto.Cipher.getInstance("AES"));` | `    useCipher(KeyGenerator.getInstance("AES"));` | Java/Spring | `Semgrep use-of-default-aes` | `    useCipher(KeyGenerator.getInstance("AES"));` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 075 semgrep usecipher javax crypto cipher getinstance aes --> |
| JAVA-076 | Semgrep: >- | `    byte[] hashValue = DigestUtils.getMd5Digest().digest(password.getBytes());`<br>`    return hashValue;`<br>`  }`<br>`  public byte[] ok(String password) {` | `    byte[] hashValue = DigestUtils.getSha512Digest().digest(password.getBytes());`<br>`    return hashValue;`<br>`  }`<br>`}` | Java/Spring | `Semgrep use-of-md5-digest-utils` | `    byte[] hashValue = DigestUtils.getSha512Digest().digest(password.getBytes());     return hashValue;   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 076 semgrep byte hashvalue digestutils getmd5digest digest password getbytes return public ok string --> |
| JAVA-077 | Semgrep: >- | `      java.security.MessageDigest md = java.security.MessageDigest.getInstance("MD5");`<br>`      byte[] input = {(byte) '?'};`<br>`      Object inputParam = param;`<br>`      if (inputParam instanceof String) input = ((String) inputParam).getBytes();`<br>`      if (inputParam instanceof java.io.InputStream) {`<br>`          byte[] strInput = new byte[1000];`<br>`          int i = ((java.io.InputStream) inputParam).read(strInput);`<br>`          if (i == -1) {`<br>`              response.getWriter()`<br>`                      .println(`<br>`...`<br>`  }`<br>`  public byte[] good(String password) {` | `    MessageDigest md5Digest = MessageDigest.getInstance("SHA-512");`<br>`    md5Digest.update(password.getBytes());`<br>`    byte[] hashValue = md5Digest.digest();`<br>`    return hashValue;`<br>`  }`<br>`}` | Java/Spring | `Semgrep use-of-md5` | `    MessageDigest md5Digest = MessageDigest.getInstance("SHA-512");     md5Digest.update(password.getBytes());     byte[] hashValue = md5Digest.digest();     return hashValue;   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 077 semgrep security messagedigest md getinstance md5 byte input object inputparam param if instanceof string getbytes io inputstream strinput new 1000 --> |
| JAVA-078 | Semgrep: >- | `    useCipher(Cipher.getInstance("RC2"));`<br>`  }`<br>`  public void ok() {` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");`<br>`  }`<br>`}` | Java/Spring | `Semgrep use-of-rc2` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 078 semgrep usecipher cipher getinstance rc2 public void ok --> |
| JAVA-079 | Semgrep: >- | `    useCipher(Cipher.getInstance("RC4"));`<br>`  }`<br>`  public void ok() {` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");`<br>`  }`<br>`}` | Java/Spring | `Semgrep use-of-rc4` | `    Cipher.getInstance("AES/CBC/PKCS7PADDING");   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 079 semgrep usecipher cipher getinstance rc4 public void ok --> |
| JAVA-080 | Semgrep: >- | `        double value = java.lang.Math.random();` | `        double value2 = java.security.SecureRandom();`<br>`        String rememberMeKey = Double.toString(value).substring(2); // Trim off the 0. at the front.`<br>`        String user = "Doug";`<br>`        String fullClassName = this.getClass().getName();`<br>`        String testCaseNumber =`<br>`                fullClassName.substring(`<br>`                        fullClassName.lastIndexOf('.') + 1 + "BenchmarkTest".length());`<br>`        user += testCaseNumber;`<br>`        String cookieName = "rememberMe" + testCaseNumber;`<br>`        boolean foundUser = false;`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep weak-random` | `        double value2 = java.security.SecureRandom();         String rememberMeKey = Double.toString(value).substring(2); // Trim off the 0. at the front.         String user = "Doug";         String fullClassName = this.getClass().getName();         String testCaseNumber =                 fullClassName.substring(                         fullClassName.lastIndexOf('.') + 1 + "BenchmarkTest".length(...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 080 semgrep double value lang math random --> |
| JAVA-081 | Semgrep: >- | `        groovyLoader.parseClass(script,"test.groovy");`<br>`        String hardcodedScript = "test.groovy";` | `        shell.parse(hardcodedScript);`<br>`    }`<br>`}` | Java/Spring | `Semgrep dangerous-groovy-shell` | `        shell.parse(hardcodedScript);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 081 semgrep groovyloader parseclass script test groovy string hardcodedscript --> |
| JAVA-082 | Semgrep: >- | `    private void unsafeELTemplate(String message, ConstraintValidatorContext context) {`<br>`         context.disableDefaultConstraintViolation();`<br>`         context`<br>`             .someMethod()`<br>`             .buildConstraintViolationWithTemplate(message)`<br>`             .addConstraintViolation();`<br>`    }` | `    private void safeELTemplate(String message, ConstraintValidatorContext context) {`<br>`         context.disableDefaultConstraintViolation();`<br>`         context`<br>`             .someMethod()`<br>`             .buildConstraintViolationWithTemplate("somestring")`<br>`             .addConstraintViolation();`<br>`    }`<br>`}` | Java/Spring | `Semgrep el-injection` | `    private void safeELTemplate(String message, ConstraintValidatorContext context) {          context.disableDefaultConstraintViolation();          context              .someMethod()              .buildConstraintViolationWithTemplate("somestring")              .addConstraintViolation();     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 082 semgrep private void unsafeeltemplate string message constraintvalidatorcontext context disabledefaultconstraintviolation somemethod buildconstraintviolationwithtemplate addconstraintviolation --> |
| JAVA-083 | Semgrep: >- | `        ResultSet rs = statement.executeQuery();`<br>`    }`<br>`}`<br>`public class SqlExampleNonStringBuilderConstructor{`<br>`    public Retry<ResultSet> getRetry(final String mainQuery, final Connection connection) {`<br>`        // not a StringBuilder`<br>`        return new Retry<>(`<br>`            // also not a StringBuilder`<br>`            new Callable<ResultSet>() {`<br>`                public ResultSet call() throws SQLException {`<br>`...`<br>`                        mainQuery, ResultSet.TYPE_FORWARD_ONLY, ResultSet.CONCUR_READ_ONLY);`<br>`                    statement.setFetchSize(Integer.MIN_VALUE);` | `                    return statement.executeQuery ();`<br>`                }`<br>`            },`<br>`            Retry.RETRY_FOREVER);`<br>`    }`<br>`}` | Java/Spring | `Semgrep formatted-sql-string` | `                    return statement.executeQuery ();                 }             },             Retry.RETRY_FOREVER);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 083 semgrep resultset rs statement executequery public class sqlexamplenonstringbuilderconstructor retry getretry final string mainquery connection not stringbuilder --> |
| JAVA-084 | Semgrep: >- | `        Cookie cookie = new Cookie("author", name);`<br>`        response.addCookie(cookie);`<br>`    }`<br>`    private Response safe(String name, Response response) {` | `        Cookie cookie = new Cookie("author", name);`<br>`        response.addCookie(cookie);`<br>`        return response;`<br>`    }`<br>`    @RequestMapping(value = "/{name}/{book}", method = RequestMethod.POST)`<br>`    @PreAuthorize(Permissions.USER)`<br>`    @ResponseBody`<br>`    public void loadBook(@PathVariable final String name, @PathVariable final String book, HttpServletResponse response) throws APIException {`<br>`        AuthorObj author = AuthorObj.getAuthor(name, book);` | Java/Spring | `Semgrep http-response-splitting` | `        Cookie cookie = new Cookie("author", name);         response.addCookie(cookie);         return response;     }     @RequestMapping(value = "/{name}/{book}", method = RequestMethod.POST)     @PreAuthorize(Permissions.USER)     @ResponseBody     public void loadBook(@PathVariable final String name, @PathVariable final String book, HttpServletResponse response) throws APIException {         A...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 084 semgrep cookie new author name response addcookie private safe string --> |
| JAVA-085 | Semgrep: >- | `        String updateQuery = "update Users set salary = '"+paramSalary+"' where name = '"+paramName+"'";`<br>`        jdbc.update(updateQuery);`<br>`    }`<br>`    public void safe(String paramName) {`<br>`        JdbcTemplate jdbc = new JdbcTemplate();` | `        int count = jdbc.queryForObject("select count(*) from Users where name = ?", Integer.class, paramName);`<br>`    }`<br>`}` | Java/Spring | `Semgrep jdbc-sql-formatted-string` | `        int count = jdbc.queryForObject("select count(*) from Users where name = ?", Integer.class, paramName);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 085 semgrep string updatequery update users set salary paramsalary where name paramname jdbc public void safe jdbctemplate new --> |
| JAVA-086 | Semgrep: >- | `    public void unsafeOgnlReflectionProvider3(String input, OgnlTextParser reflectionProvider) throws IntrospectionException, ReflectionException {`<br>`        reflectionProvider.evaluate( input );`<br>`    }` | `    public void safeOgnlReflectionProvider1(OgnlReflectionProvider reflectionProvider, Class type) throws IntrospectionException, ReflectionException {`<br>`        String input = "thisissafe";`<br>`        reflectionProvider.getGetMethod(type, input);`<br>`    }` | Java/Spring | `Semgrep ognl-injection` | `    public void safeOgnlReflectionProvider1(OgnlReflectionProvider reflectionProvider, Class type) throws IntrospectionException, ReflectionException {         String input = "thisissafe";         reflectionProvider.getGetMethod(type, input);     }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 086 semgrep public void unsafeognlreflectionprovider3 string input ognltextparser reflectionprovider throws introspectionexception reflectionexception evaluate --> |
| JAVA-087 | Semgrep: >- | `    @Override`<br>`    public Mono<Void> filter(ServerWebExchange exchange, WebFilterChain chain) {`<br>`        exchange.getResponse()`<br>`            .getHeaders()`<br>`            .add("Access-Control-Allow-Origin", "*.some.domain");`<br>`        return chain.filter(exchange);`<br>`    }` | `    public void setErrorsResponse1(Errors errors, HttpStatus responseHttpStatus, HttpServletRequest request, HttpServletResponse response) throws IOException {`<br>`        response.addHeader("Foo", "Bar");`<br>`        response.getWriter().write(responseData.getBody());`<br>`    }` | Java/Spring | `Semgrep permissive-cors` | `    public void setErrorsResponse1(Errors errors, HttpStatus responseHttpStatus, HttpServletRequest request, HttpServletResponse response) throws IOException {         response.addHeader("Foo", "Bar");         response.getWriter().write(responseData.getBody());     }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 087 semgrep override public mono void filter serverwebexchange exchange webfilterchain chain getresponse getheaders add access control allow origin some domain return --> |
| JAVA-088 | Semgrep: >- | `    public static void scripting1(String userInput) throws ScriptException {`<br>`        ScriptEngineManager scriptEngineManager = new ScriptEngineManager();`<br>`        ScriptEngine scriptEngine = scriptEngineManager.getEngineByExtension("js");`<br>`        Object result = scriptEngine.eval("test=1;" + userInput);`<br>`    }` | `    public static void scriptingSafe() throws ScriptException {`<br>`        ScriptEngineManager scriptEngineManager = new ScriptEngineManager();`<br>`        ScriptEngine scriptEngine = scriptEngineManager.getEngineByExtension("js");`<br>`        String code = "var test=3;test=test*2;";`<br>`        Object result = scriptEngine.eval(code);`<br>`    }`<br>`}` | Java/Spring | `Semgrep script-engine-injection` | `    public static void scriptingSafe() throws ScriptException {         ScriptEngineManager scriptEngineManager = new ScriptEngineManager();         ScriptEngine scriptEngine = scriptEngineManager.getEngineByExtension("js");         String code = "var test=3;test=test*2;";         Object result = scriptEngine.eval(code);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 088 semgrep public static void scripting1 string userinput throws scriptexception scriptenginemanager new scriptengine getenginebyextension js object result eval test --> |
| JAVA-089 | Semgrep: >- | `        criteria.add(Restrictions.sqlRestriction("param1  = ? and param2 = " + input,new String[] {input}, new Type[] {StandardBasicTypes.STRING}));` | `        criteria.add(Restrictions.sqlRestriction("test=1234"));`<br>`        final String localSafe = "where id=1337";` | Java/Spring | `Semgrep hibernate-sqli` | `        criteria.add(Restrictions.sqlRestriction("test=1234"));         final String localSafe = "where id=1337";` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 089 semgrep criteria add restrictions sqlrestriction param1 and param2 input new string type standardbasictypes --> |
| JAVA-090 | Semgrep: >- | `        stmt.addBatch(sql);`<br>`        String sqlString = "select * from Users where name = '123'";` | `        stmt.addBatch(sqlString);`<br>`    }`<br>`}` | Java/Spring | `Semgrep jdbc-sqli` | `        stmt.addBatch(sqlString);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 090 semgrep stmt addbatch sql string sqlstring select from users where name 123 --> |
| JAVA-091 | Semgrep: >- | `        pm.newQuery((Extent) null,"id == "+input);` | `        pm.newQuery((Extent) null,"id == 1");`<br>`    }`<br>`}` | Java/Spring | `Semgrep jdo-sqli` | `        pm.newQuery((Extent) null,"id == 1");     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 091 semgrep pm newquery extent null id input --> |
| JAVA-092 | Semgrep: >- | `        em.createNativeQuery(sql, UserEntity.class);`<br>`    }`<br>`    public void getUserWithNativeQuerySafe(EntityManager em) {`<br>`        String sql = "select * from Users where user = 'admin'";` | `        em.createNativeQuery(sql);` | Java/Spring | `Semgrep jpa-sqli` | `        em.createNativeQuery(sql);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 092 semgrep em createnativequery sql userentity class public void getuserwithnativequerysafe entitymanager string select from users where user admin --> |
| JAVA-093 | Semgrep: >- | `            org.springframework.jdbc.support.rowset.SqlRowSet results =`<br>`                    org.owasp.benchmark.helpers.DatabaseHelper.JDBCtemplate.queryForRowSet(sql);`<br>`            response.getWriter().println("Your results are: ");`<br>`            //		System.out.println("Your results are");`<br>`            while (results.next()) {`<br>`                response.getWriter()`<br>`                        .println(`<br>`                                org.owasp`<br>`                                                .esapi`<br>`                                                .ESAPI`<br>`...`<br>`            java.sql.Connection connection =`<br>`                    org.owasp.benchmark.helpers.DatabaseHelper.getSqlConnection();` | `            java.sql.CallableStatement statement = connection.prepareCall(sql);`<br>`            java.sql.ResultSet rs = statement.executeQuery();`<br>`            org.owasp.benchmark.helpers.DatabaseHelper.printResults(rs, sql, response);`<br>`        } catch (java.sql.SQLException e) {`<br>`            if (org.owasp.benchmark.helpers.DatabaseHelper.hideSQLErrors) {`<br>`                response.getWriter().println("Error processing request.");`<br>`                return;`<br>`            } else throw new ServletException(e);`<br>`        }`<br>`    }`<br>`}` | Java/Spring | `Semgrep tainted-sql-from-http-request` | `            java.sql.CallableStatement statement = connection.prepareCall(sql);             java.sql.ResultSet rs = statement.executeQuery();             org.owasp.benchmark.helpers.DatabaseHelper.printResults(rs, sql, response);         } catch (java.sql.SQLException e) {             if (org.owasp.benchmark.helpers.DatabaseHelper.hideSQLErrors) {                 response.getWriter().println("Erro...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 093 semgrep org springframework jdbc support rowset sqlrowset results owasp benchmark helpers databasehelper jdbctemplate queryforrowset sql response getwriter println your are system out --> |
| JAVA-094 | Semgrep: >- | `        GroupPeer.executeQuery(injection,"",false);`<br>`    }`<br>`    public void falsePositive(BasePeer peer0) {`<br>`        String constantValue = "SELECT * FROM test";` | `        peer0.executeQuery(constantValue);` | Java/Spring | `Semgrep turbine-sqli` | `        peer0.executeQuery(constantValue);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 094 semgrep grouppeer executequery injection false public void falsepositive basepeer peer0 string constantvalue select from test --> |
| JAVA-095 | Semgrep: >- | `        conn.prepare(injection, null);`<br>`    }`<br>`    public void falsePositive1(SqlClient client) {`<br>`        String constantValue = "SELECT * FROM test";` | `        client.query(constantValue);`<br>`    }`<br>`    public void falsePositive2(SqlConnection conn) {`<br>`        String constantValue = "SELECT * FROM test";` | Java/Spring | `Semgrep vertx-sqli` | `        client.query(constantValue);     }     public void falsePositive2(SqlConnection conn) {         String constantValue = "SELECT * FROM test";` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 095 semgrep conn prepare injection null public void falsepositive1 sqlclient client string constantvalue select from test --> |
| JAVA-096 | Semgrep: >- | `        argList.add("echo " + bar);`<br>`        // deepruleid: tainted-cmd-from-http-request`<br>`        ProcessBuilder pb = new ProcessBuilder(argList);`<br>`        try {`<br>`            // deepruleid: tainted-cmd-from-http-request`<br>`            Process p = pb.start();`<br>`            org.owasp.benchmark.helpers.Utils.printOSCommandResults(p, response);`<br>`        } catch (IOException e) {`<br>`            System.out.println(`<br>`                    "Problem executing cmdi - java.lang.ProcessBuilder(java.util.List) Test Case");`<br>`...`<br>`            argList.add("-c");`<br>`        }` | `        argList.add("echo " + "param");`<br>`        ProcessBuilder pb = new ProcessBuilder();`<br>`        pb.command(argList);`<br>`        try {`<br>`            Process p = pb.start();`<br>`            org.owasp.benchmark.helpers.Utils.printOSCommandResults(p, response);`<br>`        } catch (IOException e) {`<br>`            System.out.println(`<br>`                    "Problem executing cmdi - java.lang.ProcessBuilder(java.util.List) Test Case");`<br>`            throw new ServletException(e);`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep tainted-cmd-from-http-request` | `        argList.add("echo " + "param");         ProcessBuilder pb = new ProcessBuilder();         pb.command(argList);         try {             Process p = pb.start();             org.owasp.benchmark.helpers.Utils.printOSCommandResults(p, response);         } catch (IOException e) {             System.out.println(                     "Problem executing cmdi - java.lang.ProcessBuilder(java.util.Li...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 096 semgrep arglist add echo bar deepruleid tainted cmd from http request processbuilder pb new try process start --> |
| JAVA-097 | Semgrep: >- | `            Process p = r.exec(args, argsEnv);`<br>`            org.owasp.benchmark.helpers.Utils.printOSCommandResults(p, response);`<br>`        } catch (IOException e) {`<br>`            System.out.println("Problem executing cmdi - TestCase");`<br>`            response.getWriter()`<br>`                    .println(org.owasp.esapi.ESAPI.encoder().encodeForHTML(e.getMessage()));`<br>`            return;`<br>`        }`<br>`    }`<br>`}`<br>`...`<br>`        Runtime r = Runtime.getRuntime();`<br>`        try {` | `            Process p = r.exec(args, argsEnv);`<br>`            org.owasp.benchmark.helpers.Utils.printOSCommandResults(p, response);` | Java/Spring | `Semgrep tainted-env-from-http-request` | `            Process p = r.exec(args, argsEnv);             org.owasp.benchmark.helpers.Utils.printOSCommandResults(p, response);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 097 semgrep process exec args argsenv org owasp benchmark helpers utils printoscommandresults response catch ioexception system out println problem executing cmdi testcase getwriter --> |
| JAVA-098 | Semgrep: >- | `                    idc.search(base, filter, sc);`<br>`            while (results.hasMore()) {`<br>`                javax.naming.directory.SearchResult sr =`<br>`                        (javax.naming.directory.SearchResult) results.next();`<br>`                javax.naming.directory.Attributes attrs = sr.getAttributes();`<br>`                javax.naming.directory.Attribute attr = attrs.get("uid");`<br>`                javax.naming.directory.Attribute attr2 = attrs.get("street");`<br>`                if (attr != null) {`<br>`                    response.getWriter()`<br>`                            .println(`<br>`...`<br>`            boolean found = false;`<br>`            javax.naming.NamingEnumeration<javax.naming.directory.SearchResult> results =` | `                    ctx.search(base, filter, filters, sc);`<br>`            while (results.hasMore()) {`<br>`                javax.naming.directory.SearchResult sr =`<br>`                        (javax.naming.directory.SearchResult) results.next();`<br>`                javax.naming.directory.Attributes attrs = sr.getAttributes();`<br>`                javax.naming.directory.Attribute attr = attrs.get("uid");`<br>`                javax.naming.directory.Attribute attr2 = attrs.get("street");`<br>`                if (attr != null) {`<br>`                    response.getWriter()`<br>`                            .println(`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep tainted-ldapi-from-http-request` | `                    ctx.search(base, filter, filters, sc);             while (results.hasMore()) {                 javax.naming.directory.SearchResult sr =                         (javax.naming.directory.SearchResult) results.next();                 javax.naming.directory.Attributes attrs = sr.getAttributes();                 javax.naming.directory.Attribute attr = attrs.get("uid");               ...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 098 semgrep idc search base filter sc while results hasmore javax naming directory searchresult sr next attributes attrs getattributes attribute attr get uid --> |
| JAVA-099 | Semgrep: >- | `        request.getSession().putValue(bar, bar);`<br>`        response.getWriter()`<br>`                .println(`<br>`                        "Item: '"`<br>`                                + org.owasp.benchmark.helpers.Utils.encodeForHTML(bar)`<br>`                                + "' with value: 10340 saved in session.");`<br>`    }`<br>`}`<br>`@WebServlet(value = "/trustbound-00/BenchmarkTest00004ok")`<br>`public class BenchmarkTest00004ok extends HttpServlet {`<br>`...`<br>`        }`<br>`        // javax.servlet.http.HttpSession.setAttribute(java.lang.String^,java.lang.Object)` | `        request.getSession().setAttribute(param, "10340");`<br>`        response.getWriter()`<br>`                .println(`<br>`                        "Item: '"`<br>`                                + org.owasp.benchmark.helpers.Utils.encodeForHTML(param)`<br>`                                + "' with value: '10340' saved in session.");`<br>`    }`<br>`}` | Java/Spring | `Semgrep tainted-session-from-http-request` | `        request.getSession().setAttribute(param, "10340");         response.getWriter()                 .println(                         "Item: '"                                 + org.owasp.benchmark.helpers.Utils.encodeForHTML(param)                                 + "' with value: '10340' saved in session.");     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 099 semgrep request getsession putvalue bar response getwriter println item org owasp benchmark helpers utils encodeforhtml with value 10340 saved in session --> |
| JAVA-100 | Semgrep: >- | `            org.w3c.dom.NodeList nodeList = (org.w3c.dom.NodeList) xp.compile(expression).evaluate(xmlDocument, javax.xml.xpath.XPathConstants.NODESET);`<br>`            response.getWriter().println("Your query results are: <br/>");`<br>`            for (int i = 0; i < nodeList.getLength(); i++) {`<br>`                org.w3c.dom.Element value = (org.w3c.dom.Element) nodeList.item(i);`<br>`                response.getWriter().println(value.getTextContent() + "<br/>");`<br>`            }`<br>`        } catch (javax.xml.xpath.XPathExpressionException`<br>`                \| javax.xml.parsers.ParserConfigurationException`<br>`                \| org.xml.sax.SAXException e) {`<br>`            response.getWriter()`<br>`...`<br>`            javax.xml.xpath.XPath xp = xpf.newXPath();`<br>`            String expression = "/Employees/Employee[@emplid='1234']";` | `            String result = xp.evaluate(expression, xmlDocument);`<br>`            response.getWriter().println("Your query results are: " + result + "<br/>");`<br>`        } catch (javax.xml.xpath.XPathExpressionException`<br>`                \| javax.xml.parsers.ParserConfigurationException`<br>`                \| org.xml.sax.SAXException e) {`<br>`            response.getWriter()`<br>`                    .println(`<br>`                            "Error parsing XPath input: '"`<br>`                                    + org.owasp.esapi.ESAPI.encoder().encodeForHTML(bar)`<br>`                                    + "'");`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep tainted-xpath-from-http-request` | `            String result = xp.evaluate(expression, xmlDocument);             response.getWriter().println("Your query results are: " + result + "<br/>");         } catch (javax.xml.xpath.XPathExpressionException                 \\| javax.xml.parsers.ParserConfigurationException                 \\| org.xml.sax.SAXException e) {             response.getWriter()                     .println(          ...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 100 semgrep org w3c dom nodelist xp compile expression evaluate xmldocument javax xml xpath xpathconstants nodeset response getwriter println your query results are --> |
| JAVA-101 | Semgrep: >- | `        Class<?> loadClass = Class.forName(userInput + "MyThread");`<br>`        Thread thread = (Thread) loadClass.newInstance();`<br>`        thread.start();`<br>`        thread.join();`<br>`    }`<br>`    private static void demoOk() throws ClassNotFoundException,`<br>`            IllegalAccessException, InstantiationException, InterruptedException {` | `        Class<?> loadClass = Class.forName("org.learn.MyThread");`<br>`        Thread thread = (Thread) loadClass.newInstance();`<br>`        thread.start();`<br>`        thread.join();`<br>`    }`<br>`}` | Java/Spring | `Semgrep unsafe-reflection` | `        Class<?> loadClass = Class.forName("org.learn.MyThread");         Thread thread = (Thread) loadClass.newInstance();         thread.start();         thread.join();     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 101 semgrep class loadclass forname userinput mythread thread newinstance start join private static void demook throws classnotfoundexception --> |
| JAVA-102 | Semgrep: >- | `    public void unvalidatedRedirect4(HttpServletRequest req, HttpServletResponse resp) {`<br>`        String url = req.getParameter("urlRedirect");`<br>`        resp.addHeader("Location", url);`<br>`    }` | `    public void falsePositiveRedirect1(HttpServletResponse resp) throws IOException {`<br>`        String url = "/Home";`<br>`        if (url != null) {`<br>`            resp.sendRedirect(url);`<br>`        }`<br>`    }` | Java/Spring | `Semgrep unvalidated-redirect` | `    public void falsePositiveRedirect1(HttpServletResponse resp) throws IOException {         String url = "/Home";         if (url != null) {             resp.sendRedirect(url);         }     }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 102 semgrep public void unvalidatedredirect4 httpservletrequest req httpservletresponse resp string url getparameter urlredirect addheader location --> |
| JAVA-103 | Semgrep: >- | `    public String encodeRedirectUrlRewrite(HttpServletResponse resp, String url) {`<br>`        return resp.encodeRedirectUrl(url); //Deprecated`<br>`    }` | `    public String encodeRedirectURLRewrite(SomeDifferentRequest resp, String url) {`<br>`        return resp.encodeURL(url);`<br>`    }` | Java/Spring | `Semgrep url-rewriting` | `    public String encodeRedirectURLRewrite(SomeDifferentRequest resp, String url) {         return resp.encodeURL(url);     }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 103 semgrep public string encoderedirecturlrewrite httpservletresponse resp url return encoderedirecturl deprecated --> |
| JAVA-104 | Semgrep: >- | `        SSLContext ctx = SSLContext.getInstance("TLSv1.1");`<br>`    }`<br>`    public void test6() {` | `        SSLContext ctx = SSLContext.getInstance("TLSv1.2");`<br>`    }`<br>`    public void test7() {` | Java/Spring | `Semgrep weak-ssl-context` | `        SSLContext ctx = SSLContext.getInstance("TLSv1.2");     }     public void test7() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 104 semgrep sslcontext ctx getinstance tlsv1 public void test6 --> |
| JAVA-105 | Semgrep: >- | `    public static Object handleXml(InputStream in) {`<br>`        XMLDecoder d = new XMLDecoder(in);`<br>`        try {`<br>`            Object result = d.readObject(); //Deserialization happen here`<br>`            return result;`<br>`        }`<br>`        finally {`<br>`            d.close();`<br>`        }`<br>`    }` | `    public static Object handleXml1() {`<br>`        XMLDecoder d = new XMLDecoder("<safe>XML</safe>");`<br>`        try {`<br>`            Object result = d.readObject();`<br>`            return result;`<br>`        }`<br>`        finally {`<br>`            d.close();`<br>`        }`<br>`    }` | Java/Spring | `Semgrep xml-decoder` | `    public static Object handleXml1() {         XMLDecoder d = new XMLDecoder("<safe>XML</safe>");         try {             Object result = d.readObject();             return result;         }         finally {             d.close();         }     }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 105 semgrep public static object handlexml inputstream in xmldecoder new try result readobject deserialization happen here return --> |
| JAVA-106 | Semgrep: DOCTYPE declarations are enabled for this DocumentBuilderFactory. This | `        dbf.newDocumentBuilder();`<br>`    }`<br>`    private DocumentBuilderFactory newFactory(){`<br>`        return DocumentBuilderFactory.newInstance();`<br>`    }`<br>`}`<br>`class GoodDocumentBuilderFactoryCtr {`<br>`    private final DocumentBuilderFactory dbf;`<br>`    public GoodDocumentBuilderFactoryCtr() throws Exception {`<br>`        dbf = DocumentBuilderFactory.newInstance();`<br>`        dbf.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);` | `        dbf.newDocumentBuilder();`<br>`    }`<br>`}`<br>`class GoodDocumentBuilderFactoryCtr2 {`<br>`    public void somemethod() throws Exception {`<br>`        DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance();`<br>`        setFeatures(dbf);` | Java/Spring | `Semgrep documentbuilderfactory-disallow-doctype-decl-missing` | `        dbf.newDocumentBuilder();     } } class GoodDocumentBuilderFactoryCtr2 {     public void somemethod() throws Exception {         DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance();         setFeatures(dbf);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 106 semgrep doctype declarations are enabled for this documentbuilderfactory dbf newdocumentbuilder private newfactory return newinstance --> |
| JAVA-107 | Semgrep: DOCTYPE declarations are enabled for this SAXParserFactory. This | `        spf.newSAXParser();`<br>`    }`<br>`    private SAXParserFactory newFactory(){`<br>`        return SAXParserFactory.newInstance();`<br>`    }`<br>`}`<br>`class GoodSAXParserFactoryCtr {`<br>`    private final SAXParserFactory spf;`<br>`    public GoodSAXParserFactoryCtr() throws Exception {`<br>`        spf = SAXParserFactory.newInstance();`<br>`        spf.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);` | `        spf.newSAXParser();`<br>`    }`<br>`}`<br>`class GoodSAXParserFactoryCtr2 {`<br>`    public void somemethod() throws Exception {`<br>`        SAXParserFactory spf = SAXParserFactory.newInstance();`<br>`        setFeatures(spf);` | Java/Spring | `Semgrep saxparserfactory-disallow-doctype-decl-missing` | `        spf.newSAXParser();     } } class GoodSAXParserFactoryCtr2 {     public void somemethod() throws Exception {         SAXParserFactory spf = SAXParserFactory.newInstance();         setFeatures(spf);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 107 semgrep doctype declarations are enabled for this saxparserfactory spf newsaxparser private newfactory return newinstance --> |
| JAVA-108 | Semgrep: >- | `        Pattern.compile(regex,Pattern.CASE_INSENSITIVE);` | `        data.matches("[a-z]+");`<br>`    }`<br>`}` | Java/Spring | `Semgrep java-pattern-from-string-parameter` | `        data.matches("[a-z]+");     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 108 semgrep pattern compile regex case insensitive --> |
| JAVA-109 | Semgrep: >- | `        PrintWriter writer = resp.getWriter();`<br>`        writer.write(input1);`<br>`    }`<br>`    protected void ok(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {`<br>`        String input1 = req.getParameter("input1");` | `        resp.getWriter().write(Encode.forHtml(input1));`<br>`    }`<br>`}` | Java/Spring | `Semgrep servletresponse-writer-xss` | `        resp.getWriter().write(Encode.forHtml(input1));     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 109 semgrep printwriter writer resp getwriter write input1 protected void ok httpservletrequest req httpservletresponse throws servletexception ioexception string getparameter --> |
| JAVA-110 | Semgrep: >- | `        Yaml yaml = new Yaml();`<br>`        yaml.load(toLoad);`<br>`    }`<br>`    public void safeConstructorLoad(String toLoad) {` | `        Yaml yaml = new Yaml(new SafeConstructor());`<br>`        yaml.load(toLoad);`<br>`    }`<br>`    public void customConstructorLoad(String toLoad, Class goodClass) {` | Java/Spring | `Semgrep use-snakeyaml-constructor` | `        Yaml yaml = new Yaml(new SafeConstructor());         yaml.load(toLoad);     }     public void customConstructorLoad(String toLoad, Class goodClass) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 110 semgrep yaml new load toload public void safeconstructorload string --> |
| JAVA-111 | Semgrep: >- | `    paramMap.put("$where", "this.sharedWith == \"" + userName + "\" && this.email == \"" + email + "\"");`<br>`    BasicDBObject query = (BasicDBObject) BasicDBObjectBuilder`<br>`        .start(paramMap)`<br>`        .get();`<br>`    MongoCursor<Document> cursor = collection.find(query).iterator();`<br>`    ArrayList<Document> results = new ArrayList<>();`<br>`    while (cursor.hasNext()) {`<br>`      Document doc = cursor.next();`<br>`      results.add(doc);`<br>`    }`<br>`...`<br>`  public ArrayList<Document> basicDBObjectBuilderStartMap(String userName, String email) {`<br>`    HashMap<String, String> paramMap = new HashMap<>();` | `    paramMap.put("$where", "this.sharedWith == \"CONSTANT\" && this.email == \"CONSTANT\"");`<br>`    BasicDBObject query = (BasicDBObject) BasicDBObjectBuilder`<br>`        .start(paramMap)`<br>`        .get();`<br>`    MongoCursor<Document> cursor = collection.find(query).iterator();`<br>`    ArrayList<Document> results = new ArrayList<>();`<br>`    while (cursor.hasNext()) {`<br>`      Document doc = cursor.next();`<br>`      results.add(doc);`<br>`    }`<br>`...`<br>`  public ArrayList<Document> basicDBObjectBuilderStartMap(String userName, String email) {`<br>`    HashMap<String, String> paramMap = new HashMap<>();` | Java/Spring | `Semgrep mongodb-nosqli` | `    paramMap.put("$where", "this.sharedWith == \"CONSTANT\" && this.email == \"CONSTANT\"");     BasicDBObject query = (BasicDBObject) BasicDBObjectBuilder         .start(paramMap)         .get();     MongoCursor<Document> cursor = collection.find(query).iterator();     ArrayList<Document> results = new ArrayList<>();     while (cursor.hasNext()) {       Document doc = cursor.next();       results...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 111 semgrep parammap put where this sharedwith username email basicdbobject query basicdbobjectbuilder start get mongocursor document cursor collection find iterator arraylist results new --> |
| JAVA-112 | Semgrep: >- | `public interface IBSidesService extends Remote {`<br>`   boolean registerTicket(String ticketID) throws RemoteException;`<br>`   void vistTalk(String talkname) throws RemoteException;`<br>`   void poke(Attendee attende) throws RemoteException;`<br>`}` | `public interface IBSidesServiceOK extends Remote {`<br>`   boolean registerTicket(long ticketID) throws RemoteException;`<br>`   void vistTalk(long talkID) throws RemoteException;`<br>`   void poke(int attende) throws RemoteException;`<br>`}`<br>`public class Attendee {`<br>`    public int id;`<br>`    public String handle;`<br>`}`<br>`public class BSidesServer {`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep server-dangerous-class-deserialization` | `public interface IBSidesServiceOK extends Remote {    boolean registerTicket(long ticketID) throws RemoteException;    void vistTalk(long talkID) throws RemoteException;    void poke(int attende) throws RemoteException; } public class Attendee {     public int id;     public String handle; } public class BSidesServer { ...     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 112 semgrep public interface ibsidesservice extends remote boolean registerticket string ticketid throws remoteexception void visttalk talkname poke attendee attende --> |
| JAVA-113 | Semgrep: Default session middleware settings: `setSecure` not set to true. This | `              Cookie cookie = new Cookie("name", "value");`<br>`          }`<br>`   }`<br>` public class Ok {`<br>`          public void ok1() {` | `             Cookie cookie = new Cookie("name", "value");`<br>`             cookie.setSecure(true);`<br>`          }`<br>`}` | Java/Spring | `Semgrep cookie-issecure-false` | `             Cookie cookie = new Cookie("name", "value");              cookie.setSecure(true);           } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 113 semgrep default session middleware settings setsecure not set to true this cookie new name value public class ok void ok1 --> |
| JAVA-114 | Semgrep: Default session middleware settings: `setSecure` not set to true. This | `              cookie.setSecure(false);`<br>`          }`<br>`   }`<br>` public class Ok {`<br>`          public void ok1() {` | `             Cookie cookie = new Cookie("name", "value");`<br>`             cookie.setSecure(true);`<br>`          }`<br>`}` | Java/Spring | `Semgrep cookie-setSecure` | `             Cookie cookie = new Cookie("name", "value");              cookie.setSecure(true);           } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 114 semgrep default session middleware settings setsecure not set to true this cookie false public class ok void ok1 --> |
| JAVA-115 | Semgrep: >- | `        http`<br>`            .csrf().disable()`<br>`            .authorizeRequests()`<br>`                .antMatchers("/", "/home").permitAll()`<br>`                .anyRequest().authenticated()`<br>`                .and()`<br>`            .formLogin()`<br>`                .loginPage("/login")`<br>`                .permitAll()`<br>`                .and()`<br>`...`<br>`    @Override`<br>`    protected void configure(HttpSecurity http) throws Exception {` | `        http`<br>`            .authorizeRequests()`<br>`                .antMatchers("/", "/home").permitAll()`<br>`                .anyRequest().authenticated()`<br>`                .and()`<br>`            .formLogin()`<br>`                .loginPage("/login")`<br>`                .permitAll()`<br>`                .and()`<br>`            .logout()`<br>`...`<br>`    }`<br>`}` | Java/Spring | `Semgrep spring-csrf-disabled` | `        http             .authorizeRequests()                 .antMatchers("/", "/home").permitAll()                 .anyRequest().authenticated()                 .and()             .formLogin()                 .loginPage("/login")                 .permitAll()                 .and()             .logout() ...     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 115 semgrep http csrf disable authorizerequests antmatchers home permitall anyrequest authenticated and --> |
| JAVA-116 | Semgrep: >- | `        NamedParameterBatchUpdateUtils.executeBatchUpdate(sql, new ArrayList<Object[]>(),new int[] {Types.INTEGER}, jdbcOperations);`<br>`    }`<br>`    public void queryNamedParameterBatchUpdateUtilsSafe() {`<br>`        String sql = "UPDATE Users SET name = 'safe' where id = 1";` | `        NamedParameterBatchUpdateUtils.executeBatchUpdate(sql, new ArrayList<Object[]>(), new int[]{Types.INTEGER}, jdbcOperations);`<br>`    }`<br>`}` | Java/Spring | `Semgrep spring-sqli` | `        NamedParameterBatchUpdateUtils.executeBatchUpdate(sql, new ArrayList<Object[]>(), new int[]{Types.INTEGER}, jdbcOperations);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 116 semgrep namedparameterbatchupdateutils executebatchupdate sql new arraylist object int types integer jdbcoperations public void querynamedparameterbatchupdateutilssafe string update users set name safe where id --> |
| JAVA-117 | Semgrep: >- | `    @RequestMapping("/redirect5")`<br>`    public ModelAndView redirect5(@RequestParam("url") String url) {`<br>`        String view = "redirect:" + url;`<br>`        return new ModelAndView(view);`<br>`    }` | `    @RequestMapping("/redirectfp")`<br>`    public String redirectfp() {`<br>`        return "redirect:/";`<br>`    }`<br>`}` | Java/Spring | `Semgrep spring-unvalidated-redirect` | `    @RequestMapping("/redirectfp")     public String redirectfp() {         return "redirect:/";     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 117 semgrep requestmapping redirect5 public modelandview requestparam url string view redirect return new --> |
| JAVA-118 | Semgrep: >- | `            return new ResponseEntity<Success>(vulnerablePayloadWithPlaceHolder, HttpStatus.OK);`<br>`        } else {`<br>`            return new ResponseEntity<>(HttpStatus.BAD_REQUEST);`<br>`        }`<br>`    }`<br>`    // Escape all special characters to their corresponding HTML hex format`<br>`    // and validate input.`<br>`    // Would be even better if Content Security Policy (CSP) is set.`<br>`    @AttackVector(`<br>`            vulnerabilityExposed = VulnerabilityType.REFLECTED_XSS,`<br>`...`<br>`                \|\| allowedValues.contains(imageLocation)) {`<br>`            vulnerablePayloadWithPlaceHolder += imageLocation;` | `            return new ResponseEntity<Success>(vulnerablePayloadWithPlaceHolder, HttpStatus.OK);`<br>`        } else {`<br>`            return new ResponseEntity<>(HttpStatus.BAD_REQUEST);`<br>`        }`<br>`    }`<br>`    // Escape all special characters to their corresponding HTML hex format`<br>`    // and validate input.`<br>`    // Would be even better if Content Security Policy (CSP) is set.`<br>`    @AttackVector(`<br>`            vulnerabilityExposed = VulnerabilityType.REFLECTED_XSS,`<br>`...`<br>`                            HtmlUtils.htmlEscapeHex(imageLocation));`<br>`            String cleaned = Encode.forHtml(payload);` | Java/Spring | `Semgrep tainted-html-string` | `            return new ResponseEntity<Success>(vulnerablePayloadWithPlaceHolder, HttpStatus.OK);         } else {             return new ResponseEntity<>(HttpStatus.BAD_REQUEST);         }     }     // Escape all special characters to their corresponding HTML hex format     // and validate input.     // Would be even better if Content Security Policy (CSP) is set.     @AttackVector(             vu...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 118 semgrep return new responseentity success vulnerablepayloadwithplaceholder httpstatus ok else bad request escape all special characters to their corresponding html hex format --> |
| JAVA-119 | Semgrep: >- | `    @RequestMapping(value = "/path")`<br>`    public void writeData2() {`<br>`        // State-changing operations performed within this method.`<br>`    }`<br>`    /**`<br>`     * For methods without side-effects use either`<br>`     * RequestMethod.GET, RequestMethod.HEAD, RequestMethod.TRACE, or RequestMethod.OPTIONS.`<br>`     */` | `    @RequestMapping(value = "/path", method = RequestMethod.GET)`<br>`    public String readData() {`<br>`        // No state-changing operations performed within this method.`<br>`        return "";`<br>`    }`<br>`    /**`<br>`     * For state-changing methods use either`<br>`     * RequestMethod.POST, RequestMethod.PUT, RequestMethod.DELETE, or RequestMethod.PATCH.`<br>`     */` | Java/Spring | `Semgrep unrestricted-request-mapping` | `    @RequestMapping(value = "/path", method = RequestMethod.GET)     public String readData() {         // No state-changing operations performed within this method.         return "";     }     /**      * For state-changing methods use either      * RequestMethod.POST, RequestMethod.PUT, RequestMethod.DELETE, or RequestMethod.PATCH.      */` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 119 semgrep requestmapping value path public void writedata2 state changing operations performed within this method for methods without side effects use either --> |
| JAVA-120 | Semgrep: >- | `    var ajv = new Ajv({  smth: 'else', smth: 'else', allErrors: true, smth: 'else' });`<br>`    ajv.addSchema(schema, 'input');`<br>`}`<br>`function okTest1() {` | `    let ajv = new Ajv({ allErrors: process.env.DEBUG });`<br>`    ajv.addSchema(schema, 'input');`<br>`}`<br>`function okTest2() {` | Java/Spring | `Semgrep ajv-allerrors-true` | `    let ajv = new Ajv({ allErrors: process.env.DEBUG });     ajv.addSchema(schema, 'input'); } function okTest2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 120 semgrep var ajv new smth else allerrors true addschema schema input function oktest1 --> |
| JAVA-121 | Semgrep: >- | `     $window.location.href = input + '/app/logout';`<br>`     //Data is not coming from user input`<br>`     $location.location.location = test` | `     $window.location.href = "//untatintedredirect"`<br>`   };`<br>`});` | Java/Spring | `Semgrep detect-angular-open-redirect` | `     $window.location.href = "//untatintedredirect"    }; });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 121 semgrep window location href input app logout data is not coming from user test --> |
| JAVA-122 | Semgrep: >- | `    $sceDelegateProvider.resourceUrlWhitelist(['http://**.semgrep.dev']);` | `    $sceDelegateProvider.resourceUrlWhitelist(['http://semgrep.dev/ooo']);` | Java/Spring | `Semgrep detect-angular-resource-loading` | `    $sceDelegateProvider.resourceUrlWhitelist(['http://semgrep.dev/ooo']);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 122 semgrep scedelegateprovider resourceurlwhitelist http dev --> |
| JAVA-123 | Semgrep: >- | `    .hash(user.Password, hashSettings)`<br>`    .then((hash) => ({ ...user, Password: hash }))`<br>`    .catch((err) => console.error('Error during hashing: ${err}'));`<br>`};`<br>`function okTest(user) {`<br>`  if (!user.Password) return Promise.resolve(user);` | `  return argon2`<br>`    .hash(user.Password, {`<br>`      type: argon2.argon2id,`<br>`      memoryCost: 2 ** 16,`<br>`      parallelism: os.cpus().length \|\| 8,`<br>`    })`<br>`    .then((hash) => ({ ...user, Password: hash }))`<br>`    .catch((err) => console.error('Error during hashing: ${err}'));`<br>`}` | Java/Spring | `Semgrep unsafe-argon2-config` | `  return argon2     .hash(user.Password, {       type: argon2.argon2id,       memoryCost: 2 ** 16,       parallelism: os.cpus().length \\|\\| 8,     })     .then((hash) => ({ ...user, Password: hash }))     .catch((err) => console.error(`Error during hashing: ${err}`)); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 123 semgrep hash user password hashsettings then catch err console error during hashing function oktest if return promise resolve --> |
| JAVA-124 | Semgrep: >- | `    tableData[i].description = tableData[i].description.replaceAll('<', '&lt;').replaceAll('>', '&gt;')` | `    tableData[i].description = tableData[i].description.replaceAll('<', 'left angle bracket')`<br>`  }`<br>`}` | Java/Spring | `Semgrep detect-replaceall-sanitization` | `    tableData[i].description = tableData[i].description.replaceAll('<', 'left angle bracket')   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 124 semgrep tabledata description replaceall lt gt --> |
| JAVA-125 | Semgrep: >- | `    cp.spawnSync(event['cmd']);` | `    cp.exec('ls')`<br>`};` | Java/Spring | `Semgrep detect-child-process` | `    cp.exec('ls') };` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 125 semgrep cp spawnsync event cmd --> |
| JAVA-126 | Semgrep: >- | `  const data = await ddbDocClient.send(new QueryCommand(params));`<br>`  console.log("Success:", data.Items);`<br>`}`<br>`exports.handler = function(event, context) {`<br>`  const ddbClient = new DynamoDBClient({ region: 'REGION' });`<br>`  const ddbDocClient = DynamoDBDocumentClient.from(ddbClient);`<br>`  const params = {`<br>`    KeyConditionExpression: "Title = :s",`<br>`    ExpressionAttributeValues: {`<br>`      ":s": { S: event.body.title }`<br>`...`<br>`    TableName: "TVSHOWS",`<br>`  }` | `  const data = await ddbDocClient.send(new QueryCommand(params));`<br>`  console.log("Success:", data.Items);`<br>`}` | Java/Spring | `Semgrep dynamodb-request-object` | `  const data = await ddbDocClient.send(new QueryCommand(params));   console.log("Success:", data.Items); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 126 semgrep const data await ddbdocclient send new querycommand params console log success items exports handler function event context ddbclient dynamodbclient region dynamodbdocumentclient --> |
| JAVA-127 | Semgrep: >- | `  await connection.raw('`<br>`    INSERT INTO  (id, character, cartoon, link)`<br>`    VALUES(`<br>`        '${event.id}',`<br>`        '${event.character}',`<br>`        '${event.cartoon}',`<br>`        '${event.link}'`<br>`    )`<br>`    ');` | `  await connection.raw('SELECT * FROM foobar');`<br>`};` | Java/Spring | `Semgrep knex-sqli` | `  await connection.raw('SELECT * FROM foobar'); };` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 127 semgrep await connection raw insert into id character cartoon link values event --> |
| JAVA-128 | Semgrep: >- | `  const [results, fields] = await conn.execute(selectStmt);` | `  const [results2, fields2] = await conn.execute('SELECT * FROM foobar WHERE id = ?', [columnName]);`<br>`  var res = {};`<br>`  if(results.length > 0){`<br>`    res = results.map((row) => JSON.stringify(row));`<br>`    }`<br>`    var response = JSON.stringify({"results": res});`<br>`    conn.end();`<br>`  return response;`<br>`};` | Java/Spring | `Semgrep mysql-sqli` | `  const [results2, fields2] = await conn.execute('SELECT * FROM foobar WHERE id = ?', [columnName]);   var res = {};   if(results.length > 0){     res = results.map((row) => JSON.stringify(row));     }     var response = JSON.stringify({"results": res});     conn.end();   return response; };` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 128 semgrep const results fields await conn execute selectstmt --> |
| JAVA-129 | Semgrep: >- | `    await sequelize.query(query)` | `    await sequelize.query(`<br>`      'SELECT * FROM projects WHERE status = :status',`<br>`      {`<br>`        replacements: { status: 'active' },`<br>`        type: QueryTypes.SELECT`<br>`      }`<br>`    );`<br>`  } catch (error) {`<br>`    console.log(error);`<br>`  }`<br>`  return { key: JSON.stringify(records) };`<br>`};` | Java/Spring | `Semgrep sequelize-sqli` | `    await sequelize.query(       'SELECT * FROM projects WHERE status = :status',       {         replacements: { status: 'active' },         type: QueryTypes.SELECT       }     );   } catch (error) {     console.log(error);   }   return { key: JSON.stringify(records) }; };` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 129 semgrep await sequelize query --> |
| JAVA-130 | Semgrep: >- | `    const script = new vm.Script('`<br>`        function add(a, b) {`<br>`          return a + ${event['something']};`<br>`        }`<br>`        const x = add(1, 2);`<br>`    ');`<br>`    script.runInThisContext();` | `    var sandbox2 = {`<br>`        foo: 1`<br>`    }`<br>`    vm.createContext(sandbox2)`<br>`    vm.runInContext('safeEval(orderLinesData)', sandbox2, { timeout: 2000 })` | Java/Spring | `Semgrep vm-runincontext-injection` | `    var sandbox2 = {         foo: 1     }     vm.createContext(sandbox2)     vm.runInContext('safeEval(orderLinesData)', sandbox2, { timeout: 2000 })` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 130 semgrep const script new vm function add return event something --> |
| JAVA-131 | Semgrep: >- | `  util.toFastProperties(input);`<br>`}`<br>`function ok1() {` | `  util.toFastProperties("smth hardcoded");`<br>`}` | Java/Spring | `Semgrep tofastproperties-code-execution` | `  util.toFastProperties("smth hardcoded"); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 131 semgrep util tofastproperties input function ok1 --> |
| JAVA-132 | Semgrep: >- | `document.write("<OPTION value=1>"+document.location.href.substring(document.location.href.indexOf("default=")+8)+"</OPTION>");` | `document.write("<OPTION value=2>English</OPTION>");` | Java/Spring | `Semgrep dom-based-xss` | `document.write("<OPTION value=2>English</OPTION>");` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 132 semgrep document write option value location href substring indexof default --> |
| JAVA-133 | Semgrep: >- | `  document.write(name);`<br>`}`<br>`function ok1() {`<br>`  const name = "<div>it's ok</div>";` | `  el.innerHTML = name;`<br>`}`<br>`function ok2() {` | Java/Spring | `Semgrep insecure-document-method` | `  el.innerHTML = name; } function ok2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 133 semgrep document write name function ok1 const div it ok --> |
| JAVA-134 | Semgrep: >- | `  document.body.innerHTML = name;`<br>`}`<br>`function ok1() {`<br>`  const name = "<div>it's ok</div>";` | `  el.innerHTML = name;`<br>`}`<br>`function ok2() {` | Java/Spring | `Semgrep insecure-innerhtml` | `  el.innerHTML = name; } function ok2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 134 semgrep document body innerhtml name function ok1 const div it ok --> |
| JAVA-135 | Semgrep: >- | `window.addEventListener("message", handler, false);` | `window.addEventListener("message", function (evt) {`<br>`  if (evt.origin == "http://example.com") {`<br>`    console.log('Normal inline function declaration with origin validation');`<br>`  }`<br>`});` | Java/Spring | `Semgrep insufficient-postmessage-origin-validation` | `window.addEventListener("message", function (evt) {   if (evt.origin == "http://example.com") {     console.log('Normal inline function declaration with origin validation');   } });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 135 semgrep window addeventlistener message handler false --> |
| JAVA-136 | Semgrep: >- | `    location.href = '${userInput}/hi'`<br>`}`<br>`function test4(userInput) {` | `    location.href = 'https://www.hardcoded.place/${userInput}'` | Java/Spring | `Semgrep js-open-redirect-from-function` | `    location.href = `https://www.hardcoded.place/${userInput}`` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 136 semgrep location href userinput hi function test4 --> |
| JAVA-137 | Semgrep: User controlled data in a HTML string may result in XSS | `        this._elt.innerHTML = '<svg viewBox="-20 -20 140 140" width="100" height="100">' +`<br>`            '<defs>' +`<br>`            '<marker id="prism-previewer-easing-marker" viewBox="0 0 4 4" refX="2" refY="2" markerUnits="strokeWidth">' +`<br>`            '<circle cx="2" cy="2" r="1.5" />' +`<br>`            '</marker>' +`<br>`            '</defs>' +`<br>`            '<path d="M0,100 C20,50, 40,30, 100,0" />' +`<br>`            '<line x1="0" y1="100" x2="20" y2="50" marker-start="url(' + location.href + '#prism-previewer-easing-marker)" marker-end="url(' + location.href + '#prism-previewer-easing-marker)" />' +`<br>`            '<line x1="100" y1="0" x2="40" y2="30" marker-start="url(' + location.href + '#prism-previewer-easing-marker)" marker-end="url(' + location.href + '#prism-previewer-easing-marker)" />' +`<br>`            '</svg>';`<br>`...`<br>`    value = queries[key];`<br>`    if (angular.isDefined(value)) {` | `    params.push(key + '=' + value.toString());` | Java/Spring | `Semgrep raw-html-concat` | `    params.push(key + '=' + value.toString());` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 137 semgrep user controlled data in html string may result xss this elt innerhtml svg viewbox 20 140 width 100 height defs marker --> |
| JAVA-138 | Semgrep: >- | `        this.$element = jQuery(["<span class=\"jqtree-title jqtree-dragging\">",nodeName, "</span>"].join());`<br>`        this.$element.css("position", "absolute");`<br>`        $tree.append(this.$element);`<br>`    }`<br>`    DragElement.prototype.move = function (pageX, pageY) {`<br>`        this.$element.offset({`<br>`            left: pageX - this.offsetX,`<br>`            top: pageY - this.offsetY`<br>`        });`<br>`    };`<br>`...`<br>`    value = queries[key];`<br>`    if (angular.isDefined(value)) {` | `    params.push(key + '=' + value.toString());` | Java/Spring | `Semgrep raw-html-join` | `    params.push(key + '=' + value.toString());` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 138 semgrep this element jquery span class jqtree title dragging nodename join css position absolute tree append dragelement prototype move function pagex pagey --> |
| JAVA-139 | Semgrep: >- | `window.top?.postMessage("data", "*", [`<br>`    transfer,`<br>`]);`<br>`//postMessage Safe Usage` | `popup.postMessage("hello there!", "http://domain.tld");` | Java/Spring | `Semgrep wildcard-postmessage-configuration` | `popup.postMessage("hello there!", "http://domain.tld");` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 139 semgrep window top postmessage data transfer safe usage --> |
| JAVA-140 | Semgrep: Directory listing/indexing is enabled, which may lead to disclosure of sensitive directories | `app.use('/ftp', serveIndex('ftp', {`<br>`    icons: true`<br>`}));` | `app.use(bodyParser.text({`<br>`    type: '*/*'`<br>`}));` | Java/Spring | `Semgrep express-check-directory-listing` | `app.use(bodyParser.text({     type: '*/*' }));` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 140 semgrep directory listing indexing is enabled which may lead to disclosure of sensitive directories app use ftp serveindex icons true --> |
| JAVA-141 | Semgrep: >- | `        safeEval(orderLinesData)` | `        vm.runInContext("safeEval('orderLinesData')", sandbox, { timeout: 2000 }) // ignore hardcoded strings in semgrep` | Java/Spring | `Semgrep express-detect-notevil-usage` | `        vm.runInContext("safeEval('orderLinesData')", sandbox, { timeout: 2000 }) // ignore hardcoded strings in semgrep` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 141 semgrep safeeval orderlinesdata --> |
| JAVA-142 | Semgrep: >- | `app.get('/ok-protected', jwt(opts), function(req, res) {`<br>`    if (!req.user.admin) return res.sendStatus(401);`<br>`    res.sendStatus(200);`<br>`});` | `app.get('/ok-protected', jwt({ secret: process.env.SECRET, isRevoked: blacklist.isRevoked }), function(req, res) {`<br>`    if (!req.user.admin) return res.sendStatus(401);`<br>`    res.sendStatus(200);`<br>`});` | Java/Spring | `Semgrep express-jwt-not-revoked` | `app.get('/ok-protected', jwt({ secret: process.env.SECRET, isRevoked: blacklist.isRevoked }), function(req, res) {     if (!req.user.admin) return res.sendStatus(401);     res.sendStatus(200); });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 142 semgrep app get ok protected jwt opts function req res if user admin return sendstatus 401 200 --> |
| JAVA-143 | Semgrep: >- | `	libxmljs2.parseXml(req.body, {noent:true,noblanks:true})` | `	libxmljs2.parseXml(req.body, {noent:false,noblanks:true})`<br>`}` | Java/Spring | `Semgrep express-libxml-noent` | `	libxmljs2.parseXml(req.body, {noent:false,noblanks:true}) }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 143 semgrep libxmljs2 parsexml req body noent true noblanks --> |
| JAVA-144 | Semgrep: Detected use of parseXml() function with the `noent` field set to `true`. This can lead to | `        libxml.parseXml(data, xml_opts)` | `        libxml.parseXml(data, { noblanks: true, nocdata: true })`<br>`        const xmlString = xmlDoc.toString(false)`<br>`        res.status(410)`<br>`        next(new Error('B2B customer complaints via file upload have been deprecated for security reasons: ' + utils.trunc(xmlString, 400) + ' (' + file.originalname + ')'))`<br>`      } catch (err) {`<br>`        if (utils.contains(err.message, 'Script execution timed out')) {`<br>`          res.status(503)`<br>`          next(new Error('Sorry, we are temporarily not available! Please try again later.'))`<br>`        } else {`<br>`          res.status(410)`<br>`...`<br>`  res.status(204).end()`<br>`}` | Java/Spring | `Semgrep express-libxml-vm-noent` | `        libxml.parseXml(data, { noblanks: true, nocdata: true })         const xmlString = xmlDoc.toString(false)         res.status(410)         next(new Error('B2B customer complaints via file upload have been deprecated for security reasons: ' + utils.trunc(xmlString, 400) + ' (' + file.originalname + ')'))       } catch (err) {         if (utils.contains(err.message, 'Script execution timed ou...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 144 semgrep detected use of parsexml function with the noent field set to true this can lead libxml data xml opts --> |
| JAVA-145 | Semgrep: >- | `        var pth = path.join(opts.path, data[i]);`<br>`        doSmth(pth);`<br>`    }`<br>`})`<br>`app.post('/ok-test1', function okTest1(req,res) {`<br>`    let data = ['one', 'two', 'three'];`<br>`    for (let x of data) {` | `        var pth = path.join(opts.path, x);`<br>`        doSmth(pth);`<br>`    }`<br>`})`<br>`app.post('/ok-test2', function okTest2() {`<br>`    function someFunc() {`<br>`        createFile({` | Java/Spring | `Semgrep express-path-join-resolve-traversal` | `        var pth = path.join(opts.path, x);         doSmth(pth);     } }) app.post('/ok-test2', function okTest2() {     function someFunc() {         createFile({` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 145 semgrep var pth path join opts data dosmth app post ok test1 function oktest1 req res let one two three --> |
| JAVA-146 | Semgrep: >- | `  secret: 'foo',`<br>`  saveUninitialized: false,`<br>`}`<br>`app.use(session(secret2));`<br>`app.use(session({` | `  secret: config.secret,`<br>`  resave: false,`<br>`  saveUninitialized: false,`<br>`}));` | Java/Spring | `Semgrep express-session-hardcoded-secret` | `  secret: config.secret,   resave: false,   saveUninitialized: false, }));` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 146 semgrep secret foo saveuninitialized false app use session secret2 --> |
| JAVA-147 | Semgrep: >- | `    request.get(url)`<br>`  }`<br>`}`<br>`module.exports = function goodWithTypes () {`<br>`  return ({ params, query, session }: Request, res: Response, next: NextFunction) => {`<br>`    const url = session` | `     request.get(url)`<br>`  }`<br>`}`<br>`module.exports = function advanced () {`<br>`  return ({ body }: Request, res: Response, next: NextFunction) => {`<br>`    const url = body.url`<br>`    joinModeOrDeepSemgrep(url, res, next)`<br>`  }`<br>`  function joinModeOrDeepSemgrep (url: string, res: Response, next: NextFunction) {`<br>`      // todo: express-ssrf`<br>`...`<br>`  }`<br>`}` | Java/Spring | `Semgrep express-ssrf` | `     request.get(url)   } } module.exports = function advanced () {   return ({ body }: Request, res: Response, next: NextFunction) => {     const url = body.url     joinModeOrDeepSemgrep(url, res, next)   }   function joinModeOrDeepSemgrep (url: string, res: Response, next: NextFunction) {       // todo: express-ssrf ...   } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 147 semgrep request get url module exports function goodwithtypes return params query session res response next nextfunction const --> |
| JAVA-148 | Semgrep: The following function call $SER.$FUNC accepts user controlled data which can result in Remote | `	serialize_to_js.deserialize(str)` | `	foo.deserialize(str)`<br>`}` | Java/Spring | `Semgrep express-third-party-object-deserialization` | `	foo.deserialize(str) }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 148 semgrep the following function call ser func accepts user controlled data which can result in remote serialize to js deserialize str --> |
| JAVA-149 | Semgrep: >- | `  myObj[req.body] = foobar()`<br>`  res.send('ok')`<br>`})`<br>`app.get('/okTest', function(req, res) {`<br>`  var prop = "$" + req.query.userControlled` | `  myObj[prop] = function() {}`<br>`  res.send('ok')`<br>`})` | Java/Spring | `Semgrep remote-property-injection` | `  myObj[prop] = function() {}   res.send('ok') })` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 149 semgrep myobj req body foobar res send ok app get oktest function var prop query usercontrolled --> |
| JAVA-150 | Semgrep: >- | `    return res.render('tpl.' + req.query.path + '.smth-else', {foo: bar})`<br>`})`<br>`app.get('/ok-test', (req, res) => {` | `    return res.render(hardcodedPath, {foo: bar})`<br>`})`<br>`app.listen(port, () => console.log('Example app listening at http://localhost:${port}'))` | Java/Spring | `Semgrep res-render-injection` | `    return res.render(hardcodedPath, {foo: bar}) }) app.listen(port, () => console.log(`Example app listening at http://localhost:${port}`))` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 150 semgrep return res render tpl req query path smth else foo bar app get ok test --> |
| JAVA-151 | Semgrep: >- | `    res.write('Response</br>' + html);`<br>`});`<br>`const jsonRouter = express.Router();`<br>`jsonRouter.use(express.json());`<br>`jsonRouter.get('/noxss-json', function (req, res) {`<br>`    var name = req.query.name;` | `    res.write({ name });`<br>`});`<br>`app.use(jsonRouter);`<br>`// For https://github.com/returntocorp/semgrep-rules/issues/2872`<br>`app.post(`<br>`    "/:id",`<br>`    async (req, res, next) => {`<br>`        const userId = req.params?.id;`<br>`        if (user.email !== req.body.email) {`<br>`            const command = new AdminUpdateUserAttributesCommand({`<br>`...`<br>`                ],`<br>`            });` | Java/Spring | `Semgrep direct-response-write` | `    res.write({ name }); }); app.use(jsonRouter); // For https://github.com/returntocorp/semgrep-rules/issues/2872 app.post(     "/:id",     async (req, res, next) => {         const userId = req.params?.id;         if (user.email !== req.body.email) {             const command = new AdminUpdateUserAttributesCommand({ ...                 ],             });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 151 semgrep res write response br html const jsonrouter express router use json get noxss function req var name query --> |
| JAVA-152 | Semgrep: >- | `  template.escape = (t) => { return t; }`<br>`  let html = template.render(blogItem, { });`<br>`}`<br>`function ok() {` | `  const template = require("mustache");`<br>`  let html = template.render(blogItem, { });`<br>`}` | Java/Spring | `Semgrep escape-function-overwrite` | `  const template = require("mustache");   let html = template.render(blogItem, { }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 152 semgrep template escape return let html render blogitem function ok --> |
| JAVA-153 | Semgrep: >- | `    res.set('access-control-allow-origin', origin)`<br>`});`<br>`app.get('/okTest1', function (req, res) {`<br>`    foobar()` | `    res.set('access-control-allow-origin', 'xyz.com')`<br>`});` | Java/Spring | `Semgrep cors-misconfiguration` | `    res.set('access-control-allow-origin', 'xyz.com') });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 153 semgrep res set access control allow origin app get oktest1 function req foobar --> |
| JAVA-154 | Semgrep: >- | `    let data = Object.assign(defaultData, newData)`<br>`    doSmthWith(data)`<br>`    return res.send(func())`<br>`})`<br>`let okController = function (req, res) {`<br>`    const defaultData = {foo: {bar: true}}` | `    let data = Object.assign(defaultData, {foo: getFoo()})`<br>`    doSmthWith(data)`<br>`    return res.send({ok: true})`<br>`}`<br>`app.get('/ok-test2', okController)`<br>`app.listen(port, () => console.log('Example app listening at http://localhost:${port}'))` | Java/Spring | `Semgrep express-data-exfiltration` | `    let data = Object.assign(defaultData, {foo: getFoo()})     doSmthWith(data)     return res.send({ok: true}) } app.get('/ok-test2', okController) app.listen(port, () => console.log(`Example app listening at http://localhost:${port}`))` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 154 semgrep let data object assign defaultdata newdata dosmthwith return res send func okcontroller function req const foo bar true --> |
| JAVA-155 | Semgrep: >- | `    parser.parse(req.body)`<br>`    res.send('Hello World!')`<br>`}`<br>`app.get('/okTest1', async (req, res) => {`<br>`    var parser = new expat.Parser('UTF-8')` | `    parser.write('<xml>hardcoded</xml>')`<br>`    res.send('Hello World!')`<br>`})`<br>`app.get('/okTest2', async (req, res) => {`<br>`    var parser = new expat.Parser('UTF-8')`<br>`    var data = foo()` | Java/Spring | `Semgrep express-expat-xxe` | `    parser.write('<xml>hardcoded</xml>')     res.send('Hello World!') }) app.get('/okTest2', async (req, res) => {     var parser = new expat.Parser('UTF-8')     var data = foo()` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 155 semgrep parser parse req body res send hello world app get oktest1 async var new expat utf --> |
| JAVA-156 | Semgrep: >- | `let hardcodedSecret = 'shhhhhhared-secret'`<br>`app.get('/protected2', jwt({ secret: hardcodedSecret }), function(req, res) {`<br>`    if (!req.user.admin) return res.sendStatus(401);`<br>`    res.sendStatus(200);`<br>`});`<br>`let secret = "hardcode"`<br>`const opts = Object.assign({issuer: 'http://issuer'}, {secret: secret})`<br>`app.get('/protected3', jwt(opts), function(req, res) {`<br>`    if (!req.user.admin) return res.sendStatus(401);`<br>`    res.sendStatus(200);`<br>`});` | `app.get('/ok-protected', jwt({ secret: process.env.SECRET }), function(req, res) {`<br>`    if (!req.user.admin) return res.sendStatus(401);`<br>`    res.sendStatus(200);`<br>`});`<br>`let configSecret = config.get('secret')`<br>`const opts = Object.assign({issuer: 'http://issuer'}, {secret: configSecret})` | Java/Spring | `Semgrep express-jwt-hardcoded-secret` | `app.get('/ok-protected', jwt({ secret: process.env.SECRET }), function(req, res) {     if (!req.user.admin) return res.sendStatus(401);     res.sendStatus(200); }); let configSecret = config.get('secret') const opts = Object.assign({issuer: 'http://issuer'}, {secret: configSecret})` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 156 semgrep let hardcodedsecret shhhhhhared secret app get protected2 jwt function req res if user admin return sendstatus 401 200 hardcode --> |
| JAVA-157 | Semgrep: >- | `    await page.evaluateJavaScript(req.body.script);` | `    var url = 'https://stackoverflow.com/'`<br>`    const status = await page.openUrl(url, {}, {});`<br>`    const content = await page.property('content');`<br>`    console.log(content);`<br>`    await instance.exit();`<br>`    res.send('Hello World!')`<br>`})`<br>`app.listen(port, () => console.log('Example app listening at http://localhost:${port}'))` | Java/Spring | `Semgrep express-phantom-injection` | `    var url = 'https://stackoverflow.com/'     const status = await page.openUrl(url, {}, {});     const content = await page.property('content');     console.log(content);     await instance.exit();     res.send('Hello World!') }) app.listen(port, () => console.log(`Example app listening at http://localhost:${port}`))` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 157 semgrep await page evaluatejavascript req body script --> |
| JAVA-158 | Semgrep: >- | `    await page.evaluate('alert(' + body + ')');`<br>`    await page.screenshot({path: 'example.png'});`<br>`    await browser.close();`<br>`    res.send('Hello World!');`<br>`}`<br>`app.post('/test2', controller)`<br>`app.post('/ok-test', async (req, res) => {`<br>`    const browser = await puppeteer.launch();`<br>`    const page = await browser.newPage();` | `    await page.goto('https://example.com');`<br>`    await page.screenshot({path: 'example.png'});`<br>`    await browser.close();`<br>`    res.send('Hello World!');`<br>`})`<br>`const controller = async (req, res) => {`<br>`    const browser = await puppeteer.launch();`<br>`    const page = await browser.newPage();` | Java/Spring | `Semgrep express-puppeteer-injection` | `    await page.goto('https://example.com');     await page.screenshot({path: 'example.png'});     await browser.close();     res.send('Hello World!'); }) const controller = async (req, res) => {     const browser = await puppeteer.launch();     const page = await browser.newPage();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 158 semgrep await page evaluate alert body screenshot path example png browser close res send hello world app post test2 controller --> |
| JAVA-159 | Semgrep: >- | `    s.run('lol(${req.query.userInput})', cb);`<br>`    res.send('Hello world');`<br>`})`<br>`app.get('/ok-test1', function (req, res) {` | `    const s = new Sandbox();`<br>`    s.run('lol("hi")', cb);`<br>`    res.send('Hello world');`<br>`})`<br>`app.get('/ok-test2', function (req, res) {` | Java/Spring | `Semgrep express-sandbox-code-injection` | `    const s = new Sandbox();     s.run('lol("hi")', cb);     res.send('Hello world'); }) app.get('/ok-test2', function (req, res) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 159 semgrep run lol req query userinput cb res send hello world app get ok test1 function --> |
| JAVA-160 | Semgrep: >- | `    const script = new vm.Script('`<br>`        function add(a, b) {`<br>`          return a + ${req.query.userInput};`<br>`        }`<br>`        const x = add(1, 2);`<br>`    ');`<br>`    script.runInThisContext();`<br>`    res.send('hello world')`<br>`})` | `app.get('/', function okTest6(req, res) {`<br>`    const script = new vm.Script('`<br>`        function add(a, b) {`<br>`          return a + b;`<br>`        }`<br>`        const x = add(1, 2);`<br>`    ');`<br>`    script.runInThisContext();`<br>`    res.send('hello world')`<br>`})` | Java/Spring | `Semgrep express-vm-injection` | `app.get('/', function okTest6(req, res) {     const script = new vm.Script(`         function add(a, b) {           return a + b;         }         const x = add(1, 2);     `);     script.runInThisContext();     res.send('hello world') })` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 160 semgrep const script new vm function add return req query userinput --> |
| JAVA-161 | Semgrep: >- | `  const nodeVM = new NodeVM({timeout: 40 * 1000, sandbox});`<br>`  return nodeVM`<br>`})` | `app.get('/ok-test4', async function okTest1() {`<br>`  code = '`<br>`    console.log("Hello world")`<br>`  ';`<br>`  const sandbox = {`<br>`    setTimeout,`<br>`    fs`<br>`  };`<br>`  return new VM({timeout: 40 * 1000, sandbox}).run(code);`<br>`})` | Java/Spring | `Semgrep express-vm2-injection` | `app.get('/ok-test4', async function okTest1() {   code = `     console.log("Hello world")   `;   const sandbox = {     setTimeout,     fs   };   return new VM({timeout: 40 * 1000, sandbox}).run(code); })` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 161 semgrep const nodevm new timeout 40 1000 sandbox return --> |
| JAVA-162 | Semgrep: >- | `  const img = wkhtmltoimage.generate(req.body, { output: 'vuln.pdf' })`<br>`  res.send(img)`<br>`})`<br>`app.post('/test-ok', async (req, res) => {` | `  const data = '<html></html>'`<br>`  const img = wkhtmltoimage.generate(data, { output: 'vuln.pdf' })`<br>`  res.send(img)`<br>`})`<br>`app.listen(port, () => console.log('Example app listening at http://localhost:${port}'))` | Java/Spring | `Semgrep express-wkhtmltoimage-injection` | `  const data = '<html></html>'   const img = wkhtmltoimage.generate(data, { output: 'vuln.pdf' })   res.send(img) }) app.listen(port, () => console.log(`Example app listening at http://localhost:${port}`))` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 162 semgrep const img wkhtmltoimage generate req body output vuln pdf res send app post test ok async --> |
| JAVA-163 | Semgrep: >- | `  const pdf = wkhtmltopdf(req.query.q, { output: 'vuln.pdf' })`<br>`  res.send(pdf)`<br>`})`<br>`app.post('/ok', async (req, res) => {` | `  const pdf = wkhtmltopdf('<html></html>', { output: 'vuln.pdf' })`<br>`  res.send(pdf)`<br>`})`<br>`app.post('/test', async (req, res) => {` | Java/Spring | `Semgrep express-wkhtmltopdf-injection` | `  const pdf = wkhtmltopdf('<html></html>', { output: 'vuln.pdf' })   res.send(pdf) }) app.post('/test', async (req, res) => {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 163 semgrep const pdf wkhtmltopdf req query output vuln res send app post ok async --> |
| JAVA-164 | Semgrep: >- | `        const content = xml2json.toJson(req.body, {coerce: true, object: true});`<br>`        res.send(content)`<br>`    })`<br>`    app.listen(port, () => console.log('Example app listening at http://localhost:${port}'))`<br>`}`<br>`function okTest() {`<br>`    const express = require('express')`<br>`    const xml2json = require('xml2json')`<br>`    const app = express()`<br>`    const port = 3000`<br>`    app.get('/', (req, res) => {` | `        const content = expat.toJson(someVerifiedData(), {coerce: true, object: true});`<br>`        res.send(content)`<br>`    })`<br>`    app.listen(port, () => console.log('Example app listening at http://localhost:${port}'))`<br>`}` | Java/Spring | `Semgrep express-xml2json-xxe` | `        const content = expat.toJson(someVerifiedData(), {coerce: true, object: true});         res.send(content)     })     app.listen(port, () => console.log(`Example app listening at http://localhost:${port}`)) }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 164 semgrep const content xml2json tojson req body coerce true object res send app listen port console log example listening at http localhost --> |
| JAVA-165 | Semgrep: >- | `    html = html.concat(req.query.message)`<br>`    html = html.concat("</h1>")`<br>`    res.send(html);`<br>`})`<br>`app.post('/ok-test', async (req, res) => {`<br>`    let { foobar } = req.query`<br>`    let sanitizedParam = sanitizeUrl(foobar)`<br>`    const url = '${baseUrl}/foo/bar?yo=123&param=${sanitizedParam}'` | `    return res.send('<a href="${url}" />')`<br>`})`<br>`app.get('/ok', async (req, res) => {` | Java/Spring | `Semgrep raw-html-format` | `    return res.send(`<a href="${url}" />`) }) app.get('/ok', async (req, res) => {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 165 semgrep html concat req query message h1 res send app post ok test async let foobar --> |
| JAVA-166 | Semgrep: >- | `    const func = require(req.body)`<br>`    return res.send(func())`<br>`})(req, res)`<br>`app.get('/ok-test', (req, res) => {` | `    const func = require(hardcodedPath)`<br>`    return res.send(func())`<br>`})`<br>`let okController = function (req, res) {` | Java/Spring | `Semgrep require-request` | `    const func = require(hardcodedPath)     return res.send(func()) }) let okController = function (req, res) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 166 semgrep const func require req body return res send app get ok test --> |
| JAVA-167 | Semgrep: >- | `    res.set('X-Frame-Options', req.query.opts)`<br>`    res.send('ok')`<br>`})`<br>`app.get('/', function (req, res) {` | `    res.set('X-Frame-Options', 'SAMEORIGIN')`<br>`    res.send('ok')`<br>`})` | Java/Spring | `Semgrep x-frame-options-misconfiguration` | `    res.set('X-Frame-Options', 'SAMEORIGIN')     res.send('ok') })` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 167 semgrep res set frame options req query opts send ok app get function --> |
| JAVA-168 | Semgrep: >- | `    var grpc = require('grpc');`<br>`    var booksProto = grpc.load('books.proto');`<br>`    var server = new grpc.Server();`<br>`    server.addProtoService(booksProto.books.BookService.service, {});`<br>`    server.bind('0.0.0.0:50051', grpc.ServerCredentials.createInsecure());`<br>`    server.start();`<br>`}`<br>`function testOk1() {` | `    var {credentials, Client} = require('grpc');`<br>`    var channel_creds = credentials.createSsl(root_certs);`<br>`    var client = new Client(address, channel_creds);`<br>`    client.list({}, function(error, books) {`<br>`        if (error)`<br>`            console.log('Error: ', error);`<br>`        else`<br>`            console.log(books);`<br>`    });`<br>`}` | Java/Spring | `Semgrep grpc-nodejs-insecure-connection` | `    var {credentials, Client} = require('grpc');     var channel_creds = credentials.createSsl(root_certs);     var client = new Client(address, channel_creds);     client.list({}, function(error, books) {         if (error)             console.log('Error: ', error);         else             console.log(books);     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 168 semgrep var grpc require booksproto load books proto server new addprotoservice bookservice service bind 50051 servercredentials createinsecure start --> |
| JAVA-169 | Semgrep: Found an initialization of the Intercom Messenger that identifies a | `myCustomSettings = {`<br>`  app_id: appId,`<br>`  name: myUserName,`<br>`  user_id: myUserID,`<br>`};`<br>`Intercom('boot', myCustomSettings);` | `myCustomSettingsWithHash = {`<br>`  app_id: appId,`<br>`  name: myUserName,`<br>`  user_id: myUserID,`<br>`  user_hash: 'my-user-hash'`<br>`};`<br>`Intercom('boot', myCustomSettingsWithHash);` | Java/Spring | `Semgrep intercom-settings-user-identifier-without-user-hash` | `myCustomSettingsWithHash = {   app_id: appId,   name: myUserName,   user_id: myUserID,   user_hash: 'my-user-hash' }; Intercom('boot', myCustomSettingsWithHash);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 169 semgrep found an initialization of the intercom messenger that identifies mycustomsettings app id appid name myusername user myuserid boot --> |
| JAVA-170 | Semgrep: >- | `    const token = JWT.sign(user, secret)`<br>`    return token;`<br>`}`<br>`function example2(user) {` | `    const token = JWT.sign({name: user.name}, secret)`<br>`    return token;`<br>`}`<br>`function example3(user) {` | Java/Spring | `Semgrep jose-exposed-data` | `    const token = JWT.sign({name: user.name}, secret)     return token; } function example3(user) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 170 semgrep const token jwt sign user secret return function example2 --> |
| JAVA-171 | Semgrep: >- | `  const token9 = JWT.verify(payload, key18)`<br>`}`<br>`function example10() {`<br>`  const jose = require('jose')`<br>`  const { JWK, JWT } = jose`<br>`  const payload = {foo: 'bar'}`<br>`  const secret2 = config.secret` | `  const token11 = JWT.sign(payload, JWK.asKey(secret2))`<br>`}`<br>`function example11() {`<br>`  const jose = require('jose')`<br>`  const { JWK, JWT } = jose`<br>`  const payload = {foo: 'bar'}`<br>`  const secret2 = config.secret` | Java/Spring | `Semgrep hardcoded-jwt-secret` | `  const token11 = JWT.sign(payload, JWK.asKey(secret2)) } function example11() {   const jose = require('jose')   const { JWK, JWT } = jose   const payload = {foo: 'bar'}   const secret2 = config.secret` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 171 semgrep const token9 jwt verify payload key18 function example10 jose require jwk foo bar --> |
| JAVA-172 | Semgrep: >- | `            select.append(bar)`<br>`        }`<br>`      }`<br>`    );`<br>`  }`<br>`  function ok1() {`<br>`    const item = '<div></div>';` | `    $( ".inner" ).wrap(item);`<br>`  }`<br>`  function ok2(userInput) {` | Java/Spring | `Semgrep jquery-insecure-method` | `    $( ".inner" ).wrap(item);   }   function ok2(userInput) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 172 semgrep select append bar function ok1 --> |
| JAVA-173 | Semgrep: >- | `            $( "#weather-temp" ).html( "<strong>" + result + "</strong> degrees" );`<br>`        }`<br>`    });`<br>`}`<br>`function ok1() {` | `     $( "button.continue" ).text( "Next Step..." );`<br>`}`<br>`function ok2() {`<br>`    $.ajax({`<br>`        url: "/api/getWeather",`<br>`        data: {`<br>`            zipcode: 97201`<br>`        },`<br>`        success: function( result ) {` | Java/Spring | `Semgrep prohibit-jquery-html` | `     $( "button.continue" ).text( "Next Step..." ); } function ok2() {     $.ajax({         url: "/api/getWeather",         data: {             zipcode: 97201         },         success: function( result ) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 173 semgrep weather temp html strong result degrees function ok1 --> |
| JAVA-174 | Semgrep: >- | `  if (jwt.decode(token, true).param === true) {`<br>`    console.log('token is valid');`<br>`  }`<br>`}`<br>`function ok(token, key) {` | `  jwt.verify(token, key);`<br>`  if (jwt.decode(token, true).param === true) {`<br>`    console.log('token is valid');`<br>`  }`<br>`}`<br>`const ok2 = (token, key) => {` | Java/Spring | `Semgrep jwt-decode-without-verify` | `  jwt.verify(token, key);   if (jwt.decode(token, true).param === true) {     console.log('token is valid');   } } const ok2 = (token, key) => {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 174 semgrep if jwt decode token true param console log is valid function ok key --> |
| JAVA-175 | Semgrep: >- | `    var token = jwt.sign(user, key, {expiresIn: 60*60*10});`<br>`    res.json({`<br>`        success: true,`<br>`        message: 'Enjoy your token!',`<br>`        token: token`<br>`    });`<br>`});`<br>`User.findOne({name: req.body.name}, function(err, user){` | `    const {name, email} = user`<br>`    var token = jwt.sign({name, email}, key, {expiresIn: 60*60*10});`<br>`    return token;`<br>`});`<br>`User.findOne({name: req.body.name}, function(err, user){` | Java/Spring | `Semgrep jwt-exposed-data` | `    const {name, email} = user     var token = jwt.sign({name, email}, key, {expiresIn: 60*60*10});     return token; }); User.findOne({name: req.body.name}, function(err, user){` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 175 semgrep var token jwt sign user key expiresin 60 10 res json success true message enjoy your --> |
| JAVA-176 | Semgrep: >- | `    jwt.verify('token-here', secret, { algorithms: ['RS256', 'none'] }, function(err, payload) {`<br>`        console.log(payload);`<br>`    });`<br>`}` | `const jwt = require("jsonwebtoken");`<br>`const secret = 'some-secret';`<br>`const payload = jwt.verify('token-here', secret, { algorithms: ['RS256', 'HS256'] });` | Java/Spring | `Semgrep jwt-none-alg` | `const jwt = require("jsonwebtoken"); const secret = 'some-secret'; const payload = jwt.verify('token-here', secret, { algorithms: ['RS256', 'HS256'] });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 176 semgrep jwt verify token here secret algorithms rs256 none function err payload console log --> |
| JAVA-177 | Semgrep: >- | `    const decoded = jwt.decode(token, secretKey, 'false');`<br>`    res.json({ message: 'Hello ${decoded.username}' });`<br>`  } catch (error) {`<br>`    res.status(401).json({ error: 'Unauthorized. Invalid token.' });`<br>`  }`<br>`});`<br>`// Route that requires authentication`<br>`app.get('/protectedRoute4', (req, res) => {`<br>`  const token = req.headers.authorization;`<br>`  if (!token) {`<br>`...`<br>`  }`<br>`  try {` | `    const decoded = jwt.decode(token, secretKey);`<br>`    res.json({ message: 'Hello ${decoded.username}' });`<br>`  } catch (error) {`<br>`    res.status(401).json({ error: 'Unauthorized. Invalid token.' });`<br>`  }`<br>`});`<br>`// Route that requires authentication`<br>`app.get('/protectedRoute5', (req, res) => {`<br>`  const token = req.headers.authorization;`<br>`  if (!token) {`<br>`...`<br>`  }`<br>`  try {` | Java/Spring | `Semgrep jwt-simple-noverify` | `    const decoded = jwt.decode(token, secretKey);     res.json({ message: `Hello ${decoded.username}` });   } catch (error) {     res.status(401).json({ error: 'Unauthorized. Invalid token.' });   } }); // Route that requires authentication app.get('/protectedRoute5', (req, res) => {   const token = req.headers.authorization;   if (!token) { ...   }   try {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 177 semgrep const decoded jwt decode token secretkey false res json message hello username catch error status 401 unauthorized invalid --> |
| JAVA-178 | Semgrep: >- | `    zlib.deflate(payload, (err, buffer) => {});`<br>`})`<br>`for (let i = 0; i < 30000; ++i) {` | `    zlib.deflateSync(payload);`<br>`}` | Java/Spring | `Semgrep zlib-async-loop` | `    zlib.deflateSync(payload); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 178 semgrep zlib deflate payload err buffer for let 30000 --> |
| JAVA-179 | Semgrep: >- | `  return 'this is {start.line}'`<br>`}`<br>`function name3() {` | `  return "this is ${start.line}"`<br>`}`<br>`function name3() {` | Java/Spring | `Semgrep missing-template-string-indicator` | `  return "this is ${start.line}" } function name3() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 179 semgrep return this is start line function name3 --> |
| JAVA-180 | Semgrep: >- | `const str2 = old_str2.replaceAll("\t", "    ")` | `const str3 = old_str3.replace("\t", "    ");` | Java/Spring | `Semgrep no-replaceall` | `const str3 = old_str3.replace("\t", "    ");` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 180 semgrep const str2 old replaceall --> |
| JAVA-181 | Semgrep: `$X` is assigned twice; the first assignment is useless | `x3 = 1;`<br>`x3 = 2;` | `x4 = {value1: 42};`<br>`x4 = {x4, value2: 43};` | Java/Spring | `Semgrep useless-assignment` | `x4 = {value1: 42}; x4 = {x4, value2: 43};` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 181 semgrep is assigned twice the first assignment useless x3 --> |
| JAVA-182 | Semgrep: >- | `  return eval(command)`<br>`});`<br>`function ok1(req,res) {`<br>`  var command = "eval('123')";` | `  return eval(command)`<br>`}` | Java/Spring | `Semgrep code-string-concat` | `  return eval(command) }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 182 semgrep return eval command function ok1 req res var 123 --> |
| JAVA-183 | Semgrep: >- | `    cp.spawn('sh', [userInput]);`<br>`}`<br>`function testOk(userInput) {`<br>`    foobar(userInput);` | `    spawn('ls', ['-la', '/tmp']);`<br>`}` | Java/Spring | `Semgrep dangerous-spawn-shell` | `    spawn('ls', ['-la', '/tmp']); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 183 semgrep cp spawn sh userinput function testok foobar --> |
| JAVA-184 | Semgrep: >- | `  fs.writeFile(fileName, data, (err) => {`<br>`    if (err) throw err;`<br>`    console.log('The file has been saved!');`<br>`  });`<br>`}`<br>`function okTest1(data) {`<br>`  const data = new Uint8Array(Buffer.from('Hello Node.js'));` | `  fs.writeFile('message.txt', data, (err) => {`<br>`    if (err) throw err;`<br>`    console.log('The file has been saved!');`<br>`  });`<br>`}`<br>`async function okTest2() {`<br>`  let filehandle;`<br>`  try {` | Java/Spring | `Semgrep detect-non-literal-fs-filename` | `  fs.writeFile('message.txt', data, (err) => {     if (err) throw err;     console.log('The file has been saved!');   }); } async function okTest2() {   let filehandle;   try {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 184 semgrep fs writefile filename data err if throw console log the file has been saved function oktest1 --> |
| JAVA-185 | Semgrep: >- | `  const reg = new RegExp("\\w+" + name)`<br>`  return reg.exec(name)`<br>`}`<br>`function jsliteral (name) {`<br>`  const exp = /a.*/;` | `  const reg = new RegExp(exp);`<br>`  return reg.exec(name);`<br>`}` | Java/Spring | `Semgrep detect-non-literal-regexp` | `  const reg = new RegExp(exp);   return reg.exec(name); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 185 semgrep const reg new regexp name return exec function jsliteral exp --> |
| JAVA-186 | Semgrep: >- | `    require(path.resolve(process.cwd(), file, source));`<br>`}`<br>`function okDynamicRequire1() {`<br>`    var lib = path.join(path.dirname(fs.realpathSync(__filename)), "index.js");` | `    require(lib).run(process.argv.slice(2));`<br>`}`<br>`function okDynamicRequire2(userInput) {`<br>`    var name = process.env.NAME`<br>`    var path = name + '/smth/path';`<br>`    var mod = path + '/module.js';` | Java/Spring | `Semgrep detect-non-literal-require` | `    require(lib).run(process.argv.slice(2)); } function okDynamicRequire2(userInput) {     var name = process.env.NAME     var path = name + '/smth/path';     var mod = path + '/module.js';` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 186 semgrep require path resolve process cwd file source function okdynamicrequire1 var lib join dirname fs realpathsync filename index js --> |
| JAVA-187 | Semgrep: >- | `"a".match(r)` | `"a".match(b)` | Java/Spring | `Semgrep detect-redos` | `"a".match(b)` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 187 semgrep match --> |
| JAVA-188 | Semgrep: >- | `  const hmac = crypto.createHmac('sha256', rsa_key)`<br>`  return hmac.update(email + this.roles.deluxe).digest('hex')`<br>`}`<br>`const safely_stored_key = config.get('AWS_KEY')` | `const safe_hmac = crypto.createHmac('sha256', safely_stored_key)` | Java/Spring | `Semgrep hardcoded-hmac-key` | `const safe_hmac = crypto.createHmac('sha256', safely_stored_key)` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 188 semgrep const hmac crypto createhmac sha256 rsa key return update email this roles deluxe digest hex safely stored config get aws --> |
| JAVA-189 | Semgrep: >- | `    items[req.query.name] = req.query.text;`<br>`    res.end(200);`<br>`});`<br>`app.post('/testOk/:id', (req, res) => {`<br>`    let id = req.params.id;`<br>`    if (id !== 'constructor' && id !== '__proto__') {`<br>`        let items = req.session.todos[id];`<br>`        if (!items) {`<br>`            items = req.session.todos[id] = {};`<br>`        }` | `        items[req.query.name] = req.query.text;`<br>`    }`<br>`    res.end(200);`<br>`});`<br>`function ok1(req, res) {`<br>`    let items = req.session.todos["id"];`<br>`    if (!items) {`<br>`        items = req.session.todos["id"] = {};`<br>`    }` | Java/Spring | `Semgrep prototype-pollution-assignment` | `        items[req.query.name] = req.query.text;     }     res.end(200); }); function ok1(req, res) {     let items = req.session.todos["id"];     if (!items) {         items = req.session.todos["id"] = {};     }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 189 semgrep items req query name text res end 200 app post testok id let params if constructor proto --> |
| JAVA-190 | Semgrep: >- | `    obj = obj[key] \|\| (obj[key] = {});`<br>`  }`<br>`  obj[last] = val;`<br>`}`<br>`function okTest1(name) {`<br>`  if (name.indexOf('.') === -1) {`<br>`    this.config[name] = value;`<br>`    return this;`<br>`  }`<br>`  let config = this.config;`<br>`...`<br>`  const length = name.length;`<br>`  name.forEach((item, index) => {` | `    config = config[index];`<br>`  });`<br>`  return this;`<br>`}`<br>`function okTest2(name) {`<br>`  let config = this.config;`<br>`  name = name.split('.');`<br>`  const length = name.length;`<br>`  for (let i = 0; i < name.length; i++) {` | Java/Spring | `Semgrep prototype-pollution-loop` | `    config = config[index];   });   return this; } function okTest2(name) {   let config = this.config;   name = name.split('.');   const length = name.length;   for (let i = 0; i < name.length; i++) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 190 semgrep obj key last val function oktest1 name if indexof --> |
| JAVA-191 | Semgrep: >- | `const pid = spawnSync('ls', ['-lh', '/usr'], {shell: '/bin/sh'});` | `spawn('ls', ['-lh', '/usr'], {shell:false});` | Java/Spring | `Semgrep spawn-shell-true` | `spawn('ls', ['-lh', '/usr'], {shell:false});` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 191 semgrep const pid spawnsync ls lh usr shell bin sh --> |
| JAVA-192 | Semgrep: >- | `  await connection.raw('`<br>`    INSERT INTO  (id, character, cartoon, link)`<br>`    VALUES(`<br>`        '${req.query.id}',`<br>`        '${req.body.character}',`<br>`        '${req.query.cartoon}',`<br>`        '${req.foo.link}'`<br>`    )`<br>`    ');` | `  await connection.raw('SELECT * FROM foobar');`<br>`};` | Java/Spring | `Semgrep node-knex-sqli` | `  await connection.raw('SELECT * FROM foobar'); };` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 192 semgrep await connection raw insert into id character cartoon link values req query body --> |
| JAVA-193 | Semgrep: >- | `  const dbResult = await request.query("SELECT * FROM [foobar].[dbo].[users] WHERE user_id =" + userInput);`<br>`  return dbResult;`<br>`}`<br>`async function testOk1() {`<br>`  const pool = await new mssql.ConnectionPool({server: 'localhost'});`<br>`  const request = pool.request();`<br>`  const query = "SELECT * FROM [foobar].[dbo].[users] WHERE user_id = 1";` | `  const dbResult = await request.query(query);`<br>`  return dbResult;`<br>`}` | Java/Spring | `Semgrep node-mssql-sqli` | `  const dbResult = await request.query(query);   return dbResult; }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 193 semgrep const dbresult await request query select from foobar dbo users where user id userinput return async function testok1 pool new mssql --> |
| JAVA-194 | Semgrep: >- | `      .query('SELECT * FROM users WHERE id =' + userinput)`<br>`      .then(res => console.log('user:', res.rows[0]))`<br>`      .catch(err =>`<br>`        setImmediate(() => {`<br>`          throw err`<br>`        })`<br>`      )`<br>`}`<br>`function ok1() {`<br>`    const { Client } = require('pg')`<br>`...`<br>`    await client.connect()`<br>`    query = "SELECT * FROM users WHERE email=".concat("hello")` | `    client.query(query)`<br>`}`<br>`function ok2() {`<br>`    const { Client } = require('pg')`<br>`    const client = new Client()`<br>`    await client.connect()`<br>`    query = "SELECT name FROM users WHERE age=" + "3"` | Java/Spring | `Semgrep node-postgres-sqli` | `    client.query(query) } function ok2() {     const { Client } = require('pg')     const client = new Client()     await client.connect()     query = "SELECT name FROM users WHERE age=" + "3"` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 194 semgrep query select from users where id userinput then res console log user rows catch err setimmediate throw --> |
| JAVA-195 | Semgrep: >- | `      compiledTemplate = compiledTemplate.replace('<script id="subtitle"></script>', '<script id="subtitle" type="text/vtt" data-label="English" data-lang="en">' + subs + '</script>')`<br>`      res.send(compiledTemplate)`<br>`    })`<br>`  }`<br>`  function favicon () {`<br>`    return utils.extractFilename(config.get('application.favicon'))`<br>`  }`<br>`}`<br>`function getSubsFromFile () {`<br>`  let subtitles = 'JuiceShopJingle.vtt'`<br>`...`<br>`        return {`<br>`            send: function( _, complete ) {` | `                script = jQuery("<script>").prop({`<br>`                    async: true,`<br>`                    charset: s.scriptCharset,`<br>`                    src: s.url`<br>`                }).on(`<br>`                    "load error",`<br>`                    callback = function( evt ) {`<br>`                        script.remove();`<br>`                        callback = null;`<br>`                        if ( evt ) {`<br>`...`<br>`    }`<br>`});` | Java/Spring | `Semgrep unknown-value-with-script-tag` | `                script = jQuery("<script>").prop({                     async: true,                     charset: s.scriptCharset,                     src: s.url                 }).on(                     "load error",                     callback = function( evt ) {                         script.remove();                         callback = null;                         if ( evt ) { ...     } });` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 195 semgrep compiledtemplate replace script id subtitle type text vtt data label english lang en subs res send function favicon return utils extractfilename --> |
| JAVA-196 | Semgrep: >- | `  const action = window[message.name];`<br>`  action(message.payload);`<br>`}`<br>`let api = {`<br>`  foo: function () { /* do smth */ },`<br>`  bar: function () { /* do smth */ }`<br>`}`<br>`function okTest1(data) {`<br>`  const message = JSON.parse(data);`<br>`  if (!api.hasOwnProperty(message.name)) {`<br>`    return;`<br>`  }` | `  api[message.name](message.payload);`<br>`}`<br>`function okTest2(data) {` | Java/Spring | `Semgrep unsafe-dynamic-method` | `  api[message.name](message.payload); } function okTest2(data) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 196 semgrep const action window message name payload let api foo function do smth bar --> |
| JAVA-197 | Semgrep: >- | `  return util.format(logs, ip);`<br>`}`<br>`function okTest1(data) {`<br>`  const {user, ip} = data`<br>`  foobar(user)`<br>`  const logs = 'Unauthorized access attempt by user'` | `  console.log(logs, ip);`<br>`}`<br>`function okTest2(data) {`<br>`  const {user, ip} = data`<br>`  foobar(user)` | Java/Spring | `Semgrep unsafe-formatstring` | `  console.log(logs, ip); } function okTest2(data) {   const {user, ip} = data   foobar(user)` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 197 semgrep return util format logs ip function oktest1 data const user foobar unauthorized access attempt by --> |
| JAVA-198 | Semgrep: Insecure WebSocket Detected. WebSocket Secure (wss) should be used for all WebSocket connections. | `ws://foo/bar` | `var secure_url = "wss://my/url";` | Java/Spring | `Semgrep detect-insecure-websocket` | `var secure_url = "wss://my/url";` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 198 semgrep insecure websocket detected secure wss should be used for all connections ws foo bar --> |
| JAVA-199 | Semgrep: >- | `  let data = Object.assign(systemData, {foo: true}, jsonData)`<br>`  return doSmthWith(data)`<br>`}`<br>`function okTest1() {`<br>`  const jsonData = JSON.parse('{"one": 1}')` | `  let data = Object.assign(systemData, {foo: true}, jsonData)`<br>`  return doSmthWith(data)`<br>`}`<br>`function okTest2() {`<br>`  const input = '{"one": 1}'`<br>`  const jsonData = JSON.parse(input)` | Java/Spring | `Semgrep insecure-object-assign` | `  let data = Object.assign(systemData, {foo: true}, jsonData)   return doSmthWith(data) } function okTest2() {   const input = '{"one": 1}'   const jsonData = JSON.parse(input)` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 199 semgrep let data object assign systemdata foo true jsondata return dosmthwith function oktest1 const json parse one --> |
| JAVA-200 | Semgrep: >- | `        const gitClone = childProcess.spawn('git', [ 'clone', gitUrl ]);`<br>`        return res.send('ok');`<br>`    }`<br>`}`<br>`function testOk1() {`<br>`    const { spawn } = require('child_process');`<br>`    function downloadGitCommitOk1() {` | `        const gitClone = spawn('git', [ 'clone', 'https://hardcoded-url.com' ]);`<br>`        return res.send('ok');`<br>`    }`<br>`}`<br>`function testOk2() {`<br>`    const childProcess = require('child_process');`<br>`    const gitUrl = 'https://hardcoded-url.com';`<br>`    function downloadGitCommitOk2() {` | Java/Spring | `Semgrep spawn-git-clone` | `        const gitClone = spawn('git', [ 'clone', 'https://hardcoded-url.com' ]);         return res.send('ok');     } } function testOk2() {     const childProcess = require('child_process');     const gitUrl = 'https://hardcoded-url.com';     function downloadGitCommitOk2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 200 semgrep const gitclone childprocess spawn git clone giturl return res send ok function testok1 require child process --> |
| JAVA-201 | Semgrep: >- | `    let decipher = createDecipheriv("chacha20-poly1305", key, iv);`<br>`    let decrypted = decipher.update(ciphertext.slice(0, ciphertext.byteLength - 16 - 12));`<br>`    decrypted = Buffer.concat([decrypted]);`<br>`    return decrypted;`<br>`}`<br>`function decrypt6(ciphertext, key) {`<br>`    iv = ciphertext.iv`<br>`    encryptedData = ciphertext.data`<br>`    auth = ciphertext.auth` | `    var decipher = crypto.createDecipheriv("aes-128-gcm", key, iv);`<br>`    decipher.setAuthTag(auth);`<br>`    let result = Buffer.concat([decipher.update(encryptedData), decipher.final()]);`<br>`    return result;`<br>`}`<br>`function decrypt7(ciphertext, key) {`<br>`    iv = ciphertext.iv`<br>`    encryptedData = ciphertext.data`<br>`    auth = ciphertext.auth` | Java/Spring | `Semgrep aead-no-final` | `    var decipher = crypto.createDecipheriv("aes-128-gcm", key, iv);     decipher.setAuthTag(auth);     let result = Buffer.concat([decipher.update(encryptedData), decipher.final()]);     return result; } function decrypt7(ciphertext, key) {     iv = ciphertext.iv     encryptedData = ciphertext.data     auth = ciphertext.auth` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 201 semgrep let decipher createdecipheriv chacha20 poly1305 key iv decrypted update ciphertext slice bytelength 16 12 buffer concat return function decrypt6 --> |
| JAVA-202 | Semgrep: >- | `    const cipher = createCipher("aes-256-ccm", key, {authTagLength: 16})`<br>`    cipher.setAAD(Buffer.alloc(0), {plaintextLength: plaintext.length})`<br>`    let result = cipher.update(plaintext) + cipher.final()`<br>`    return result + cipher.getAuthTag()`<br>`}`<br>`function decrypt3(key, ciphertext) {`<br>`    let encrypted = Buffer.from(ciphertext, 'base64');`<br>`    let iv = encrypted.slice(encrypted.byteLength - 12, encrypted.byteLength);` | `    let decipher = crypto.createDecipheriv("aes-256-ocb", key, iv, {authTagLength: 16});`<br>`    let decrypted = decipher.update(encrypted.slice(0, encrypted.byteLength - 16 - 12));`<br>`    decrypted = Buffer.concat([decrypted]);`<br>`    return decrypted;`<br>`}`<br>`function encrypt2(key, plaintext) {`<br>`    let iv = crypto.randomBytes(12);` | Java/Spring | `Semgrep create-de-cipher-no-iv` | `    let decipher = crypto.createDecipheriv("aes-256-ocb", key, iv, {authTagLength: 16});     let decrypted = decipher.update(encrypted.slice(0, encrypted.byteLength - 16 - 12));     decrypted = Buffer.concat([decrypted]);     return decrypted; } function encrypt2(key, plaintext) {     let iv = crypto.randomBytes(12);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 202 semgrep const cipher createcipher aes 256 ccm key authtaglength 16 setaad buffer alloc plaintextlength plaintext length let result update final return getauthtag --> |
| JAVA-203 | Semgrep: >- | `    const decipher = crypto.createDecipheriv("aes-192-gcm", key, iv);`<br>`    decipher.setAuthTag(auth);`<br>`    let result = decipher.update(encryptedData) + decipher.final();`<br>`    return result.toString("utf8");`<br>`}`<br>`function decrypt3(ciphertext, key) {`<br>`    iv = ciphertext.iv`<br>`    encryptedData = ciphertext.data`<br>`    auth = ciphertext.auth` | `    var decipher = crypto.createDecipheriv("aes-128-gcm", key, iv, {authTagLength: 16});`<br>`    decipher.setAuthTag(auth);`<br>`    let result = Buffer.concat([decipher.update(encryptedData), decipher.final()]);`<br>`    return result;`<br>`}`<br>`function decrypt4(ciphertext, key) {`<br>`    iv = ciphertext.iv`<br>`    encryptedData = ciphertext.data`<br>`    auth = ciphertext.auth`<br>`    // even though auth tag is shorter than it should be, this looks like a conscious choice` | Java/Spring | `Semgrep gcm-no-tag-length` | `    var decipher = crypto.createDecipheriv("aes-128-gcm", key, iv, {authTagLength: 16});     decipher.setAuthTag(auth);     let result = Buffer.concat([decipher.update(encryptedData), decipher.final()]);     return result; } function decrypt4(ciphertext, key) {     iv = ciphertext.iv     encryptedData = ciphertext.data     auth = ciphertext.auth     // even though auth tag is shorter than it shoul...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 203 semgrep const decipher crypto createdecipheriv aes 192 gcm key iv setauthtag auth let result update encrypteddata final return tostring utf8 function decrypt3 --> |
| JAVA-204 | Semgrep: >- | `    const {Parser} = require('node-expat')`<br>`    const parser = new Parser('UTF-8')`<br>`    parser.write(input)`<br>`}`<br>`function okTest3() {` | `    var expat = require('node-expat')`<br>`    var parser = new expat.Parser('UTF-8')`<br>`    parser.parse("safe input")`<br>`}`<br>`function okTest4() {` | Java/Spring | `Semgrep expat-xxe` | `    var expat = require('node-expat')     var parser = new expat.Parser('UTF-8')     parser.parse("safe input") } function okTest4() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 204 semgrep const parser require node expat new utf write input function oktest3 --> |
| JAVA-205 | Semgrep: >- | `  await page.evaluateJavaScript(userInput);` | `  var url = 'https://stackoverflow.com/'`<br>`  const status = await page.openUrl(url, {}, {});`<br>`  const content = await page.property('content');`<br>`  console.log(content);`<br>`  await instance.exit();`<br>`})();` | Java/Spring | `Semgrep phantom-injection` | `  var url = 'https://stackoverflow.com/'   const status = await page.openUrl(url, {}, {});   const content = await page.property('content');   console.log(content);   await instance.exit(); })();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 205 semgrep await page evaluatejavascript userinput --> |
| JAVA-206 | Semgrep: >- | `  const browser = await chromium.launch({args:['--remote-debugging-port=${port}','--somethin-else']});`<br>`  const page = await browser.newPage();`<br>`  await page.goto('https://example.com');`<br>`  await browser.close();`<br>`})();`<br>`(async () => {` | `  const browser = await chromium.launch({args:['--somethin-else', '--more-examples']});`<br>`  const page = await browser.newPage();`<br>`  await page.goto('https://example.com');`<br>`  await browser.close();`<br>`})();` | Java/Spring | `Semgrep playwright-exposed-chrome-devtools` | `  const browser = await chromium.launch({args:['--somethin-else', '--more-examples']});   const page = await browser.newPage();   await page.goto('https://example.com');   await browser.close(); })();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 206 semgrep const browser await chromium launch args remote debugging port somethin else page newpage goto https example com close async --> |
| JAVA-207 | Semgrep: >- | `    s.run('lol(${userInput})', cb);`<br>`}`<br>`function okTest1(cb) {`<br>`    const s = new Sandbox();` | `    s.run('lol("hi")', cb);`<br>`}`<br>`function okTest2(cb) {`<br>`    const s = new Sandbox();`<br>`    var code = 'lol("hi")'` | Java/Spring | `Semgrep sandbox-code-injection` | `    s.run('lol("hi")', cb); } function okTest2(cb) {     const s = new Sandbox();     var code = 'lol("hi")'` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 207 semgrep run lol userinput cb function oktest1 const new sandbox --> |
| JAVA-208 | Semgrep: >- | `    var saxStream = require("sax").createStream(strict, options)`<br>`    saxStream.on("opentag", function (node) {`<br>`        // same object as above`<br>`    })`<br>`    saxStream.on("doctype", function (node) {`<br>`        processType(node)`<br>`    })`<br>`    fs.createReadStream("file.xml")`<br>`        .pipe(saxStream)`<br>`        .pipe(fs.createWriteStream("file-copy.xml"))`<br>`}`<br>`function okTest1() {` | `    var saxStream = require("sax").createStream(strict, options)`<br>`    saxStream.on("ontext", function (node) {`<br>`        // same object as above`<br>`    })`<br>`    fs.createReadStream("file.xml").pipe(saxStream).pipe(fs.createWriteStream("file-copy.xml"))`<br>`}` | Java/Spring | `Semgrep sax-xxe` | `    var saxStream = require("sax").createStream(strict, options)     saxStream.on("ontext", function (node) {         // same object as above     })     fs.createReadStream("file.xml").pipe(saxStream).pipe(fs.createWriteStream("file-copy.xml")) }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 208 semgrep var saxstream require sax createstream strict options on opentag function node same object as above doctype processtype --> |
| JAVA-209 | Semgrep: >- | `  dev: {`<br>`    username: "0xdbe",`<br>`    database: "app_db",`<br>`    dialect: "postgres",`<br>`    host: "127.0.0.1",`<br>`    dialectOptions: {`<br>`      ssl: {`<br>`        rejectUnauthorized: false`<br>`      }`<br>`    }`<br>`...`<br>`// Example for postgresql`<br>`module.exports = {` | `  dev: {`<br>`    username: "0xdbe",`<br>`    database: "app_db",`<br>`    dialect: "postgres",`<br>`    host: "127.0.0.1",`<br>`  }`<br>`};` | Java/Spring | `Semgrep sequelize-tls-disabled-cert-validation` | `  dev: {     username: "0xdbe",     database: "app_db",     dialect: "postgres",     host: "127.0.0.1",   } };` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 209 semgrep dev username 0xdbe database app db dialect postgres host 127 dialectoptions --> |
| JAVA-210 | Semgrep: >- | `      ssl: {`<br>`        minVersion: 'TLSv1.1'`<br>`      }`<br>`    }`<br>`  }`<br>`};`<br>`module.exports = {`<br>`  local: {`<br>`    username: "AppUser",`<br>`    database: "AppDb",`<br>`...`<br>`    host: "127.0.0.1",`<br>`    dialectOptions: {` | `      ssl: {`<br>`        minVersion: 'TLSv1.2'`<br>`      }`<br>`    }`<br>`  }`<br>`};` | Java/Spring | `Semgrep sequelize-weak-tls-version` | `      ssl: {         minVersion: 'TLSv1.2'       }     }   } };` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 210 semgrep ssl minversion tlsv1 --> |
| JAVA-211 | Semgrep: >- | `    const result = serialize({foo: '<img src=x />'}, {unsafe: true, space: 2})`<br>`    return result`<br>`}`<br>`function testOk() {` | `    const result = serialize({foo: '<img src=x />'}, {space: 2})`<br>`    return result`<br>`}`<br>`function testOk2() {` | Java/Spring | `Semgrep unsafe-serialize-javascript` | `    const result = serialize({foo: '<img src=x />'}, {space: 2})     return result } function testOk2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 211 semgrep const result serialize foo img src unsafe true space return function testok --> |
| JAVA-212 | Semgrep: >- | `    return shell.exec(input, {silent: true})`<br>`}`<br>`function okTest3(userInput) {` | `    const input = 'ls ./'`<br>`    return shell.exec(input, {silent: true})`<br>`}`<br>`function okTest4(userInput) {` | Java/Spring | `Semgrep shelljs-exec-injection` | `    const input = 'ls ./'     return shell.exec(input, {silent: true}) } function okTest4(userInput) {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 212 semgrep return shell exec input silent true function oktest3 userinput --> |
| JAVA-213 | Semgrep: >- | `    var params = { multiArgs: [ 'one', 'tow', 'three' ] };`<br>`    var promise = thenify(function (callback) {`<br>`        callback(null, 1, 2, 3);`<br>`    }, params);`<br>`}`<br>`function ok1() {` | `    var promise = thenify(function (callback) {`<br>`        callback(null, 1, 2, 3);`<br>`    }, { multiArgs: false });`<br>`}`<br>`function ok2() {` | Java/Spring | `Semgrep multiargs-code-execution` | `    var promise = thenify(function (callback) {         callback(null, 1, 2, 3);     }, { multiArgs: false }); } function ok2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 213 semgrep var params multiargs one tow three promise thenify function callback null ok1 --> |
| JAVA-214 | Semgrep: >- | `function test2(input) {`<br>`  const sandbox = {`<br>`    setTimeout,`<br>`    input`<br>`  };`<br>`  const nodeVM = new NodeVM({timeout: 40 * 1000, sandbox});`<br>`  return nodeVM.run('console.log("Hello world")')`<br>`}` | `async function okTest1() {`<br>`  code = '`<br>`    console.log("Hello world")`<br>`  ';`<br>`  const sandbox = {`<br>`    setTimeout,`<br>`    fs`<br>`  };`<br>`  return new VM({timeout: 40 * 1000, sandbox}).run(code);`<br>`}` | Java/Spring | `Semgrep vm2-context-injection` | `async function okTest1() {   code = `     console.log("Hello world")   `;   const sandbox = {     setTimeout,     fs   };   return new VM({timeout: 40 * 1000, sandbox}).run(code); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 214 semgrep function test2 input const sandbox settimeout nodevm new timeout 40 1000 --> |
| JAVA-215 | Semgrep: >- | `  return wkhtmltopdf(userInput, { output: 'vuln.pdf' })`<br>`}` | `wkhtmltopdf('<html><html/>', { output: 'vuln.pdf' })`<br>`function okTest(userInput) {`<br>`   var html = '<html><html/>';` | Java/Spring | `Semgrep wkhtmltopdf-injection` | `wkhtmltopdf('<html><html/>', { output: 'vuln.pdf' }) function okTest(userInput) {    var html = '<html><html/>';` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 215 semgrep return wkhtmltopdf userinput output vuln pdf --> |
| JAVA-216 | Semgrep: >- | `    const xml2json = require('xml2json')`<br>`    const result = xml2json.toJson(body, { object: true, arrayNotation: true })`<br>`    return result`<br>`}`<br>`function okTest1() {` | `    const xml2json = require('xml2json')`<br>`    const result = xml2json.toJson('<xml></xml>', { object: true, arrayNotation: true })`<br>`    return result`<br>`}`<br>`function okTest1() {` | Java/Spring | `Semgrep xml2json-xxe` | `    const xml2json = require('xml2json')     const result = xml2json.toJson('<xml></xml>', { object: true, arrayNotation: true })     return result } function okTest1() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 216 semgrep const xml2json require result tojson body object true arraynotation return function oktest1 --> |
| JAVA-217 | Semgrep: `$X` is assigned twice; the first assignment is useless | `x3 = 1;`<br>`x3 = 2;` | `x4 = {value1: 42};`<br>`x4 = {x4, value2: 43};` | Java/Spring | `Semgrep useless-assignment` | `x4 = {value1: 42}; x4 = {x4, value2: 43};` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 217 semgrep is assigned twice the first assignment useless x3 --> |
| JAVA-218 | Semgrep: spring-ftp-request | `        DefaultFtpSessionFactory ftpSessionFactory = new DefaultFtpSessionFactory();`<br>`        String url = "ftp://example.com";`<br>`        ftpSessionFactory.setHost(url);`<br>`        ftpSessionFactory.setPort(properties.getPort());`<br>`        ftpSessionFactory.setUsername(properties.getUsername());`<br>`        ftpSessionFactory.setPassword(properties.getPassword());`<br>`        ftpSessionFactory.setClientMode(properties.getClientMode().getMode());`<br>`        if (properties.getCacheSessions() != null) {`<br>`            CachingSessionFactory<FTPFile> csf = new CachingSessionFactory<>(ftpSessionFactory);`<br>`            return csf;`<br>`...`<br>`    @ConditionalOnMissingBean`<br>`    public SessionFactory<FTPFile> ok1(FtpSessionFactoryProperties properties) {` | `        DefaultFtpSessionFactory ftpSessionFactory = new DefaultFtpSessionFactory();`<br>`        ftpSessionFactory.setHost("sftp://example.com");`<br>`        ftpSessionFactory.setPort(properties.getPort());`<br>`        ftpSessionFactory.setUsername(properties.getUsername());`<br>`        ftpSessionFactory.setPassword(properties.getPassword());`<br>`        ftpSessionFactory.setClientMode(properties.getClientMode().getMode());`<br>`        if (properties.getCacheSessions() != null) {`<br>`            CachingSessionFactory<FTPFile> csf = new CachingSessionFactory<>(ftpSessionFactory);`<br>`            return csf;`<br>`        }`<br>`...`<br>`    @ConditionalOnMissingBean`<br>`    public SessionFactory<FTPFile> ok2(FtpSessionFactoryProperties properties) {` | Java/Spring | `Semgrep spring-ftp-request` | `        DefaultFtpSessionFactory ftpSessionFactory = new DefaultFtpSessionFactory();         ftpSessionFactory.setHost("sftp://example.com");         ftpSessionFactory.setPort(properties.getPort());         ftpSessionFactory.setUsername(properties.getUsername());         ftpSessionFactory.setPassword(properties.getPassword());         ftpSessionFactory.setClientMode(properties.getClientMode().getM...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 218 semgrep spring ftp request defaultftpsessionfactory ftpsessionfactory new string url example com sethost setport properties getport setusername getusername setpassword getpassword --> |
| JAVA-219 | Semgrep: spring-http-request | `        restTemplate template = new RestTemplate();`<br>`        Foo updatedInstance = new Foo("newName");`<br>`        updatedInstance.setId(createResponse.getBody().getId());`<br>`        String resourceUrl = "http://example.com";`<br>`        HttpEntity<Foo> requestUpdate = new HttpEntity<>(updatedInstance, headers);`<br>`        template.exchange(resourceUrl, HttpMethod.PUT, requestUpdate, Void.class);`<br>`    }`<br>`}`<br>`public class Ok {`<br>`    public void ok1() {` | `        RestTemplate restTemplate = new RestTemplate();`<br>`        return restTemplate.delete("https://example.com");`<br>`    }`<br>`    public void ok2() {` | Java/Spring | `Semgrep spring-http-request` | `        RestTemplate restTemplate = new RestTemplate();         return restTemplate.delete("https://example.com");     }     public void ok2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 219 semgrep spring http request resttemplate template new foo updatedinstance newname setid createresponse getbody getid string resourceurl example com httpentity requestupdate headers exchange --> |
| JAVA-220 | Semgrep: disallow-old-tls-versions1 | `        SSLConnectionSocketFactory sf = new SSLConnectionSocketFactory(`<br>`                sslContext,`<br>`                new String[] {"TLSv1", "TLSv1.1", "SSLv3"},`<br>`                null,`<br>`                SSLConnectionSocketFactory.BROWSER_COMPATIBLE_HOSTNAME_VERIFIER);`<br>`    }`<br>`}`<br>`class Ok {`<br>`    public void ok_disable_old_tls1() {` | `        SSLConnectionSocketFactory sf = new SSLConnectionSocketFactory(`<br>`                sslContext,`<br>`                new String[] {"TLSv1.2"},`<br>`                null,`<br>`                SSLConnectionSocketFactory.BROWSER_COMPATIBLE_HOSTNAME_VERIFIER);`<br>`    }`<br>`    public void ok_disable_old_tls2() {` | Java/Spring | `Semgrep disallow-old-tls-versions1` | `        SSLConnectionSocketFactory sf = new SSLConnectionSocketFactory(                 sslContext,                 new String[] {"TLSv1.2"},                 null,                 SSLConnectionSocketFactory.BROWSER_COMPATIBLE_HOSTNAME_VERIFIER);     }     public void ok_disable_old_tls2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 220 semgrep disallow old tls versions1 sslconnectionsocketfactory sf new sslcontext string tlsv1 sslv3 null browser compatible hostname verifier --> |
| JAVA-221 | Semgrep: disallow-old-tls-versions2 | `        java.lang.System.setProperty("jdk.tls.client.protocols", "TLSv1.2,TLSv1.3,SSLv3");`<br>`    }`<br>`}`<br>`public class Ok {`<br>`    public void bad1() {` | `        java.lang.System.setProperty("jdk.tls.client.protocols", "TLSv1.2,TLSv1.3");`<br>`    }`<br>`    public void bad1() {` | Java/Spring | `Semgrep disallow-old-tls-versions2` | `        java.lang.System.setProperty("jdk.tls.client.protocols", "TLSv1.2,TLSv1.3");     }     public void bad1() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 221 semgrep disallow old tls versions2 lang system setproperty jdk client protocols tlsv1 sslv3 public class ok void bad1 --> |
| JAVA-222 | Semgrep: http-components-request | `        CloseableHttpClient httpclient = HttpClients.createDefault();`<br>`        CloseableHttpResponse response1 = httpclient.execute(new HttpPost("http://example.com"));`<br>`    }`<br>`}`<br>`class Ok {`<br>`    public void ok1() {` | `        CloseableHttpClient httpclient = HttpClients.createDefault();`<br>`        HttpGet httpGet = new HttpGet("https://targethost/homepage");`<br>`        CloseableHttpResponse response1 = httpclient.execute(httpGet);`<br>`    }`<br>`    public void ok2() {` | Java/Spring | `Semgrep http-components-request` | `        CloseableHttpClient httpclient = HttpClients.createDefault();         HttpGet httpGet = new HttpGet("https://targethost/homepage");         CloseableHttpResponse response1 = httpclient.execute(httpGet);     }     public void ok2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 222 semgrep http components request closeablehttpclient httpclient httpclients createdefault closeablehttpresponse response1 execute new httppost example com class ok public void ok1 --> |
| JAVA-223 | Semgrep: httpclient-http-request | `                .uri(URI.create(uri))`<br>`                .POST(BodyPublishers.ofString(data))`<br>`                .build();`<br>`        client.sendAsync(request, BodyHandlers.ofString())`<br>`            .thenApply(HttpResponse::body)`<br>`            .thenAccept(System.out::println)`<br>`            .join();`<br>`    }`<br>`}`<br>`class Ok {`<br>`...`<br>`        HttpClient client = HttpClient.newHttpClient();`<br>`        HttpRequest request = HttpRequest.newBuilder()` | `            .uri(URI.create("https://openjdk.java.net/"))`<br>`            .build();`<br>`        client.sendAsync(request, BodyHandlers.ofString())`<br>`            .thenApply(HttpResponse::body)`<br>`            .thenAccept(System.out::println)`<br>`            .join();`<br>`    }`<br>`    public void sendok2() {`<br>`        HttpClient client = HttpClient.newHttpClient();`<br>`        HttpRequest request = HttpRequest.newBuilder()` | Java/Spring | `Semgrep httpclient-http-request` | `            .uri(URI.create("https://openjdk.java.net/"))             .build();         client.sendAsync(request, BodyHandlers.ofString())             .thenApply(HttpResponse::body)             .thenAccept(System.out::println)             .join();     }     public void sendok2() {         HttpClient client = HttpClient.newHttpClient();         HttpRequest request = HttpRequest.newBuilder()` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 223 semgrep httpclient http request uri create post bodypublishers ofstring data build client sendasync bodyhandlers thenapply httpresponse body thenaccept system out println --> |
| JAVA-224 | Semgrep: httpget-http-request | `        HttpGet httpGet = new HttpGet(url);`<br>`        HttpClients.createDefault().execute(httpGet);`<br>`    }`<br>`}`<br>`public class Ok {`<br>`    private static void sendok1() throws IOException {` | `        HttpGet httpGet = new HttpGet("https://example.com");`<br>`        HttpClients.createDefault().execute(httpGet);`<br>`    }`<br>`    private static void sendok2() throws IOException {`<br>`        String url = "https://example.com";` | Java/Spring | `Semgrep httpget-http-request` | `        HttpGet httpGet = new HttpGet("https://example.com");         HttpClients.createDefault().execute(httpGet);     }     private static void sendok2() throws IOException {         String url = "https://example.com";` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 224 semgrep httpget http request new url httpclients createdefault execute public class ok private static void sendok1 throws ioexception --> |
| JAVA-225 | Semgrep: httpurlconnection-http-request | `            URL urlObj = new URL("http://example.com");`<br>`            URLConnection urlCon = urlObj.openConnection();`<br>`            int responseCode = urlCon.getResponseCode();`<br>`    }`<br>`}`<br>`public class Ok {`<br>`    private static void sendok1() throws IOException {` | `            URL obj = new URL("https://example.com");`<br>`            HttpURLConnection con = (HttpURLConnection) obj.openConnection();`<br>`            con.setRequestMethod("GET");`<br>`            con.setRequestProperty("User-Agent", USER_AGENT);`<br>`            int responseCode = con.getResponseCode();`<br>`            con.connect();`<br>`    }`<br>`    private static void sendok2() throws IOException {`<br>`            String url = "https://example.com";` | Java/Spring | `Semgrep httpurlconnection-http-request` | `            URL obj = new URL("https://example.com");             HttpURLConnection con = (HttpURLConnection) obj.openConnection();             con.setRequestMethod("GET");             con.setRequestProperty("User-Agent", USER_AGENT);             int responseCode = con.getResponseCode();             con.connect();     }     private static void sendok2() throws IOException {             String url ...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 225 semgrep httpurlconnection http request url urlobj new example com urlconnection urlcon openconnection int responsecode getresponsecode public class ok --> |
| JAVA-226 | Semgrep: socket-request | `        pingSocket = new Socket(servername, 23);`<br>`        out = new PrintWriter(pingSocket.getOutputStream(), true);`<br>`        in = new BufferedReader(new InputStreamReader(pingSocket.getInputStream()));`<br>`            out.println("ping");`<br>`            System.out.println(in.readLine());`<br>`            out.close();`<br>`            in.close();`<br>`            pingSocket.close();`<br>`    }`<br>`}`<br>`class Ok {`<br>`    public void oksocket1() {` | `        pingSocket = new Socket("ssh://example.com", 22);`<br>`        out = new PrintWriter(pingSocket.getOutputStream(), true);`<br>`        in = new BufferedReader(new InputStreamReader(pingSocket.getInputStream()));`<br>`            out.println("ping");`<br>`            System.out.println(in.readLine());`<br>`            out.close();`<br>`            in.close();`<br>`            pingSocket.close();`<br>`    }`<br>`    public void oksocket2() {` | Java/Spring | `Semgrep socket-request` | `        pingSocket = new Socket("ssh://example.com", 22);         out = new PrintWriter(pingSocket.getOutputStream(), true);         in = new BufferedReader(new InputStreamReader(pingSocket.getInputStream()));             out.println("ping");             System.out.println(in.readLine());             out.close();             in.close();             pingSocket.close();     }     public void oksocke...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 226 semgrep socket request pingsocket new servername 23 out printwriter getoutputstream true in bufferedreader inputstreamreader getinputstream println ping system readline close --> |
| JAVA-227 | Semgrep: tls-renegotiation | `        java.lang.System.setProperty("sun.security.ssl.allowUnsafeRenegotiation", true);`<br>`    }`<br>`}`<br>`class Ok {`<br>`    public void ok1() {` | `        java.lang.System.setProperty("sun.security.ssl.allowUnsafeRenegotiation", false);`<br>`    }`<br>`}` | Java/Spring | `Semgrep tls-renegotiation` | `        java.lang.System.setProperty("sun.security.ssl.allowUnsafeRenegotiation", false);     } }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 227 semgrep tls renegotiation lang system setproperty sun security ssl allowunsaferenegotiation true class ok public void ok1 --> |
| JAVA-228 | Semgrep: unirest-http-request | `        Unirest.get("http://httpbin.org")`<br>`            queryString("fruit", "apple")`<br>`            .queryString("droid", "R2D2")`<br>`            .asString();`<br>`    }`<br>`}`<br>`class Ok {`<br>`    public void ok1() {` | `        HttpResponse<JsonNode> response = Unirest.post("https://httpbin.org/post")`<br>`            .header("accept", "application/json")`<br>`            .queryString("apiKey", "123")`<br>`            .field("parameter", "value")`<br>`            .field("firstname", "Gary")`<br>`            .asJson();`<br>`    }`<br>`    public void ok2() {` | Java/Spring | `Semgrep unirest-http-request` | `        HttpResponse<JsonNode> response = Unirest.post("https://httpbin.org/post")             .header("accept", "application/json")             .queryString("apiKey", "123")             .field("parameter", "value")             .field("firstname", "Gary")             .asJson();     }     public void ok2() {` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 228 semgrep unirest http request get httpbin org querystring fruit apple droid r2d2 asstring --> |
| JAVA-229 | Semgrep: rest-http-client-support | `    got('http://api.nasa.gov/planetary/apod?api_key=DEMO_KEY', { json: true }).then(response => {`<br>`    console.log(response.body.url);`<br>`    console.log(response.body.explanation);`<br>`    }).catch(error => {`<br>`    console.log(error.response.body);`<br>`    });`<br>`}` | `const got = require('got');`<br>`function ok_http6() {`<br>`    got('https://api.nasa.gov/planetary/apod?api_key=DEMO_KEY', { json: true }).then(response => {`<br>`    console.log(response.body.url);`<br>`    console.log(response.body.explanation);`<br>`    }).catch(error => {`<br>`    console.log(error.response.body);`<br>`    });`<br>`}` | Java/Spring | `Semgrep rest-http-client-support` | `const got = require('got'); function ok_http6() {     got('https://api.nasa.gov/planetary/apod?api_key=DEMO_KEY', { json: true }).then(response => {     console.log(response.body.url);     console.log(response.body.explanation);     }).catch(error => {     console.log(error.response.body);     }); }` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 229 semgrep rest http client support got api nasa gov planetary apod key demo json true then response console log body url explanation --> |
| JAVA-230 | CryptoAPI-Bench brokencrypto: BrokenCryptoBBCase1 | `KeyGenerator keyGen = KeyGenerator.getInstance("DES");`<br>`SecretKey key = keyGen.generateKey();`<br>`Cipher cipher = Cipher.getInstance("DES/ECB/PKCS5Padding");`<br>`cipher.init(Cipher.ENCRYPT_MODE, key);` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES");`<br>`SecretKey key = keyGen.generateKey();`<br>`Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");`<br>`cipher.init(Cipher.ENCRYPT_MODE, key);` | Java | `CryptoAPI-Bench BrokenCryptoBBCase1` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES"); SecretKey key = keyGen.generateKey(); Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding"); cipher.init(Cipher.ENCRYPT_MODE, key);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 230 cryptoapi bench brokencrypto brokencryptobbcase1 keygenerator keygen getinstance des secretkey key generatekey cipher ecb pkcs5padding init encrypt mode --> |
| JAVA-231 | CryptoAPI-Bench brokencrypto: BrokenCryptoBBCase2 | `KeyGenerator keyGen = KeyGenerator.getInstance("Blowfish");`<br>`SecretKey key = keyGen.generateKey();`<br>`Cipher cipher = Cipher.getInstance("Blowfish");`<br>`cipher.init(Cipher.ENCRYPT_MODE, key);` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES");`<br>`SecretKey key = keyGen.generateKey();`<br>`Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");`<br>`cipher.init(Cipher.ENCRYPT_MODE, key);` | Java | `CryptoAPI-Bench BrokenCryptoBBCase2` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES"); SecretKey key = keyGen.generateKey(); Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding"); cipher.init(Cipher.ENCRYPT_MODE, key);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 231 cryptoapi bench brokencrypto brokencryptobbcase2 keygenerator keygen getinstance blowfish secretkey key generatekey cipher init encrypt mode --> |
| JAVA-232 | CryptoAPI-Bench brokenhash: BrokenHashBBCase1 | `String name = "abcdef";`<br>`MessageDigest md = MessageDigest.getInstance("SHA1");`<br>`md.update(name.getBytes());`<br>`System.out.println(md.digest());` | `try {`<br>`String name = "abcdef";`<br>`MessageDigest md = MessageDigest.getInstance("SHA-256");`<br>`md.update(name.getBytes());`<br>`System.out.println(md.digest());`<br>`} catch (NoSuchAlgorithmException e) {`<br>`e.printStackTrace();` | Java | `CryptoAPI-Bench BrokenHashBBCase1` | `try { String name = "abcdef"; MessageDigest md = MessageDigest.getInstance("SHA-256"); md.update(name.getBytes()); System.out.println(md.digest()); } catch (NoSuchAlgorithmException e) { e.printStackTrace();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 232 cryptoapi bench brokenhash brokenhashbbcase1 string name abcdef messagedigest md getinstance sha1 update getbytes system out println digest --> |
| JAVA-233 | CryptoAPI-Bench brokenmac: BrokenMacBBCase1 | `KeyGenerator keyGen = KeyGenerator.getInstance("AES");`<br>`SecureRandom secRandom = new SecureRandom();`<br>`keyGen.init(secRandom);`<br>`Key key = keyGen.generateKey();`<br>`Mac mac = Mac.getInstance("HmacMD5");`<br>`mac.init(key);`<br>`String msg = new String("TSE2021");`<br>`byte[] bytes = msg.getBytes();`<br>`byte[] macResult = mac.doFinal(bytes);` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES");`<br>`SecureRandom secRandom = new SecureRandom();`<br>`keyGen.init(secRandom);`<br>`Key key = keyGen.generateKey();`<br>`Mac mac = Mac.getInstance("HmacSHA256");`<br>`mac.init(key);`<br>`String msg = new String("TSE2021");`<br>`byte[] bytes = msg.getBytes();`<br>`byte[] macResult = mac.doFinal(bytes);` | Java | `CryptoAPI-Bench BrokenMacBBCase1` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES"); SecureRandom secRandom = new SecureRandom(); keyGen.init(secRandom); Key key = keyGen.generateKey(); Mac mac = Mac.getInstance("HmacSHA256"); mac.init(key); String msg = new String("TSE2021"); byte[] bytes = msg.getBytes(); byte[] macResult = mac.doFinal(bytes);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 233 cryptoapi bench brokenmac brokenmacbbcase1 keygenerator keygen getinstance aes securerandom secrandom new init key generatekey mac hmacmd5 --> |
| JAVA-234 | CryptoAPI-Bench credentialinstring: CredentialInStringBBCase1 | `SecureRandom random = new SecureRandom();`<br>`String defaultKey = String.valueOf(random.ints());`<br>`byte[] keyBytes = defaultKey.getBytes();`<br>`keyBytes = Arrays.copyOf(keyBytes,16);`<br>`SecretKeySpec keySpec = new SecretKeySpec(keyBytes, "AES");`<br>`String originalString = "Testing";`<br>`Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5PADDING");`<br>`cipher.init(Cipher.ENCRYPT_MODE, keySpec);`<br>`String encrypt = Base64.getEncoder().encodeToString(cipher.doFinal(originalString.getBytes("UTF-8")));`<br>`System.out.println(encrypt);` | `SecureRandom random = new SecureRandom();`<br>`byte [] keyBytes = new byte[16];`<br>`random.nextBytes(keyBytes);`<br>`SecretKeySpec keySpec = new SecretKeySpec(keyBytes, "AES");`<br>`String originalString = "Testing";`<br>`Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5PADDING");`<br>`cipher.init(Cipher.ENCRYPT_MODE, keySpec);`<br>`String encrypt = Base64.getEncoder().encodeToString(cipher.doFinal(originalString.getBytes("UTF-8")));`<br>`System.out.println(encrypt);` | Java | `CryptoAPI-Bench CredentialInStringBBCase1` | `SecureRandom random = new SecureRandom(); byte [] keyBytes = new byte[16]; random.nextBytes(keyBytes); SecretKeySpec keySpec = new SecretKeySpec(keyBytes, "AES"); String originalString = "Testing"; Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5PADDING"); cipher.init(Cipher.ENCRYPT_MODE, keySpec); String encrypt = Base64.getEncoder().encodeToString(cipher.doFinal(originalString.getBytes("UTF-8")...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 234 cryptoapi bench credentialinstring credentialinstringbbcase1 securerandom random new string defaultkey valueof ints byte keybytes getbytes arrays copyof 16 secretkeyspec keyspec aes originalstring testing --> |
| JAVA-235 | CryptoAPI-Bench http: HttpProtocolBBCase1 | `String url = "http://insects.myspecies.info/";`<br>`System.out.println(new URL(url));` | `String url = "https://www.google.com";`<br>`System.out.println(new URL(url));` | Java | `CryptoAPI-Bench HttpProtocolBBCase1` | `String url = "https://www.google.com"; System.out.println(new URL(url));` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 235 cryptoapi bench http httpprotocolbbcase1 string url insects myspecies info system out println new --> |
| JAVA-236 | CryptoAPI-Bench pbeiteration: LessThan1000IterationPBEBBCase1 | `LessThan1000IterationPBEBBCase1 lt = new LessThan1000IterationPBEBBCase1();`<br>`lt.key2();` | `LessThan1000IterationPBECorrected lt = new LessThan1000IterationPBECorrected();`<br>`lt.key2();` | Java | `CryptoAPI-Bench LessThan1000IterationPBEBBCase1` | `LessThan1000IterationPBECorrected lt = new LessThan1000IterationPBECorrected(); lt.key2();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 236 cryptoapi bench pbeiteration lessthan1000iterationpbebbcase1 lt new key2 --> |
| JAVA-237 | CryptoAPI-Bench predictablecryptographickey: PredictableCryptographicKeyBBCase1 | `byte keyBytes[] = {20,10,30,5,5,6,8,7};`<br>`keyBytes = Arrays.copyOf(keyBytes,16);`<br>`SecretKeySpec keySpec = new SecretKeySpec(keyBytes, "AES");` | `SecureRandom random = new SecureRandom();`<br>`byte [] keyBytes = new byte[16];`<br>`random.nextBytes(keyBytes);`<br>`SecretKeySpec keySpec = new SecretKeySpec(keyBytes, "AES");`<br>`String originalString = "Testing";`<br>`Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5PADDING");`<br>`cipher.init(Cipher.ENCRYPT_MODE, keySpec);`<br>`String encrypt = Base64.getEncoder().encodeToString(cipher.doFinal(originalString.getBytes("UTF-8")));`<br>`System.out.println(encrypt);` | Java | `CryptoAPI-Bench PredictableCryptographicKeyBBCase1` | `SecureRandom random = new SecureRandom(); byte [] keyBytes = new byte[16]; random.nextBytes(keyBytes); SecretKeySpec keySpec = new SecretKeySpec(keyBytes, "AES"); String originalString = "Testing"; Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5PADDING"); cipher.init(Cipher.ENCRYPT_MODE, keySpec); String encrypt = Base64.getEncoder().encodeToString(cipher.doFinal(originalString.getBytes("UTF-8")...` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 237 cryptoapi bench predictablecryptographickey predictablecryptographickeybbcase1 byte keybytes 20 10 30 arrays copyof 16 secretkeyspec keyspec new aes --> |
| JAVA-238 | CryptoAPI-Bench predictablekeystorepassword: PredictableKeyStorePasswordBBCase1 | `String type = "JKS";`<br>`KeyStore ks = KeyStore.getInstance(type);`<br>`cacerts = new URL("https://www.google.com");`<br>`String defaultKey = "changeit";`<br>`ks.load(cacerts.openStream(), defaultKey.toCharArray());` | `String type = "JKS";`<br>`KeyStore ks = KeyStore.getInstance(type);`<br>`cacerts = new URL("https://www.google.com");`<br>`SecureRandom random = new SecureRandom();`<br>`String password = String.valueOf(random.ints());`<br>`ks.load(cacerts.openStream(), password.toCharArray());` | Java | `CryptoAPI-Bench PredictableKeyStorePasswordBBCase1` | `String type = "JKS"; KeyStore ks = KeyStore.getInstance(type); cacerts = new URL("https://www.google.com"); SecureRandom random = new SecureRandom(); String password = String.valueOf(random.ints()); ks.load(cacerts.openStream(), password.toCharArray());` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 238 cryptoapi bench predictablekeystorepassword predictablekeystorepasswordbbcase1 string type jks keystore ks getinstance cacerts new url https www google com defaultkey changeit load openstream tochararray --> |
| JAVA-239 | CryptoAPI-Bench predictablepbepassword: PredictablePBEPasswordBBCase1 | `PredictablePBEPasswordBBCase1 ckp = new PredictablePBEPasswordBBCase1();`<br>`ckp.key();` | `PredictablePBEPasswordCorrected ckp = new PredictablePBEPasswordCorrected();`<br>`ckp.key();` | Java | `CryptoAPI-Bench PredictablePBEPasswordBBCase1` | `PredictablePBEPasswordCorrected ckp = new PredictablePBEPasswordCorrected(); ckp.key();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 239 cryptoapi bench predictablepbepassword predictablepbepasswordbbcase1 ckp new key --> |
| JAVA-240 | CryptoAPI-Bench predictablepbepassword: PredictablePBEPasswordBBCase2 | `PredictablePBEPasswordBBCase2 ckp = new PredictablePBEPasswordBBCase2();`<br>`ckp.key();` | `PredictablePBEPasswordCorrected ckp = new PredictablePBEPasswordCorrected();`<br>`ckp.key();` | Java | `CryptoAPI-Bench PredictablePBEPasswordBBCase2` | `PredictablePBEPasswordCorrected ckp = new PredictablePBEPasswordCorrected(); ckp.key();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 240 cryptoapi bench predictablepbepassword predictablepbepasswordbbcase2 ckp new key --> |
| JAVA-241 | CryptoAPI-Bench predictableseeds: PredictableSeedsBBCase1 | `SecureRandom sr = new SecureRandom();`<br>`byte [] keyBytes = {(byte) 100, (byte) 200};`<br>`sr.setSeed(keyBytes);`<br>`int v = sr.nextInt();`<br>`System.out.println(v);` | `SecureRandom sr = new SecureRandom();`<br>`long l = sr.nextLong();`<br>`sr.setSeed(l);`<br>`int v = sr.nextInt();`<br>`System.out.println(v);` | Java | `CryptoAPI-Bench PredictableSeedsBBCase1` | `SecureRandom sr = new SecureRandom(); long l = sr.nextLong(); sr.setSeed(l); int v = sr.nextInt(); System.out.println(v);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 241 cryptoapi bench predictableseeds predictableseedsbbcase1 securerandom sr new byte keybytes 100 200 setseed int nextint system out println --> |
| JAVA-242 | CryptoAPI-Bench predictableseeds: PredictableSeedsBBCase2 | `byte seed = 100;`<br>`SecureRandom sr = new SecureRandom(new byte[]{seed});`<br>`int v = sr.nextInt();`<br>`System.out.println(v);` | `SecureRandom sr = new SecureRandom();`<br>`long l = sr.nextLong();`<br>`sr.setSeed(l);`<br>`int v = sr.nextInt();`<br>`System.out.println(v);` | Java | `CryptoAPI-Bench PredictableSeedsBBCase2` | `SecureRandom sr = new SecureRandom(); long l = sr.nextLong(); sr.setSeed(l); int v = sr.nextInt(); System.out.println(v);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 242 cryptoapi bench predictableseeds predictableseedsbbcase2 byte seed 100 securerandom sr new int nextint system out println --> |
| JAVA-243 | CryptoAPI-Bench staticinitializationvector: StaticInitializationVectorBBCase1 | `KeyGenerator keyGen = KeyGenerator.getInstance("AES");`<br>`SecretKey key = keyGen.generateKey();`<br>`Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");`<br>`byte [] bytes = "abcde".getBytes();`<br>`IvParameterSpec ivSpec = new IvParameterSpec(bytes);`<br>`cipher.init(Cipher.ENCRYPT_MODE,key,ivSpec);` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES");`<br>`SecretKey key = keyGen.generateKey();`<br>`Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");`<br>`SecureRandom sr = new SecureRandom();`<br>`BigInteger i = BigInteger.valueOf(sr.nextInt());`<br>`byte [] bytes = i.toByteArray();`<br>`IvParameterSpec ivSpec = new IvParameterSpec(bytes);`<br>`cipher.init(Cipher.ENCRYPT_MODE,key,ivSpec);` | Java | `CryptoAPI-Bench StaticInitializationVectorBBCase1` | `KeyGenerator keyGen = KeyGenerator.getInstance("AES"); SecretKey key = keyGen.generateKey(); Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding"); SecureRandom sr = new SecureRandom(); BigInteger i = BigInteger.valueOf(sr.nextInt()); byte [] bytes = i.toByteArray(); IvParameterSpec ivSpec = new IvParameterSpec(bytes); cipher.init(Cipher.ENCRYPT_MODE,key,ivSpec);` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 243 cryptoapi bench staticinitializationvector staticinitializationvectorbbcase1 keygenerator keygen getinstance aes secretkey key generatekey cipher cbc pkcs5padding byte bytes abcde getbytes ivparameterspec ivspec new init --> |
| JAVA-244 | CryptoAPI-Bench staticsalts: StaticSaltsBBCase1 | `StaticSaltsBBCase1 cs = new StaticSaltsBBCase1();`<br>`cs.key2();` | `StaticSaltsCorrected cs = new StaticSaltsCorrected();`<br>`cs.key2();` | Java | `CryptoAPI-Bench StaticSaltsBBCase1` | `StaticSaltsCorrected cs = new StaticSaltsCorrected(); cs.key2();` | Атакующий доставляет входные данные, соответствующие anti-pattern; реальный ущерб зависит от приёмника (sink), конфигурации и границ доверия. | <!-- semantic_anchor: java 244 cryptoapi bench staticsalts staticsaltsbbcase1 cs new key2 --> |
