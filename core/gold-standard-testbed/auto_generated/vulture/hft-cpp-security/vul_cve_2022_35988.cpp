// Vulnerable: VUL-CVE-2022-35988
done);

    if (n == 0 || m == 0) {
      if (n == m || !compute_uv_ || !full_matrices_) {
// --- svd_op_test.py ---
    for i in range(0, len(val), 2):
      self.assertAllEqual(val[i], val[i + 1])
